# Current status

## Latest checkpoint — September26 local

- Actual release: exact5f4a9caaafa94059d49ed8d8eb7bb333d6118dca passed workflow35890842385 and Pages deployment on September23UTC. Public manifest/build ID is `5f4a9ca-9ce1e6c10257`. All three continuing-course/graded-reading/Japanese gates are enabled. CI browser acceptance passed202 with27 documented platform/DEV skips; real local multi-profile Chromium/WebKit passed2; source/build scan321 and audit0high vulnerabilities. Fresh public desktop/mobile-WebKit English/Japanese coaching and Japanese diagnosis/data-control journeys passed6/6; synthetic isolated profiles, not physical-phone proof.
- The existing isolated owner desktop upgraded through the normal Settings/PWA action without re-login. Original1recording and29saved sessions retained identical hashes; no originals or credentials exported. The authenticated English Library now contains82VOA lessons,49BBC episodes and59British Council readers from scheduled metadata. The scheduled workflow35857764090 succeeded; BBC/reading checkedAt1790164800557/1790164808643 reached the actual account. This proves automatic directory delivery, not a fabricated completion or newly learned lesson. The owner's English baseline remains incomplete; no test answers were entered on the owner's behalf.
- Real owner Japanese first-open exposed a startup race absent from the earlier no-op auth-listener fixture: the SDK's late INITIAL_SESSION notification invalidated the initial adoption awaited by cloud.start, so the page tested ownership before its replacement adoption finished. Both language owners subsequently matched and the existing Retry opened diagnosis without another login. This is a real first-open defect, not an account-permission failure. The local patch ignores only INITIAL_SESSION because start already explicitly awaits verified session/membership adoption; all actual auth-change epoch fences and same-owner guards remain. Independent read-only review found no new P1/P2 in the runtime fix. The unit regression failed before the fix, and the actual-SDK isolated browser fixture also reproduced the first-open failure against5f4a9ca.
- Candidate verification:123 related sync/Japanese/auth-fence tests pass, including late INITIAL_SESSION, held-membership SIGNED_OUT/A-to-B/A-to-B-to-A, denied membership and wrong verified identity. Fake time advances SDK GET backoff without extending the5second limit or changing security assertions. Built desktop/mobile-WebKit first-open and reload pass2/2 without a Retry click. The later test-harness follow-up review hit the reviewer quota (reported September29 01:55); no repeated reviewer/provider attempts. This startup patch is not public yet. No backend/schema, model, paid call, budget or owner history changed.

## Latest checkpoint — September24 local

- Candidate f0f24ae workflow35837094376 passed lint/types/full unit, cold SQL, real local Auth/Storage/content/Deno, build/security and multi-profile migration/offline recovery, then stopped before deployment on the catalog browser fixture. The enabled graded-reading request was incorrectly served a VOA-shaped fixture; no production data or provider failed. The public site remains9520397 until a corrected release actually deploys.
- The fixture now supplies all five British Council reading levels, accepts the original omitted VOA1 selector, rejects unknown explicit sources, and keeps the intentional first-source failure/retry assertion. Independent review caught the omitted-selector compatibility issue before release and approved its correction. The cloud-configured ON build passes this exact journey on desktop Chromium and mobile WebKit2/2. The remaining built desktop suite passes61, with12 DEV-source fixtures intentionally skipped. The two affected DEV Japanese dialogue/coaching journeys were updated for explicit-help disclosure and pass2/2, preserving failure recovery, recording, usage and language-isolation assertions. Scoped lint and diff checks pass. No unchanged local SQL/full-unit matrix or paid call was repeated.
- README now describes the actual account/external-course model and the outstanding multilingual/device boundary rather than the obsolete public-V1/no-account state. Android physical acceptance remains waived; desktop/iPhone saved-work continuity, actual enabled imports/new eligible Today delivery and Japanese owner-page acceptance remain open. No backend, model, budget, history or credential configuration changed in this correction.

## Latest checkpoint — September23 local

- Owner evidence/authority changed: supplied physical-iPhone screenshot shows installed `9520397-3d524bd20861`, independently matched to the current public manifest. Desktop was already verified; Android physical acceptance is now explicitly waived, while responsive support remains. The owner approved<=0.40USD for correction tests with the3USD monthly cap retained. These amendments are recorded in `AGENTS.md` and `FINAL_UPGRADE.md`; do not ask again for Android or the already-supplied iPhone version.
- Reused the existing isolated owner acceptance profile and its normal saved login; no new login, secret export or original-Chrome access. Ordinary account controls saved/read back0.40USD daily/3USD monthly. Exactly one English and one Japanese structured evaluation succeeded on `anthropic/claude-opus-5`, respectively21120ms/3253tokens/0.033085USD and19579ms/3485tokens/0.035645USD; total0.06873USD, no fallback notices. Core English tense/embedded-question and Japanese destination-particle/question-form corrections are appropriate, Chinese explanations are returned, and fluency remains null. Japanese is an explicit language-aware gateway diagnostic, not owner Japanese-page acceptance. Only original artificial answers were submitted; no learner mastery/event was fabricated. Readback: today reported0.07271817USD, month0.074366USD, held2.5047104USD and7unconfirmed unchanged. Metadata-only receipt: ignored `.work/ai-feedback-owner-20260923-once.json`.
- These genuine responses exposed a teaching defect: summaries/next prompts/hints can contain corrected forms before the intended reference reveal; Japanese prose also overstates certainty of listener understanding. Local `CoachingFeedback` now keeps all generated feedback behind an explicit help button, quotes only original fragments present in the saved answer, resets disclosure on changed answer/feedback/language, and labels text-only limitations. Shared English conversation/Japanese coach/dialogue surfaces use it; English repair moves generated hints/patterns away from the initial view. No prompt-only guarantee, acoustic claim, provider retry, model change, schema or save/record rewrite.109 related tests plus typecheck/scoped lint/build and321-file source/build scan pass. Independent scoped read-only review has no actionable new P1/P2; a legacy English repair original-fragment provenance limitation was correctly distinguished from a new regression. Actual built desktop Chromium/mobile WebKit English and Japanese feedback/reveal/reload journeys pass4/4 without retries or overflow; synthetic isolated journals, not owner/physical-device evidence. This candidate is not yet public.
- Read-only inspection in the legitimate owner desktop session also reproduces the screenshot's12conflict groups. All are event history variants:42 TASK_OFFERED operations and2 TASK_STARTED operations across the12groups, not12competing learner answers or audio originals. These event types have zero skill weight; offered-task avoidance accounting deduplicates by taskId. No history was cleared/rebound. This explains the prompt but does not replace a real iPhone→desktop saved-work continuity test.

## Latest checkpoint — September22 local

### Speech scope clarification — no new runtime or paid test

- `SPEECH.md` still presented historical mandatory acoustic/hosted-reference gates without the owner's superseding September13 amendment. Added the current contract at its entry point: publisher human listening, saved recording/replay/retry and honest language feedback remain required; automatic acoustic scoring, Azure provisioning and bulk hosted references do not. Existing optional adapters and safety checks are unchanged. Current Japanese pages already use native publisher dialogue/kana links; English synthesis does not certify Japanese speech, and optional Japanese TTS is not a reason to block those external-reference paths. This documentation alignment adds no feature acceptance, changes no release gate for core learning/sync, and triggers no paid request or unchanged full matrix.

### Bounded live speech checks — functional success, limits retained

- One English transcription through the normal authenticated provider succeeded on `deepgram/nova-3`: the existing public5.20-second synthetic cafe diagnostic matches its reference words,6731ms/0.0003729354USD. One explicit Japanese gateway transcription also succeeded: the public-domain0.83-second「日本語」recording matches,5110ms/0.0000594833USD. The Japanese fixture is [PandaLimitedExpress's PD-self recording](https://commons.wikimedia.org/wiki/File:Ja-nihongo.ogg), with original SHA1 `fb80d7191db55fc689ec3efd83381cd49b8b0a11` verified before normalization. This single-word check is not a connected-speech benchmark or acceptance of the still-disabled Japanese UI. No owner recording or learner evidence was submitted.
- Production CSP correctly blocked a temporary inline codec helper before any Japanese paid request. No policy was changed or bypassed. The public fixture was subsequently normalized with the existing codec in a separate credential-free browser; only the resulting ordinary audio input reached the existing authenticated gateway. No paid retry occurred.
- One normal English synthesis on the currently listed `microsoft/mai-voice-2` with `en-US-Harper:MAI-Voice-2` succeeded in8053ms. Returned52320-byte MP3 decodes in the production browser to2.616seconds/48000Hz with nonzero signal; no fallback notice. This proves synthesis/decoding, not human listening approval, accent certification or acoustic scoring. The raw speech response has no actual-cost receipt, so its cost remains unknown with0.001968USD reserved, not falsely recorded as zero.
- All five text/STT/TTS checks in this round are finished. Normal server summary reports today0.00398817USD plus0.001968USD held and1unconfirmed; month0.005636USD plus2.5047104USD held and7unconfirmed. The prior2.5027424USD/6unconfirmed remain unchanged within those monthly totals. Daily0.05USD/monthly3USD limits remain saved; no keys, permissions, backend code, public release or feature gates changed. Ignored receipts `.work/ai-transcription-owner-20260922-once.json` and `.work/ai-speech-owner-20260922-once.json` retain metadata only. Do not repeat these paid checks or unchanged test matrices.
- Structured coaching remains untested because its current maximum quote exceeds this approved0.05USD allowance; the separately requested<=0.40USD allowance has not been approved. Japanese reference synthesis is not verified/configured by these English checks. Remaining product/device/release gates below stay open; this is not final delivery.

### Owner-authorized live English/Japanese dialogue — passed, broader AI acceptance still open

- The owner reports that version information is visible and explicitly authorizes AI testing within the existing limit. This is not a per-phone exact-ID or physical save/switch/resume observation. The dedicated owner session remains signed in; no login, key export or backend-secret change was needed. Fresh account readback still showed0/0 budgets before this round, so the owner-authorized bounded test set and read back0.05USD daily/3USD monthly. These guarded limits remain saved; the diagnostic is finished, not a recurring job.
- Exactly two legitimate text generations through the authenticated account service succeeded on the currently listed `google/gemini-3.8-flash`: English6811ms/557tokens/0.00150975USD; Japanese7953ms/768tokens/0.002046USD. Total reported charge0.00355575USD; both return natural task-relevant dialogue, and Japanese includes a short Chinese hint. Neither response has a fallback notice. English uses the normal exposed application provider; Japanese is an explicit `learningLanguage: ja` gateway diagnostic through that same authenticated provider, not acceptance of the still-disabled Japanese page or its language-specific local database.
- Normal server usage readback confirms today/week reported0.00355575USD,0held,0unconfirmed; the4new usage rows are2logical requests plus2provider dispatches, not4paid generations. Month reported0.00520358USD, historical held2.50274240USD and6unconfirmed remain intact. No original answer, recording, study completion or mastery evidence was entered/exported. Ignored receipt `.work/ai-dialogue-owner-20260922-once.json` contains only bounded diagnostic metadata.
- Structured feedback, STT and reference speech were not called at this dialogue-only checkpoint; subsequent bounded speech results are recorded above. Read-only configuration metadata shows no fast/strong-model overrides; deployed source therefore uses the listed `anthropic/claude-opus-5` for structured responses. Its shared6000-output-token ceiling alone quotes0.1575USD with the existing safety multiplier, before prompt allowance, exceeding the approved0.05USD round. This is a preflight cost-policy limit, not an observed failed provider request. Asked separately whether to allow a<=0.40USD correction-test allowance while retaining the3USD monthly cap and historical holds; no increase or request is assumed. The superseded failed content-audio pipeline remains untouched.

### Current public release —9520397, delivered with visible version evidence

- Single workflow35637450002 on exact9520397f4ea94245122f3b08c881a9c5cc9fa6f1 passed all verify steps and Pages deployment. Deployment6575490855 succeeded2026-09-21T18:37:50Z at the unchanged public URL. Fresh public HTML serves `index-BebzRomK.js`; credential-free `release.json` reports clean exact revision9520397 and build ID `9520397-3d524bd20861`. This is a delivered subset, not final multilingual acceptance.
- Fresh production desktop Chromium and mobile WebKit compare the embedded and published IDs successfully; mobile has no horizontal overflow. Desktop offline checking clears the comparison and explicitly cannot claim latest. The first WebKit navigation failed before content with `SSL connect error`; one subsequent ordinary connection succeeded without certificate bypass. No physical-iPhone claim is made. Isolated noncredential results are in `.work/release-public-20260922-result.json`.
- The previous dedicated owner acceptance browser had closed. Reopened only its already-created temporary profile (not the owner's original Chrome profile), and its normal session remained signed in. The actual oldcd9a422 worker displayed Update now; using that action upgraded the same account to9520397. Both current and published IDs match, the service worker controls the page, and Synced is visible. Before/after internal integrity comparisons confirm the existing1recording and1saved learning draft are unchanged; no answer, audio, token or profile export was produced. This closes the owner-desktop version/update-preservation gate without another login.
- Japanese/BBC/graded-reading release variables remain absent/OFF. No production backend or AI budget changed, no paid generation occurred. Physical Android/iPhone version and cross-device learning continuity, held imports/new eligible Today delivery, and legitimate English/Japanese AI acceptance remain open. The owner has been asked separately whether to resume the paused AI test budget within the prior3USD cap for a single total<=0.05USD round; no answer is assumed.

### Visible release identity — implementation and review evidence

- Owner clarification: being able to sign in or seeing no update prompt does not establish an installed version. Added a Settings panel showing the embedded build ID/time, the current site's published ID and a timestamped comparison. Each build emits the same public identity into JavaScript and `release.json`; uncommitted/unversioned builds are explicitly labelled. No environment dump, credential, device telemetry or learning data is included.
- Manual checking makes one bounded, credential-free, cache-bypassing manifest request and checks the existing service worker without activating/reloading it. JSON is excluded from offline precaching. Offline, malformed responses and network/worker deadlines report inability to confirm, clear prior comparison state and never masquerade as latest. Explicit updating reuses the existing App consent/reload/recovery path; account state, drafts and data schemas are unchanged.
-30 targeted release/Settings/gate tests, typecheck/scoped lint/build and305-file source/build pattern scan pass. Actual built desktop/mobile Chromium and mobile WebKit version/gate journeys pass6/6; existing real-worker desktop/mobile update/recording-preservation journeys pass6/6 without retries. The mobile version panel was visually inspected; no overflow. These synthetic checks are not physical-phone evidence. No paid API, public deployment, account-budget change or source-import/Japanese flag activation occurred.
- The new Settings-panel update action also passes a targeted real-worker desktop/mobile journey2/2: a manual check prepares the update without reloading, the explicit panel action activates it, and the saved listening draft survives. This fixture changes HTML/worker revisions while retaining the same compiled identity; it proves action wiring, not a second production-build identity. No product rewrite or unchanged full matrix was needed.
- Independent reviewer01a0c51a-8baf-77c3-8e2e-965105ee6f4c initially could not start because of a usage limit (reported retry time05:54). After the owner explicitly reported restored quota, the same reviewer successfully reviewed3a58da4 and the additional Settings-action test: no actionable P1/P2 findings in identity/cache handling, consent, disposal or OFF-by-default release gates. No alternative model or paid-provider retry was needed. This source review is not certification of the dirty local preview artifact or public CDN/physical devices.
- Backend source/migrations are unchanged from the verified main checkpoint. Fresh unauthenticated readiness requests to the dedicated ai/content/speech-assess handlers all return401/SIGN_IN. No backend deployment or budget change was required. The exact-commit frontend candidate subsequently passed the protected Pages workflow and runtime readback above; Japanese/BBC/graded-reading flags remain OFF.

### Actual owner desktop acceptance — login, catalog delivery and recovery verified

- The owner signed in manually in a dedicated isolated browser on the existing public release `index-DuZGU8Fd.js`. Actual UI shows signed-in/synced and successful account-AI connection verification. Authenticated normal synchronization and usage/manifest requests return200. Credentials remain in that browser; no token/profile export or permission relaxation was used. Connection verification is not paid-generation acceptance; budgets/usage holds were not changed and no paid call was made.
- Actual owner-client catalog responses contain VOA Level1/Level2 at52/30entries, with `checkedAt`1789996375813/1789996377977. They correspond to successful recurring workflow35604146290 on24ce2d5. The ordinary account Today page selects a VOA publisher lesson and displays Chinese listening/recall/expression guidance. This proves scheduled metadata reaches the actual account and an external course is assigned, not that a newly discovered post-completion lesson has been learned or that the held BBC/reading imports are active.
- Actual service-worker-controlled offline reload preserves the usable Today guidance; restoring connectivity returns to Synced. Local original recording bytes and sizes match the actual private remote manifest via SHA256, with only pass/count metadata observed and no audio exported. The first immediate post-DOM-load visibility sample was too early; the bounded ready-state check passed while offline. No learner answer, completion or skill score was entered by the tester. Physical phones and a genuine save-on-one-device/resume-on-another journey remain unverified.
- The previous inspection timeout terminated its REPL-launched browser, not the user's original Chrome. The replacement owner-assisted window runs in an independent process so a short inspection deadline cannot terminate its login session. The owner reauthenticated; do not repeat login while this window remains available. Ignored, noncredential evidence: `.work/owner-acceptance-20260922.json`.

### Previous September21 implementation checkpoint — still valid where unchanged

### Controlled Japanese production gate — implemented, not publicly enabled

- One build-time gate now controls all five Japanese routes, navigation, background synchronization, shared sign-out and language-specific data settings. Production requires exact `VITE_JOVE_JAPANESE=1`; absent/false stays OFF. The workflow maps `JOVE_JAPANESE_ENABLED=true` consistently into both build and browser checks. DEV retains its preview label; enabled production does not. Settings no longer initializes the Japanese store while the gate is disabled. No public variable, backend, English history, URL or PWA scope changed.
- Typecheck/scoped lint pass;18 release-flag/Settings tests pass after the last source edit (the earlier26-case selection also included Japanese-space). Actual isolated production OFF/ON bundles with the public project configuration passed desktop Chromium and mobile WebKit: disabled route/settings without creating a Japanese database; enabled language navigation, completed diagnosis/reload and data controls; then disabled rollback preserving the exact Japanese assessment and original English draft. No page errors or Japanese-page overflow; mobile screenshot inspected. Evidence: `.work/japanese-release-browser-20260921-result.json` and `.work/japanese-release-on-{desktop,webkit}-20260921.png`. The first probe's mobile navigation timeout was a test-harness offscreen-menu mistake, fixed by opening the actual collapsed menu; product code was not changed for it.
- Added a small permanent built-bundle gate test to the existing browser suite, including WebKit. Its OFF/ON desktop/mobile checks pass4/4 without retries. Existing DEV-only source-fixture tests remain separate. Independent read-only gate review found no actionable P1/P2. These fresh synthetic profiles block service workers and use no owner session or paid provider; they do not prove PWA upgrades, genuine AI quality or physical-device synchronization. Public Japanese is still held; final goal remains incomplete.

### Japanese course directory maintenance — backend delivered, frontend held

- Exact717948ae295e1c9f488a5dde9e37533d392765f3 passed single CI35532554967, including cold24-migration SQL, real isolated Auth/Storage/content/Deno, multi-profile sync and page E2E. Feature-branch Pages deployment was correctly skipped. Independent operator review closed source/runtime provenance checks. Guarded deployment verified fullSHA, clean relevant code, full23-version history and rebuilt runtime hash, then applied exact migration24 plus its history entry in one transaction. Hosted history matches source, with catalog RLS/security-invoker/client execute denials retained.
- Dedicated content function is deployed at1,913,860bytes/SHA256 `c56354cd2752e3fa84ed64117d4e0fd00700b6c1d220e8ac74b79edecb7977df`; downloaded bytes match and anonymous access returns401/SIGN_IN. No other backend, owner learning record or secret changed; paid calls0. Promoted717948a to main without rerunning unchanged Pages deployment; public frontend remainscd9a422.
- Actual six-source workflow35581424554 succeeded on717948a. Five existing snapshots were not due; Irodori refreshed72 lessons. Independent SQL readback confirms72entries/8,747bytes,0failures, last success2026-09-21T09:06:49Z. Prior actual scheduled five-source runs35537292684 and35564144424 also succeeded. Directory maintenance works, but authenticated new-course delivery into Today and Japanese public activation remain separate gates.
- Current browser-access recheck: Node has no attached browser/agent; plugin discovery found no Chrome plugin, and the one running original Chrome root exposes neither a debugging port nor pipe. No plugin permissions changed and no private browser profile was read. Real-owner acceptance still needs owner-assisted login in a fresh isolated browser.

### Japanese directory implementation history — earlier local checkpoint, superseded by delivery above

- Added the four fixed Irodori HTML directories and exactly72 known lesson-page identities. Only playback-page URLs/course positions are retained; no PDF, transcript, audio or publisher exercise is copied. Navigation-only, incomplete, duplicate, rebound and malformed links fail closed. The four-book snapshot commits atomically; a failure retains the previous reserve. This maintains finite reviewed courses, not an unlimited new-publication claim.
- The Japanese-only importer preserves authored Chinese prompts, original editorial/voice-screening `checkedAt`, learner edits, approvals and existing IDs. Only separate `externalStudy.directoryCheckedAt` advances. Shared-owner checks remain around writes. Japanese course and book maintenance run independently in the background; partial failure is visible without losing the successful sibling or adding a retry loop. The6th scheduler source remains local until migration24/runtime verification.
-101 relevant unit cases pass (1live opt-in skipped), typecheck/scoped lint/build and294-file source/build scan pass. Independent review found two variants of one container-boundary defect (footer links and quoted div-looking text); both were fixed with complete-tag/attribute tokenization, targeted fixtures and static re-review. Final parser tests14pass/1live opt-in skipped; a subsequent opt-in real four-directory check on exact717948a also passed with72 links. Final runtime1,913,860bytes/SHA256 `c56354cd2752e3fa84ed64117d4e0fd00700b6c1d220e8ac74b79edecb7977df`. Existing dependency annotation/chunk-size build warnings remain.
- The first SQL check could not run because Docker was stopped, not because a SQL assertion failed. Docker Desktop was started hidden, but its engine remained unavailable and emitted an error-dialog event. Stopped only the exact stalled read-only `docker ps` child; did not reset Docker, touch other containers or retry daemon startup. Use the existing isolated Linux CI migration gate for this substantive candidate. Migration `20260920191716_japanese_course_directory.sql` is not applied remotely. Japanese remains DEV-only; this is not a public release or completed objective.
- Candidate717948a is on the existing feature branch. Single manual CI35532554967 has passed unit/static/build/security, cold local24-migration SQL gates, actual isolated Auth/Storage/content/Deno and multi-profile Chromium/WebKit sync; the remaining page journeys are still running. The independently reviewed operator wrapper requires that exact CI success, exact fullSHA/clean relevant source and rebuilt pinned-runtime hash before its single migration/history transaction. No second unchanged full matrix is queued.

### Remaining full-delivery gates

- English: desktop and physical iPhone both have verified9520397; Android physical acceptance is waived. Enable the separately held BBC/graded-reading imports in the next reviewed grouped release, then prove an actual recurring catalog reaches a new eligible authenticated Today assignment. Stored catalog counts or synthetic browser fixtures alone do not close this gate.
- Japanese: controlled production gate and real language-aware dialogue/STT/structured-feedback gateways now pass within their documented limits. Remaining are the reviewed coaching-disclosure fix, grouped release and legitimate owner Japanese-page recording/sync journey. Do not substitute direct gateway diagnostics for UI acceptance or claim the full learning system finally accepted.
- Owner/device evidence: desktop login, normal cloud restore, audio integrity, upgrade preservation and offline/reconnect pass; physical-iPhone installed version now passes from the screenshot. Actual iPhone/desktop save→switch→resume remains open. Initial placement requires the learner's own answers, never agent-filled evidence. No original Chrome cookies, credentials or identity were extracted. Do not repeat passed login, paid calls or unchanged full matrices; do not reinstate the waived Android requirement.

### Current public release — cd9a422, delivered; recurring-job diagnosis

- Exact Pages CI35516155082 and deployment6553943269 succeeded for `cd9a422ffa036193c1f22a6ee7bd994a53cd0f3f` at2026-09-20T14:38:51Z. Public HTML serves `index-DuZGU8Fd.js`. English graded-reading code and the Japanese interrupted-book fix are included. Older public/local labels below describe historical checkpoints, not the current release.
- Actual current-public desktop Chromium and mobile WebKit checks pass: existing listening draft/reload, external-listening Chinese guidance,15-minute planning, configured sign-in UI, and new original-reading response/save/reload. The reading material was inserted only into fresh isolated browser profiles because automatic graded-reading import remains OFF. No owner login, paid call or physical phone was used. Evidence: `.work/english-reading-public-20260921-{desktop,webkit}.png` and `-result.json`; the mobile screenshot was inspected. The earlier old-to-new PWA observer did not produce a passing result, so this is current-release persistence proof, not an update-migration claim.
- Five-source workflow35516178707 failed on English reading with503/EXTERNAL_CATALOG. Later35522638366 failed on VOA Level1 while English reading succeeded; hosted readback confirms59 readers, last success2026-09-20T16:25:11Z,0failures. The two job headers use the same service-role RPC, so there is no evidence of a separate missing directory-job database grant. The moving failure indicates an intermittent request problem; its exact underlying cause remains unconfirmed, not assumed to be a schema defect.
- Backend/scheduler patch37e5a67 serializes the five metadata requests without retries, retains independent outcomes after failures, extends the workflow ceiling to five minutes for five30-second deadlines, and exposes only fixed RPC error categories without upstream SQL/messages/data.89 targeted cases pass (2live opt-ins skipped), typecheck/scoped lint/runtime build pass. Independent read-only review found no P1/P2, including an accelerated five-source stalled-body check. Actual content runtime1,834,683bytes/SHA256 `5400b447397fa4ed7adbd4f1315533df27d1ea812abf667bfee4c6e4ffab5f53` is deployed with exact downloaded-byte comparison and anonymous401/SIGN_IN. No migration, secret, other function or owner record changed. This scoped operational patch intentionally did not republish Pages or rerun its unchanged full matrix; public frontend remains exactcd9a422.
- Actual manual workflow35531614520 on37e5a67 succeeded at2026-09-20T19:14:10Z: VOA52/30, BBC49 and Tadoku141 refreshed; English reading correctly observed its existing backoff. One success confirms current operation, not the root cause or permanent elimination of intermittent RPC failures. No further manual retry or paid call was made; normal recurring observation remains necessary.
- BBC/graded-reading automatic imports remain OFF pending the owner's compatible-device update acknowledgment; Japanese remains DEV-only. Full-goal work still includes automatic eligible content reaching Today, Japanese course maintenance/release and legitimate owner AI/sync/audio/Android/iPhone/desktop continuity. Do not describe this checkpoint as final completion.

### English graded reading — September20 implementation and release preparation history

- Added the five-level British Council directory adapter, strict English-only link metadata and atomic all-level refresh; no article, image, worksheet, exercise or audio copying. Today automatically selects comfortable original reading and bounded own-note recall, alternating with existing intensive reading. Chinese main-meaning guidance, optional personal English expression, reported effort/time and recoverable too-hard/unavailable/later exits are implemented. New reading cannot enter listening/fluency/chunk fallback; material/task deep links are checked before saving. No paid provider is required for this path.
- New whole-attempt sync preserves competing answers as separate copies. Frozen recall sources use content-verified occurrence keys, surviving late same-ID report conflicts, receipt clock correction, JSON restoration to a fresh database and fresh-journal replay. Independent review found and closed the route/material mismatch and both source-recovery variants; final scoped static re-review has no blocking P1/P2. No skill or pronunciation scores are awarded for link opens, reading logs or self-reported note recall.
- Targeted suites pass:101 cases across new catalog/learning, existing Japanese original books, cloud importer and scheduler;109 new English-reading plus existing sync cases;76 prior reading/learning/scheduler cases. These selections overlap and are not an additive test count. The prior reading harness needed one explicit stub for the new unrelated component; its18 import failures were fixed, not product-flow failures. A live bounded five-directory fetch succeeded. Actual isolated DEV mobile Chromium and WebKit both passed original-link/read/write/reload/save/continue/recall-before-reveal/reload without overflow or uncaught errors. Screenshots `.work/english-reading-20260920-{mobile,webkit}.png`; synthetic profiles, not owner or physical devices. A browser-observed reveal computed-state bug was fixed. Typecheck, scoped lint (two existing test-component warnings), build and local SQL rollback pass.
- Backend `db push` preflight stopped on `LegacyDbConnectError` / direct PostgreSQL connection terminated unexpectedly;22 migrations and old privileges remained unchanged. The installed MCP account also lacks this project's permission, so it was not used for writes. The already-authorized CLI Management API remained reachable. An alternative exact-file transaction with an exclusive migration lock, full22-version preflight, count/latest guard, security assertions and atomic migration-history insert was locally rollback-tested and independently reviewed. It applied migration23 (`20260920030000`) once; hosted history text exactly matches the reviewed local file. RLS, security-invoker RPC and denial to anon/authenticated remain confirmed. No prior migration/history was rewritten.
- Dedicated hosted content runtime is deployed and downloaded bytes match the reviewed1,834,273-byte bundle, SHA256 `5771604124103acdd4d6531cedda632ed04478bb27e87ae6ce95db1602070851`; anonymous401/SIGN_IN confirmed. One existing-server-capability metadata request returned200/refreshed/59. Independent SQL readback confirms59 entries,10,627bytes of catalog JSON, success and0failures. No owner profile, recording or secret was changed; no paid API call. Five-source scheduler code is ready for the next main release, not yet claimed as a scheduled execution.
- Supabase skill security review preserves the existing RLS and service-only catalog RPC. Hosted advisors report9 existing authenticated security-definer warnings for guarded sync/recording/content helpers plus1 password-leak-protection configuration warning; the new directory RPC remains security-invoker and denied to anon/authenticated. These existing warnings are not silently dismissed or fixed by changing working owner permissions. No new secret, account or payment is introduced.
- Public Pages is still879515c until the next exact-SHA deployment is confirmed. New `JOVE_GRADED_READING_ENABLED` is separate and off, as is BBC import pending the owner's device-update acknowledgement. Japanese remains DEV-only. Still required for the full objective: deployed recurring source activation reaching Today, Japanese continuing-course maintenance, legitimate owner AI/sync/audio acceptance and actual Android/iPhone/desktop continuity. The goal remains active, not finally delivered.

### Current public release —879515c, September20 delivered

- CI35488693307 on879515ca46d43a02e8a88d09034acf9b7b1c8d81 passed verification and Pages deployment. Actual deployment6549060216 at04:34:14UTC binds that exact SHA. The previous type-declaration/bootstrap-count failures are fixed, not active blockers. The one corrected substantive release passed all unit, cold22-migration, real local Auth/Storage/content/Deno, multi-profile sync, Chromium/WebKit/browser, dependency and release-readiness gates.
- Actual public isolated desktop/mobile profiles updated from `index-BrkzxZ9C.js` to `index-DWJdX7eE.js` using Update now. English draft and reload preservation, external lessons, Chinese guidance, short-day selection and public cloud configuration passed; Japanese stayed hidden and neither profile had uncaught errors. Evidence: `.work/japanese-catalog-compat-20260920-{desktop,mobile}.png` and `-result.json`. These are synthetic local-learning profiles, not real-owner login or physical-phone acceptance.
- The four-source metadata workflow35488550238 already passed on main; hosted Tadoku141-entry reserve is live. BBC automatic browser import remains off (enable variable absent), pending the owner's save/update acknowledgement across devices. Japanese remains DEV-only; real-owner AI/sync/physical-device acceptance and sustained English graded reading remain open. Later local interruption fix82e0850 is not included in this public release. Overall goal remains active and incomplete.

### Interrupted original-book reading — LOCAL ONLY, queued for next grouped release

- If no suitable replacement exists, retain the source/comfort report and original bookmark, end this attempt explicitly as `unavailable`, keep its assignment optional/notdone, and record `TASK_STOPPED` rather than a fabricated successful completion. Positive self-reported time remains in shared workload accounting; zero minutes does not count. A later assignment uses a fresh task/session and restores the last unfinished bookmark. Two distinct unavailable Tadoku books in24hours temporarily stop publisher rotation; a subsequent positive reading report allows recovery. There is no network/paid retry or forced jump to a harder book.
- Independent review identified and closed the discarded-report/continued-link-loop, released-used-time and stale-device-unsetting-state cases. Terminal interruption now has an attested whole-session snapshot bound to task, original/current book, stop time and minutes; concurrent notes remain conflict copies. Workload receipts are deterministic, completion-before-interruption, and deduplicated by task. They are not comprehension or proficiency evidence.
-29 targeted extensive-reading/shared-day/workspace cases, typecheck and scoped lint pass. Isolated actual DEV mobile Chromium and WebKit both save a bookmark, report time, take the no-alternative exit and retain the explicit interrupted/not-completed screen after reload without overflow/uncaught errors. Screenshots `.work/japanese-books-20260920-interrupted-{mobile,webkit}.png`. Independent final static re-review found no blocking P1/P2 in this increment. No backend, public release, provider or owner-data mutation was made for this local follow-up.
- Candidate879515c has completed the separate Pages release workflow35488693307 above. Do not restart that release or describe this later local interruption behavior as public. English graded reading subsequently became the implemented candidate documented above; it was not in879515c.

### Japanese original-book reading — September20 implemented, backend delivered; frontend candidate

- First main candidate58eef25 stopped before deployment in CI35488407800: the dependency-free scheduler's `.d.mts` declaration still named only three sources, exposed by the newly added Japanese boundary test. The declaration now imports the shared catalog-source type. The older public6724 release remained intact; no paid retry or backend mutation was needed for this correction. Corrected release is pending below.
- Second candidate60726ff passed types and1952 unit cases, but CI35488537836 found one remaining old bootstrap-count expectation:128 instead of130 after adding the two fallback book links. Corrected that explicit count without changing language isolation, no-onboarding/no-overwrite assertions or runtime behavior; all other unit cases had passed. Metadata workflow35488550238 on60726ff independently succeeded with all four sources correctly not due. Scheduled maintenance now includes Japanese on main; no owner or paid-provider call was used.

- Replaced the two-link optional recommendation with automatically planned original-book reading: separate short-text/original-book rotation, publisher-level easy entry, bookmark continuation, comfort-based next-book trials, too-hard/uninteresting/unavailable replacement, optional personal notes and explicit self-reported minutes. Chinese instructions do not confuse kanji recognition with reading or speaking ability. No book-content quiz, translation, AI analysis, hosted media or invented score; completion of a saved activity is distinct from verified comprehension.
- The pinned NPO 多言語多読 directory yields141 books across Start/0–5. Hosted22nd migration `20260920020000` and new content bundle are confirmed on the dedicated Jove backend. Downloaded deployed bundle equals1,828,765bytes/SHA256 `e7d46edefebd000a394625f6cf7f0973521f0ca1bb1117254c71674d883f9d56`; anonymous401/SIGN_IN. One existing-server-capability metadata request returned200/refreshed/141. SQL independently confirms141 entries,16,805bytes of catalog JSON, success and0failures. No new credential, owner-data write, recording deletion or paid call. Local migration/SQL contracts ran in a rollback transaction, retaining the previous local catalogs; hosted migration is applied.
- Japanese import requires the same admitted owner in both immutable language databases and preserves existing learner edits. The catalog source/language/rights/URL/size contracts fail closed; failed refresh retains the last good reserve. Draft book references, bookmarks and conflicting whole sessions use the existing sync/backup machinery. Directory polling is nonblocking with a six-hour retry cooldown and account-switch cancellation. The four-source scheduler is now on main and its actual manual execution35488550238 passed; Japanese frontend activation remains separate.
- Independent review found and closed three P2s with regressions: equal-revision cross-device rebinds now reject stale writes using a per-write UUID plus expected book; long schedules rotate reading independently of later dialogue; old drafts finished today use shared workload accounting with self-reported time explicitly labelled. No P1/P2 remains in that targeted static re-review. Related source, importer, planner, reading/sync, SQL and scheduler tests pass. Initial seed-count expectations were updated for the two fallback books; the first generated SQL text had a replacement-string expansion error, fixed before any deployment and validated in local rollback. These are not hidden runtime failures.
- Actual isolated DEV mobile Chromium and WebKit both exercised automatic book task → original link → bookmark save/reload → explicit minute report → finish/reload, with no overflow or uncaught errors. Screenshots `.work/japanese-books-20260920-{mobile,webkit}.png`. These are synthetic local-learning profiles, not physical devices or owner Auth; no publisher media or paid AI was requested. Typecheck and scoped lint pass. Production Japanese remains hidden, BBC client import remains off pending the owner's all-device update acknowledgement; real-owner AI/sync and full multilingual delivery are still open.

The production-release evidence below supersedes older candidate-only/public-release-pending notes in the chronological sections that follow. Japanese remains a separate unfinished development scope.

### English compatibility release — September20 delivered

- Main6724f0eaf1dd307bfaf32e4bb409ec7b69ea945c passed CI35485376886 verification **and deployment**; GitHub Pages deployment6548517164 at03:17UTC binds that exact SHA. No duplicate feature-branch matrix was dispatched. All21-migration/Auth/Storage/Deno/Chromium/WebKit/app-browser gates passed in this one release workflow.
- Actual public desktop/mobile isolated profiles saved drafts on `index-5bzqNOQG.js`, accepted Update now and reached `index-BrkzxZ9C.js`. Drafts survived update and reload; external lessons, Chinese guidance,15-minute day and cloud configuration remained available, Japanese stayed hidden, and neither profile had overflow or uncaught errors. Ignored evidence: `.work/continuing-compat-release-20260920-{desktop,mobile}.png` and `-result.json`. These are synthetic local-learning profiles, not real-owner Auth or physical phones.
- BBC automatic client import is still **off** (repository enable variable absent). Its hosted49-entry reserve and the independent52/30 VOA refresh are verified above. Asked the owner to save and update Android/iPhone/desktop before activation; no acknowledgement is assumed. The earlier isolated-owner-login request also remains unanswered, so real-owner AI/sync acceptance is still open. New Japanese72-task commit1d77cdf is local only and was not included in this public compatibility release. Overall multilingual completion is not claimed.

### Japanese bridge curriculum — September20 LOCAL ONLY, after English candidate

- Added18 original Chinese-guided tasks linked to the official Irodori Pre-Intermediate A2/B1 book, extending the finite reserve from54to72. Covers explanation/clarification, constraints/negotiation, reporting problems, social interaction, study habits, simulated help-seeking and workplace communication. Japanese readings in words, mora/long-vowel/gemination attention, register, conditionals and Chinese false-friend cautions are task-specific; original prompts/examples are not publisher transcript claims. Emergencies are communication simulations, not medical/safety instructions.
- Preserved the first54 IDs/URLs/metadata. The new book has its own screening date, exact01–18 page allowlist, ordered positions55–72 and provisional0.90–0.985 difficulty; low-difficulty learners do not skip into the new book based on completed-course counts. Book labels do not certify learner proficiency. Existing saved work/English initialization are unchanged.
-31 targeted Japanese/workspace tests, typecheck and scoped lint pass. Independent review found no P1/P2; corrected the nonblocking「きかい／きっかけ」note to distinguish related meanings from different pronunciation/usage, rather than incorrectly saying their kanji are similar. Isolated actual DEV mobile page confirms72seed lessons, the new book label, exact lesson14 link, Chinese「約束」contrast and no overflow/uncaught errors. Initial fixture raced the async Start save; waiting for the actual practice link fixed the fixture without weakening app checks. Screenshot: ignored `.work/japanese-bridge-20260920-mobile.png`.
- This local-only increment is **not** in English compatibility candidate6724f0e. Japanese remains hidden from production; ongoing reading/revalidation, real-owner provider/sync and formal activation acceptance remain separate.

### Directory workflow acceptance and compatibility release in flight

- Manual execution of the actual Actions metadata workflow35485428606 on6724f0e succeeded: VOA52and30 refreshed independently, BBC was correctly not due after its49-entry refresh. Hosted SQL confirms counts49/52/30, all three failures0, BBC success02:56UTC and bothVOA03:00UTC September20. This is a genuine job-capability/backend/publisher/SQL path, not a mock or paid diagnostic; it does not prove the next cron time will be punctual. Earlier503 outcomes are historical, not a presently reproduced blocker.
- Compatibility candidate6724f0eaf1dd307bfaf32e4bb409ec7b69ea945c was pushed **only to main**, avoiding a duplicate full matrix on the feature branch. CI35485376886 is running; all code/unit/build/cold21-migration/Auth/Storage/Deno/multi-profile gates have passed, application browser stage pending at this checkpoint. Exact-SHA readiness and all3anonymous401 checks preceded push; continuing-source enable variable remains absent/defaultfalse. Enabled-mode and disabled-mode built-preview mobile checks both pass. Public two-profile update observer has saved drafts on old `index-5bzqNOQG.js`; no new public deployment/upgrade success is claimed yet.

### Continuing English courses — September20 backend delivered, client compatibility candidate

- Added the exact official BBC6MinuteEnglish RSS adapter alongside both VOA courses. Bounded2MiB inert XML, fixed series/author/language/presenter/clean-episode screening, past-year episodes, stable IDs and exact publisher lesson-page links; at most52 metadata records, no enclosures/scripts/media/paid model calls. Chinese prompts guide short-segment listening, recall, text checking and new-situation recorded output; provisional intermediate difficulty does not promote beginners or redefine the General American speaking target. Series provenance is not individual acoustic certification.
- The21st hosted migration `20260920010000` and content function19 are deployed on the dedicated Jove backend. Downloaded runtime matches1,823,255bytes/SHA256 `af8571b1e94ea864c6ceb0ee2ac0a49112a25f1685fde1e15d26be2bc481848d`; anonymous request401/SIGN_IN. One existing-server-capability metadata-only request returned200/refreshed/49lessons; SQL independently confirms49 entries, success and0failures. No new secret, owner-data change, recording deletion or paid call. The first operator precondition halted before any mutation because CLI JSON wraps results in `rows`; read-only evidence identified that shape, then the corrected guarded step succeeded.
-133 targeted tests pass (two unrelated live-VOA checks skipped), including actual pinned BBC feed, server capability, scheduler, material import/preserved edits and beginner selection. The extended catalog migration/SQL contract passes in a local rollback transaction. Typecheck, scoped lint, frontend/server builds and276-file pattern scan pass. Actual configured built-preview Chromium desktop/mobile and WebKit-mobile show directory→BBC lesson→Chinese guidance→draft saved/reloaded without false completion. Initial browser failures were missing public cloud build configuration; corrected the build, not assertions. No owner login or physical-device acceptance is inferred.
- Independent review identified the old-client origin-schema sync incompatibility. Addressed with a **two-stage activation hold**: `JOVE_CONTINUING_COURSES_ENABLED` is confirmed absent, so production builds disable BBC automatic import; compatibility client widens schema first. Build and browser checks share the same explicit flag, and compatibility checks require no BBC rows. Enabled-mode mobile check passes after the flag change. All active devices must save and install the compatible release before setting the variable true and releasing again. Never clear history or claim old installed clients are compatible. Backend support/refresh can run safely before that activation.
- This source does not complete graded/extensive reading, recurring hosted refresh acceptance, Japanese progression/revalidation or real-owner AI/sync/physical-device acceptance. Earlier directory503 attempts still have no new scheduled diagnostic result; no repeated paid diagnostic was run. Frontend compatibility release and subsequent activation must be separately recorded below.

### Japanese unavailable-link recovery — September20 DEV candidate

- Added “原站打不开，自动换一课” to the saved guided practice. Flush the current answer first, then atomically retain its session/audio, mark only its assignment optional and create one replacement using the same planned minutes. Repeated reports return the same replacement. Access failure is a self-report, never listening completion, lost ability, or a completed task.
- Uses the existing editorial difficulty band (up to current+0.15, not a promise of identical/calibrated difficulty). After two distinct failures from a publisher within24hours, stop rotating through that publisher and return to other saved work; no network probing/retry loop or fabricated listening fallback. No alternative leaves the original recoverable. Empty Today no longer claims work must have been completed.
-11 workspace tests pass, including original/recording retention, idempotence, bounded publisher fallback, no out-of-band replacement and owner-change refusal. The first owner test failed because its fixture used `key` instead of the actual `id` primary key; corrected the fixture, not the owner fence. Typecheck/scoped lint and actual local Chromium desktop/mobile journeys pass: drafts survive reload/replacement, two reports stop rotation, no false score/completion, no overflow or uncaught error. Independent review reports no P1/P2 and clarified the provisional difficulty-band wording.
- This new DEV-only slice is not part of release candidate `d0a36db`; Japanese remains hidden. Broader ongoing content/revalidation, live-provider and actual owner-device acceptance remain open.

### English production release — September20 delivered subset

- Backend maintenance candidate `fe8e1b2` full CI35482364576 passed, including cold local migrations, real Auth/Storage, Deno, multi-profile Chromium/WebKit and the unchanged full browser gate; feature-branch deployment was skipped. English candidate `d0a36db236422af69008df9e60905173f8f57f7c` adds the scoped Chinese guidance and directory classification. It was fast-forwarded to main; [CI35483198597](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/35483198597) passed the complete unchanged gates and deployed successfully. GitHub Pages deployment6548161023 binds that exact SHA.
- Fresh exact code readback: ai19 is1,816,514bytes/SHA256 `4d8eb017b748d665bdf8e677761d86eaf2f4ff6d5075a088588c7b874160cabf`; content18/speech18 are1,812,789bytes/SHA256 `852ec856bf8309b6058393450c41f22004dd91fb59d2db1d322b80ce5e03d25a`. Downloaded entrypoints match reviewed source, all three anonymous handlers return401/SIGN_IN, and the exact-SHA24hour release hold follows the20-migration readback. Actual public desktop/mobile Chromium profiles then accepted Update now from `index-C-lFuLQr.js` to `index-5bzqNOQG.js`; original drafts survived update/reload, publisher practice and cloud configuration stayed available, Chinese guidance/15-minute entry worked, Japanese stayed hidden and no overflow/uncaught errors occurred. Results/screenshots are in ignored `.work/english-maintenance-release-20260920-*` artifacts. These are fresh synthetic local-learning profiles, not real-owner Auth/physical-phone acceptance.
- Existing Chrome exposes no attachable debugging connection. Requested one owner login in an isolated acceptance window for actual authenticated AI/sync testing; no response/owner acceptance is assumed. No paid call or owner-data deletion was made. The complete multilingual goal remains active and incomplete.

### Native-Chinese English guidance — September20 local candidate

- Today now explains the automatically selected next English task in Chinese, including its actual planned minutes and short actionable steps; reading and chunk study stay distinct. Added15/30-minute choices to the existing planner without changing default preferences, saved task IDs or evidence. This is workload control, not an optimal-dose claim or an additional daily obligation.
- External listening instructions stay inside the existing stages: first listen/meaning recall (Chinese responses allowed), later publisher-text checking/one useful expression and a new English sentence, then spoken retell/replay/repair. Generic procedure guidance reveals no content answers and does not count as translation help or ability evidence. The existing ordinary blind-listen stage gets the same clear Chinese procedure boundary.
-11 narrow domain cases, typecheck, scoped lint/build and273-file source/build scan pass. Four actual built-preview Chromium desktop/mobile journeys pass, covering short-day persistence/automatic start, stage-specific guidance and saved external drafts with no invented scores. Mobile screenshot inspected; no overflow. The initial run against DEV failed only at its existing production-webmanifest prerequisite; its trace is retained and the successful run used the proper built preview. No full unchanged browser matrix or paid request was repeated.
- Independent review found no P1/P2. Corrected one wording inconsistency: initial chunk study already displays explanations, so guidance now says understand/use first and independently retrieve in later reviews, not pretend the initial display is blind recall. Japanese remains DEV-only; this local English increment is not public/final delivery.

### Bounded directory failure classification — September20 candidate

- Scheduler diagnostics now retain only six fixed backend Gateway error codes in addition to HTTP status/source, allowing a database/RPC failure to be distinguished from the refresh-stage failure. An error body is read for at most two seconds/4096 bytes; unknown codes, arbitrary messages, HTML and details are discarded. No endpoint, job capability, retries or paid flow changed. All22 scheduler-client tests, scoped lint/typecheck and independent review pass.
- The latest observed scheduled/manual runs remain failures35468866003/35477254661; there was no new manual dispatch during this increment. Wait for a subsequent diagnostic-capable run before inferring a cause. Existing52/30-entry reserves remain separate from successful recurring Today delivery.

- New local written-practice slices:18 original Japanese micro-readings plus38 small kana/rhythm units, separately locked answers, optional Chinese help, saved application notes and bounded delayed revisits. Foundation, reading and dialogue share the existing daily allowance; short days rotate them. These finite exercises plus attributed publisher links are not a complete long-term advanced library.
- English public release is now verified at `d0a36db`: graded VOA catalogs, Chinese next-task guidance, short-day choices and recording-maintenance client are shipped. The owner's earlier Lesson2 visibility confirmation stays closed. Actual backend readback contains52 Level1 and30 Level2 entries. Successful scheduled eligible Today delivery, broader advanced/reading input and remaining live AI/owner-device evidence are still open.
- Japanese local implementation now includes immutable language data/sync/recording contracts, shared daily allowance,54 original guided publisher-link lessons across three graded books, saved diagnosis, guided two-recording practice, bounded modality-specific delayed reviews with conflict recovery, and language-specific backup/cache controls with missing-recording recovery. These are not a complete advanced Japanese/reading program or verified proficiency improvement.
- Shared-account integration is now implemented locally: a separate Japanese background sync context shares login, guards the English accepted/persisted owner before any Japanese bind/upload, and cancels late initialization on sign-out. Navigation and pending/error/audio status are language-specific. Same-owner SDK transport and local desktop/mobile UI checks use isolated fixtures, not the real owner's three devices.
- Japanese remains development-only. Shared local/cloud-byte admission and recording-maintenance frontend/schema are now delivered with the English subset; actual owner-device acceptance is still open. Japanese live-provider acceptance, broader sustainable curriculum/reading/revalidation and its own production release remain required. Paid content-audio diagnostics stay paused. No current Japanese checkpoint is final completion.

### Version-bound recording maintenance — September20 local candidate

- Added an authenticated on-sync executor for expired history/pronunciation cloud copies. The server stamps language-specific metadata frontier/shared retention policy, rechecks those receipts under owner locks at Storage DELETE, and binds deletion to the exact object version. Drafts, assessments, imports and unstamped/stale records are protected. Empty successful DELETE responses are not proof of removal. Cleanup precedes uploads so a full bucket does not permanently prevent its own maintenance. Local originals are never automatically deleted.
- Recovery retains the original hash/bytes and a durable25MiB admission hold before deleting an old cloud version. Interrupted upload/confirmation can resume; an ordinary reservation cannot shorten the hold. Pending recovery runs even when current retention would otherwise skip upload, and an explicit missing cloud object does not prevent unrelated originals from synchronizing. A healthy newer replacement is reused after a version-bound, cache-nonce physical download, without another upload/delete. Generic network/auth/provider errors cannot authorize deletion of that newer copy or create a success receipt.
- Known operational limit: local Storage returns `InternalError` when V2 metadata survives but its physical file is absent. The initial automatic-repair expectation failed in both languages. Do not reinterpret generic500 as physical absence: the final implementation preserves pending recovery/local originals, stops confirmation and requires provider recovery or explicit operator investigation. No speculative missing-version reset or unbounded retry loop was retained.
- Evidence:22 real-local Auth/Storage cases passed before the final freshness refinement; the four final affected real-local cases pass (healthy V2 reuse, en/ja missing-V2 pending, denied verification). All101 sync unit cases passed, and20 local migrations/eight transactional SQL suites pass. Earlier114 combined sync/language cases also passed. These are scoped evidence, not physical-phone or production-owner acceptance.
- Hosted protocol probe: an initial8-byte synthetic file reached replacement but HTTP-info version disagreed with database readback; that attempt was not accepted, and the precise temporary object was subsequently removed with SQL absence confirmed. A corrected single bounded probe used SQL versions, tested wrong-version denial, V1 deletion, V2 replacement, late actual-V1 denial and version/nonce-bound physical hash verification. It passed and removed its temporary file. No owner recording, learning record, account or paid-AI call was involved. This verifies hosted Storage version semantics, not authenticated maintenance deployment.
- Final scoped review found no new P1/P2. Scoped lint, typecheck/build and273-file source/build pattern scan pass (`index-D8AueVOz.js`,815.58kB/gzip262.30kB); dependency annotation/chunk-size warnings remain. The healthy-V2 test now reads actual SQL versions rather than HTTP info. Prior committed candidate `872cebb` CI35479140006 passed with deployment correctly skipped on the feature branch; the new candidate's CI remains separate.
- Hosted delivery: exact-project check and dry run admitted only `20260920004000_recording_maintenance.sql`, then applied it with vault/seeds/roles unchanged. SQL readback confirms20 migrations, both receipt triggers enabled, authenticated-only maintenance RPC with empty search path/currentVersion response, and version-revalidating DELETE policy. Registered recording files, recovery holds and temporary probes are all0. No owner recording was removed. The maintenance frontend is not publicly released yet; authenticated production-owner and actual device acceptance are still open.

### Shared cloud recording allowance — September20 local candidate

- Added a default200MiB recording-bucket allowance shared by both languages/devices, separate from the configurable local-device limit. Exact-size owner/member reservations last one hour; retries cannot change their byte identity. Accounting includes every registered Storage object, including uploads lacking an application manifest, plus live reservations without a materialized object. Existing originals remain confirmable even after the limit is lowered; no original is deleted to admit a new upload.
- Real Storage testing disproved the initial RLS-only assumption: upload preflight checks run in a rolled-back transaction, while completion writes with privileged access. A second, bucket-scoped forward migration now fences the final INSERT with the same transaction lock and actual bytes, including privileged writes. Existing object UPDATEs cannot change path/owner/version/size. Both `20260920001000` and `20260920001500` are required together; publishing the first alone is not accepted. Missing/invalid metadata consumes a conservative25MiB, so legacy/multipart preflight may require that much headroom. This is an application allowance for registered recording objects, not a provider-wide billing guarantee or a claim to enumerate physical files without Storage metadata.
- Narrow evidence:110 affected SDK/unit cases pass. The new transactional SQL assertions cover expiry, missing/invalid sizes, unconfirmed objects, late arrivals, immutable originals and nonmember/ledger denial. All seven local SQL suites pass against19 migrations. Real local HTTP covers small-reservation/large-body rejection, installed-client uploads, concurrent English/Japanese reservations, one20MiB winner under a32MiB allowance, direct unreserved upload races and confirmation after a lowered cap. Temporary objects are removed through the Storage API and the fixture cap is restored. Independent static review found no new P1/P2; real-Storage and release evidence remain separately tracked. No paid request or production mutation.
- Final scoped verification: all10 real-local Auth/Storage tests pass after the final-write trigger; scoped lint, typecheck/build and271-file source/build pattern scan pass. Built entry `index-CBKNFpbG.js`,811.07kB/gzip261.30kB; known dependency annotation/chunk-size warnings remain. No unchanged full browser matrix was run for this backend slice.
- Hosted delivery: after exact-project validation and a two-migration dry run, applied only `20260920001000` and `20260920001500` to `lnkxdwzdcrtlucaezhkd` with vault updates disabled. Readback confirms19 matching local/remote migrations,209715200-byte limit, both ledger/limit tables under RLS, no direct client ledger/limit writes, no anonymous RPC grants, empty security-definer search paths and the final-write trigger enabled. Owner-only SELECT/INSERT/DELETE policies remain and no UPDATE policy exists. Aggregate registered recording storage was0 objects/0 bytes; no user audio was deleted. Independent review closed the scoped quota P2 on commit `4a9b76f`. This is actual backend/schema delivery, not real-owner cross-device audio acceptance or a public frontend release.
- Follow-up local client recovery: a strict false reservation now raises a specific capacity condition. Audio sync marks that upload blocked and continues existing downloads/retention alignment; malformed responses, account changes and transport errors still abort instead of being swallowed. The new regression plus97 existing sync cases pass. Independent review found no new P1/P2 in this change; build/typecheck, scoped lint and271-file scan pass (`index-DK3wixG8.js`). This prevents only the RPC-false early-exit case: a1MiB reservation with10MiB left can still pass and then fail Storage's conservative25MiB preflight. Cleanup must run before uploads or cover final-Storage admission failures, not assume all capacity failures are RPC false.
- Prior-candidate full CI [35477660747](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/35477660747) on `86d5b7b` passed, including the previously failing WebKit path; deploy was skipped as expected on the feature branch. That successful run predates this quota diff and is not its exact-SHA full acceptance.
- Cleanup implementation remains open, with a bounded protocol review rather than an unsafe purge: an authenticated on-sync executor can use server-stamped retention frontier/policy and revalidate owner locks at the actual Storage DELETE. It must protect draft/assessment/import records, reject forged INSERT receipts, check the actual returned deleted paths (an empty successful response may be RLS denial), and recover manifest-present/physical-file404 failures without assuming SQL and object storage commit atomically. The official Storage HEAD/info implementation reads database metadata and cannot prove physical availability. No cleanup code or production deletion has been enabled at this checkpoint. Do not repeat paid content/scheduler diagnostics to work around this separate task.

### Shared local audio allowance — September20 local candidate

- Recorder preflight/save, Library import, generated-speech cache, authenticated content cache and recording-sync download now use one admission gate. It reads the persisted English setting and counts both language databases, while replacements deduct only the target database's own ID. An origin-scoped Web Lock serializes participating current-client tabs before each target transaction; no database transaction is kept alive while waiting for the lock or reading its peer. Opening English does not create a Japanese database, and custom/other-project namespaces are not swept into the allowance.
- Originals are never evicted to fit a cache. Each reusable cache can only clear its own workspace's replaceable entries; received speech/content remains transiently usable on supported cache failure paths. Without Web Locks, single-language transactional operation remains available, but multi-language new writes fail conservatively with a browser-upgrade message instead of pretending to be cross-tab atomic. This is an application-local allowance, not a browser storage guarantee or cross-device cloud quota.
- Independent review found an owner change during the new lock wait. Fixed by retaining the capture owner with the recoverable draft, checking it before retry and in the final write transaction, and rechecking the workspace context after lock acquisition. A rejected original remains downloadable/retryable under its original owner across component remount. The reviewer closed this P2; no OPEN P1/P2 remains in the scoped local admission diff.
- Seven admission tests and51 compiled recorder/component tests pass; the earlier93 sync and44 authenticated-content cases passed with the new gate, plus24 Settings/Japanese workspace/account cases. Actual two-tab Chromium desktop/mobile races retain the original and admit only one competing cross-language write. A separate actual desktop Japanese two-recording journey passes. Windows WebKit failed at the initial raw IndexedDB Blob fixture insert, before this gate (`UnknownError: Error preparing Blob/File data to be stored in object store`); both failed traces are retained, no equivalent retry was made, and this is NOT WebKit/physical-iPhone acceptance. Final build/static evidence is recorded with this checkpoint, not a production release.
- Separate hosted review found two OPEN P2: bucket upload has a per-file limit but no atomic total-byte admission across languages/devices/in-flight or unconfirmed objects; recording retention writes expiry metadata but no personal-recording cleanup executor actually frees expired cloud objects. Implement both with protected drafts/assessment/import handling and preservation of local originals before claiming cloud capacity/retention complete.
- Remote verification35476118522 of kana commit2cedfd2 passed the preceding unit/build/local-backend gates but stopped in native WebKit E2E: English's expected disabled Transcribe button had been hidden by the earlier Japanese workspace condition. Restored the existing English affordance while leaving unavailable Japanese transcription hidden; both remain unable to dispatch without a provider. Added a compiled-SFC regression instead of deleting/relaxing the browser assertion. The failed complete run is not accepted; remaining browser cases still need a later final-candidate run, not blind repetition of this SHA. Final changed-source build/typecheck, scoped lint and268-file source/build scan pass (`index-j4AoI1od.js`); the208-case affected run passed before the last narrow affordance assertion, and both that new assertion and owner-race regression pass separately. Independent static follow-up approved the fix.

### Scheduler failure visibility — September20 local candidate

- Read-only hosted catalog snapshot: Level1 has52 entries, last success September19 17:22:01UTC; Level2 has30 entries, last success19:58:33UTC. Both catalog failure counters are zero. The initiator of the latter success was not established in this check, so it is not timer proof. [Scheduled run35468866003](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/35468866003) actually started20:55UTC and failed; its old blanket error log cannot distinguish transport, auth, HTTP or response-contract failure. No paid request or manual warmup was dispatched to hide that failure.
- Added fixed, credential-free scheduler diagnostics: whitelisted source IDs, HTTP status and local error categories, plus an explicit successful/not-due sibling result. Arbitrary error messages/stacks/response bodies never enter logs. No retries, backend privilege changes or new endpoints. Thirteen focused script tests and scoped lint pass; independent review approved the diagnostic diff. Published only the three scheduler-script/declaration/test files as main `4ba1d951fde759f252ae9265bd041f9a0adb209c`; no Japanese frontend enabling, source visibility change or owner-data mutation. The separate worktree preserves the existing public application code.
- One controlled, non-paid diagnostic dispatch [35477254661](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/35477254661) at23:52UTC now reports Level1 HTTP503 and Level2 `SUCCESS_OR_NOT_DUE`. This was manual diagnosis, NOT successful scheduled supply. Hosted attempt/success/failure counters remain unchanged. Exact active content v18 readback still matches the previously reviewed1,812,789-byte runtime / SHA256 `852ec856bf8309b6058393450c41f22004dd91fb59d2db1d322b80ce5e03d25a`. A scoped SQL service-role claim probe succeeded and rolled back; independent REST read RPCs returned200/52 and200/30. These exclude a missing RPC/basic read permission failure but do not establish the edge failure's root cause. No credential/raw upstream response was logged, no paid request was made, and no further equivalent scheduler dispatch is authorized without new diagnostic evidence. Continue unblocked cloud-capacity work rather than loop on this503.
- The unchanged UTC cron `37 */6 * * *` targets00:37/06:37/12:37/18:37, subject to GitHub delays; it does not target23:37. Do not poll a nonexistent23:37 run or call a manual dispatch a scheduled delivery. Actual successful eligible refresh and its Today consumption remain open.

### Japanese kana and rhythm foundation — September20 local candidate

- Added38 original small units covering both basic46-character inventories, voiced/semi-voiced groups, introductory contracted sounds, long vowels, small-tsu, moraic-n and particles/pitch awareness for a Chinese-native learner. Chinese explains contrasts; romanization is a temporary locator, not Chinese homophone instruction. The foundation never gates real-life communication. Three-minute slots share the existing allowance; after introduction, limited15-minute days rotate with integrated reading/dialogue rather than permanently crowding them out. Timing/rotation is a provisional workload heuristic, not a universally optimal learning schedule.
- Reused the durable reading page/store/sync contracts. Publisher playback stays at [MARUGOTO Plus](https://a1.marugotoweb.jp/en/hiragana.php), with links to its script-specific listening drills and pronunciation guide. The [official credits](https://a1.marugotoweb.jp/en/about_this_site.php) identify Japan Foundation authorship and narration; its [policy](https://marugotoweb.jp/en/site_policy.php) permits linking. An original vowel clip played successfully in the desktop browser; this is not physical-phone or comprehensive auditory-quality acceptance. No audio, publisher quiz or transcript is copied/hosted.
- Each attempt explicitly records heard-self-report or unavailable/script-only; link navigation alone earns nothing. The self-report is locked with the first answers and preserved atomically in sync. Script-only successes do not extend a later heard recheck, and custom KANA_CHECK receipts assert neither listening nor acoustic assessment. Foundation never enters reading calibration or skill/FSRS scores. Word choices avoid requiring a Japanese IME and retain the assigned script rather than treating hiragana/katakana as equivalent spelling.
- Independent review found that shared learn-kind classification could drop the other Japanese written task after sync. Added stable separate kana/reading activities without renaming persisted tasks or changing English reading/chunks; four real projection regressions cover start/complete of either, reverse order and a stale-device plan. Re-review closes the P2 with no OPEN P1/P2 in this slice. Six kana tests and59 affected Japanese tests pass; ten targeted sync cases pass (83 outside selection). Updated old seed-count assertions to the actual catalog; the previous remote run35474807464 had only that stale18-vs72 failure, not a demonstrated runtime defect.
- Desktop/mobile Chromium kana journeys pass, including no-IME answers, reload, script-only result, saved note/completion and one-day recheck; both layouts inspected. The initial browser fixture reloaded before asynchronous completion; it now waits for the real saved heading before testing persistence, without changing product behavior or weakening assertions. Typecheck/build/scoped lint and267-file source/build pattern scan pass. No paid call, hosted-backend write or production frontend release. Local preview evidence is not final delivery.

### Japanese reading and contextual kana — September20 local candidate

- Added original short daily-life passages, two meaning checks and two contextual word-reading attempts per text. First answers persist before explanations; blank answers are allowed, help use is explicit, and a separate note applies the meaning in a changed situation. Kana matches accept normalized katakana and contextual alternatives; they never assert actual pronunciation, pitch, listening or spontaneous speech. No paid AI request is needed for this path. Both meaning and reading matches remain custom text evidence, not skill/FSRS credit.
- Three editorial bands are provisional workload choices, not JLPT/CEFR assessments. Fresh independent comprehension across distinct texts/days informs calibration; earlier help AND earlier locked feedback, including late synced branches, disqualify false first-exposure claims. Practice exploration is separate: after exhausting a band, comfortable independent revisits across two days/two texts can invite an unseen next-band trial. Supported-first learners retain higher-band review and a path to further trials; recent difficult/incorrect work backs down. Simple 1/3/7/21-day text rechecks remain separate from existing expression FSRS, and two due-text revisits cannot indefinitely displace unseen eligible input.
- Independent review exposed field-level sync mixing, supported-first progression ceilings, and forgotten pre-submission help. Fixed using the existing atomic reading merge with Japanese first-answer lock receipts, whole completed snapshots, visible preserved conflict copies, and revised exposure/exploration rules. Real Japanese SyncJournal reverse/incremental application retains the original locked result and conflicting text. Final independent review reports no OPEN P1/P2 in this slice.
- 58 affected unit/integration cases pass, including15 new reading/content/owner/recovery/budget/backup/sync cases. Twelve existing English reading-conflict checks also passed (77 other cases were outside that targeted selection, not acceptance skips). Desktop/mobile Chromium reading journeys both pass after the fixes, including reload, help, locked answers, saved notes, completion, source links and discoverable conflict copies; layouts inspected. Typecheck/production build, scoped lint and the265-file source/build pattern scan pass. These are local fixtures, not authenticated production sync, physical phones or live provider acceptance. Japanese stays DEV-only; no production frontend or cloud function deployment in this slice.
- Publisher rights checked against [Tadoku's official guide](https://tadoku.org/japanese/en/free-books-en/note-en/) and its [Start](https://tadoku.org/japanese/book/6447/) / [Level0](https://tadoku.org/japanese/book/7347/) pages: attributed external links only; no copied/adapted book text, translations, quizzes or hosted media. Extensive reading is optional enjoyable input without a compulsory quiz, and opening a link earns no learning evidence.

### Japanese three-turn interaction — September20 local candidate

- Implemented an automatically allocated five-minute conversation after two introductory practices, within the same English/Japanese daily allowance. Already-started older plans are not refilled. Today and resumable history route to the dedicated development-only dialogue page. The system supplies the lesson's real-life scenario and language level; the learner does not have to design a conversation course.
- Each confirmed learner turn is frozen and saved before requesting a contextual AI reply. Original recording is optional for quiet text practice and required for the final full spoken retry; text is never claimed as oral evidence. Japanese narration remains the publisher's human input, not an unaccepted synthetic voice. Three turns are followed by one priority hint, optional example and recorded repair, or clearly labelled non-AI comparison. Fixed offline replies are explicitly rehearsal cues, not fake model output or real interaction evidence.
- Separate durable result receipts recover received replies/feedback without another model request; owner/language checks and revision/visible-answer comparisons guard writes. No automatic redispatch; replies and summaries allow at most two explicitly requested attempts, with visible uncertainty about potential repeat charges and an offline path. Cancelling does not delete the answer. JSON restore retains turns and notes while missing audio uses strict default IDs. Recording playback accepts an explicit workspace array and never borrows an equal-ID English original.
- Nine new dialogue persistence/recovery/owner/budget cases pass; seven related workspace and eight existing review cases passed. Four selected SavedRecording cases pass (the other46 cases were outside that targeted selection, not acceptance skips). Changed-source lint/typecheck passed before final test additions. Initial mixed-current/foreign DB work in Dexie.waitFor caused a premature-commit failure; corrected using the documented foreign-only isolated wait followed by the local owner fence, retaining both checks.
- Extended desktop/mobile Chromium journeys both pass: recorded first answer→provider failure→reload without automatic dispatch→explicit recovery→cancel next reply→labelled offline continuation→three turns→hint-first feedback→recorded repair→reload. Saved feedback is reused, recordings stay Japanese-only, ability evidence remains zero, and no horizontal overflow was observed. Both layouts were inspected. These are isolated API fixtures and Chromium's fake microphone, not paid models, actual owner history or physical phones. Independent read-only review reports no open P1/P2 for the dialogue/domain/planner/recording slice. Final production build,260-file source/build pattern scan and scoped lint pass. No paid API call, cloud settings change or production release occurred. Next required feature work: graded reading and kana/sound instruction, not another unchanged dialogue matrix.

### Graded Japanese and evidence-bound reading help — September20 local candidate

- Added36 original Chinese-guided missions following the official Elementary1/2 topic sequences, preserving every Starter identity and existing customized material.54 tasks span the publisher's A1/A2 resources; textbook levels are explicitly not learner grades. Each task has its own Japanese phrase/kana, contrastive grammar, listening focus and transfer task. No publisher audio/transcript/PDF is copied, and the UI links to the correct book rather than always Starter.
- Curriculum ordering and difficulty exposure now share the same validated reflection contract, ignore future/incomplete/link-only history and deduplicate the same practice. Optional difficulty feedback is a self-report about load: one hard attempt consolidates the topic; repeated hard attempts choose an earlier approachable topic. It does not update skills/FSRS or discard started assignments. Curated static Japanese links retain their real screening dates and remain usable after90days, consistent with static English seeds; this is not an automatically refreshed feed or a fresh-availability claim.
- Reading help fades only for the specific expression after two saved, unprompted matching-kana recalls on separate days at least20hours apart. Chinese-character recognition, AI scores, missing first answers, duplicate receipts or other modalities cannot trigger it. A difficult/prompted review restores help. Other valid Japanese answers are not marked wrong by this support rule. The support threshold is an explicit design heuristic, not a scientifically proven optimal cutoff or an acoustic measure. The manual button now actually collapses an initially expanded hint.
-47 targeted unit/integration cases and changed-source lint/typecheck passed. The new desktop/mobile Chromium journey passes (two cases): Starter→Elementary1 automatic selection, correct publisher link, retained54 materials, manual help toggle and evidence-based collapse after reload. The ordinary desktop two-recording journey also passes after setting/clearing optional difficulty, including JSON-safe persistence. Fixtures are isolated, not owner data or physical-device acceptance.
- Independent review found that delayed scoring could incorrectly count same-day answers as spaced recalls. Ordering, expiry and spacing now use the locked first-answer timestamp, not rating time;20 final focused domain/content cases, scoped lint and typecheck pass. Re-review closes the finding with no other open P1/P2 in this slice. Production build passed before this final DEV-only support-timing fix; no unchanged whole-project matrix or paid API request was repeated. Japanese still has no production route, and a full reading/kana program, multi-turn AI practice and actual-owner/device proof remain unfinished.

### Language-aware AI backend — hosted September20

- Deployed only the reviewed `ai` function from15608ae to the dedicated Jove project: ACTIVE/version19. Downloaded runtime equals the reviewed local bundle at1,816,514bytes, SHA256 `4d8eb017b748d665bdf8e677761d86eaf2f4ff6d5075a088588c7b874160cabf`. Production-origin anonymous requests return401/SIGN_IN with the expected CORS origin.
- `content` and `speech-assess` remain ACTIVE/version18; auth configuration, secrets, budgets and other projects are unchanged. This confirms the deployed language contract and anonymous boundary, not an authenticated Japanese paid-model call. The frontend remains the previously deployed English release, and the later graded-curriculum candidate is not in this runtime yet.

### Native-Chinese Japanese AI coach — September20 local candidate

- Implemented explicit immutable language context through local provider, account transport, recovery receipts and strict server request validation. Japanese prompts use Chinese explanations and natural Japanese expression/register/particle guidance; matching uses Japanese word units, not English word boundaries. Old English requests retain absent-language hashes. Account billing remains one owner ledger; advanced local-key daily guards now combine the same owner's two language costs without claiming a hard cross-device cap.
- The guided comparison step now has optional saved AI coaching: independent original → candidate transcription/manual text → learner checks the text → saved request → one priority hint → optional rewrite/explanation → full spoken retry. No AI result updates ability, FSRS or acoustic scores. A received feedback is tied to its original answer, not a later edited draft; matching saved feedback is reused. Transcription is separate from the typed response and must reference this practice's saved original. No English voice fallback for Japanese; Japanese synthetic speech remains unconfigured/unaccepted rather than a speaking model.
- Evidence:179 related provider/account/gateway/coach cases passed, with4 explicit optional runtime skips;8 additional language-specific provider/account/gateway cases passed;7 speech-pricing cases and8 coach persistence/cost cases passed. Desktop/mobile Chromium coaching journeys verify terminal-provider-error input retention, reload, hint-before-example and saved-feedback reuse without another dispatch (two passes), plus the ordinary two-recording practice desktop journey. All API responses are isolated fixtures; these are not paid-provider or actual-owner acceptance. A first browser fixture used400, which correctly triggered the existing one-step schema fallback; changed that terminal-error fixture to401 rather than changing the previously verified fallback policy. Mobile layout was inspected.
- Independent review found stale cross-device feedback selection and loss of a received transcription on revision conflict. Feedback now validates the visible revision/text/confirmation before dispatch. Transcription checks revision before requesting, saves received candidates independently, and reuses them on recovery without overwriting newer answers. A subsequent recovery-cancellation finding was fixed with post-await abort/disposal guards. Targeted unit cases and the extended actual desktop UI conflict/cancel/reload journey pass; re-review closes all three findings. The new browser delay fixture initially patched only a per-instance prototype; targeting the inherited Dexie method made the intended cancellation case execute, without weakening its assertions.
- Server bundle, final changed-source production build, scoped lint and257-file source/build pattern scan passed. No paid API call, account-budget change, new cloud credential or frontend release has occurred. Japanese conversation/multi-turn adaptation, broader graded/reading input and physical-device/cloud acceptance still remain.

### Hosted language schema — September20

- Applied only the independently reviewed `20260919190000_language_sync.sql` to the dedicated Jove backend after a one-migration dry run. CLI completed successfully; all17 local/remote migrations now match. No other backend or existing English table/record was changed.
- Hosted metadata read-back confirms RLS on both Japanese tables, owner/member policies, no anonymous table reads or RPC execution, no direct authenticated writes to the Japanese operation stream, and both new RPCs security-definer with an empty search path. English stream/recording objects and corresponding access restrictions remain present.
- This is actual hosted schema delivery, not real-owner multi-device metadata/audio acceptance or a Japanese frontend release. No paid AI call or Pages deployment occurred.

### Language-specific backup and recovery — September20 local

- Added a development-only Settings language selector for explicit English/Japanese export, guarded merge and reusable-cache cleanup. Each action captures its target before awaiting; changing language clears a pending file, slow cancelled reads cannot attach to the new target, and renamed wrong-language backups are rejected. English legacy filenames/envelopes remain compatible. Opening Settings or attempting to back up an unopened Japanese space does not create it. Key/cache reset stays explicitly English-scoped; originals in either language are never part of cleanup.
- Show Japanese and combined local audio sizes beside browser-wide storage usage. This is visibility, not an atomic cross-database recording quota; the existing recorder limit is still checked per workspace, and aggregate handling remains open.
- JSON-only restore now handles missing Japanese first/retry recording IDs while retaining answers and comparison notes. Unfinished comparison resumes at recording if its first original is absent. Review first answers stay locked after reference reveal; missing oral originals force a conservative retry rather than a longer self-rated interval. No acoustic/mastery evidence is fabricated.
- Focused evidence: nine actual Settings-SFC/Dexie cases, thirteen language-isolation cases and eight Japanese-review cases each passed;37 affected English backup/restore cases passed. Typecheck/scoped lint passed. A new desktop/mobile Chromium journey exports through Settings, rejects the wrong language, restores in a separate fresh context, re-records and continues with the saved comparison note (two passes). These are isolated local fixtures, not real-account or physical-device acceptance.
- Independent incremental review found two recovery edges: optional `undefined` properties injected into ordinary drafts, and nonempty recording IDs whose files are absent. Fixed both without relaxing JSON/audio validation. Focused regression covers both absent-ID and still-referenced oral records; the ordinary new-practice desktop browser journey passes too. Re-review approved the fixes; both findings are closed. No whole-project matrix, paid model request or frontend deployment was run. Next: explicit language-bound AI context/feedback and remaining graded/input/adaptive-help work, not another unchanged backup audit.

## Owner-approved direction and earlier checkpoint — 2026-09-13

- Implementation resumed by explicit owner request: English continuous external courses → language isolation → Japanese-specific learning → staged acceptance/release. Binding objective and efficiency rules are in `FINAL_UPGRADE.md`. Japanese is authorized project scope, not a saved personal memory.
- Local checkpoint implemented and independently reviewed: display name changed in the desktop/mobile shell, document metadata and install manifest; repository/public path, PWA start/scope, database and English IDs remain unchanged. External lesson selection now uses valid submitted reflection history beyond seven days, prefers eligible unpractised lessons, and rotates the least recently practised reserve after exhaustion. Link clicks/offers alone cannot advance it; assigned tasks, difficulty gates, authentic-audio priority and skill/FSRS evidence are preserved. External Today instructions no longer claim measured clip windows. Four starter links remain four; recurring supply is not accepted by this fix.
- Verification: **225 targeted unit/integration passes** across external-study/domain/content-app/database/pronunciation-components; scoped ESLint, typecheck/build and source/build secret scan (225 text files) passed. Built preview: **6 desktop/mobile Chromium/WebKit browser passes, 2 explicit Windows WebKit capture skips**; real manifest name/start/scope, reload persistence, recording save and no external-media download assertions passed. Desktop and mobile screenshots inspected for the branding/layout change. Independent read-only review approved the diff, with no new findings. This is local acceptance only, not physical-device or new production acceptance. No paid AI, cloud/backend mutation or Pages deployment occurred.
- Source-discovery checkpoint: reviewed the ESL-Lab easy directory and seven candidate publisher pages. The Coffee Shop page explicitly discloses AI collaboration; this alone neither proves nor disproves synthetic audio. No new candidate was labelled approved human speech from directory/speaker metadata alone. Verify audio provenance before admission; this is a source-screening limit, not a reason to resume the rejected paid audio pipeline.
- Still required: recurring reviewed metadata delivery beyond the four starter links; graded course coverage; robust language-specific migration/sync/backup/evidence and total-time planning; full Japanese curriculum/practice; remaining live AI and owner/physical-device production checks. No mandatory acoustic scoring or bulk media hosting.
- Current Codex goal-card readback confirms the multilingual objective and **active** status. The earlier paused/historical-objective limitation is superseded; the full goal remains unfinished.

### English catalog deployment — September14 local / September13 UTC

- **LIVE application commit `acfc40139177a11810ba9d7786cfb1cea757abeb`:** exact feature-branch CI [34746560068](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/34746560068) and production verify/deploy [34772324857](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/34772324857) both completed successfully. Feature CI included all five real transactional SQL suites, real Auth/Storage and multi-profile checks, **168 browser passes /3 skips** and zero dependency vulnerabilities. Actual public entry is `index-DRWwa0yL.js`; document/manifest show Jove Language OS while start/scope remain `/jove-english-os/`.
- Actual production-origin external-study checks completed after publication: **6 passes /2 explicit Windows WebKit capture skips** across desktop/mobile Chromium/WebKit. Guided draft reload, saved recording, honest reflection evidence, no publisher-media download, source link and branding assertions passed. Desktop/mobile captures were inspected. These fresh isolated browser fixtures are not owner-authenticated catalog delivery or physical iPhone/Android acceptance. The owner then reported a signed-in Library search for `Everyday English · Lesson 2` returning zero materials; this delivery check failed and takes priority over scheduling work.
- The dedicated backend now has **15 matching migrations**. All three hosted function runtimes were downloaded and match **1,807,631 bytes / SHA256 `415b660c64ef18e432fdc721d14cc846307778e5d55e2be3311e84b8dda1053d`**, including their entry files. Anonymous production-origin requests return401/SIGN_IN with correct CORS. One parallel download initially failed; only that missing readback was retried successfully, with no redeployment.
- **Real initial directory delivery succeeded:** one explicit `catalog-refresh` request returned200/52 lessons. Hosted readback at17:54:37UTC confirms the complete snapshot, zero refresh failures and the normal six-hour backoff. The task credential stayed inside the authorized process and fixed-endpoint HTTPS request, with redirects forbidden. This was a manual warmup, **not a scheduled run or owner-browser delivery proof**. Paid budgets remain paused; service usage remains11 rows.
- **Scheduling remains inactive, with a concrete cause:** the managed `pg_net` request queue and schema retain PUBLIC privileges granted by `supabase_admin`; the available `postgres` role cannot remove those grants. Effective ACL readback confirmed both schema usage and queue read/write access after attempted revokes. Both installation transactions were rolled back, and no credential was enqueued. Do not repeat the same revoke or disable managed triggers.
- A no-queue `http` alternative was examined only in rollback trials. The hosted extension is1.6; its timeout GUC permissions and older redirect behavior do not meet the proposed guard. The uncommitted trial migration/test additions were removed; no HTTP request, lasting extension or scheduler was created by those trials. The current application/backend runtime was not changed again.
- Final dedicated readback confirms **zero pg_cron/pg_net/http extensions**, the trial wrapper absent,52 catalog lessons,11 service-usage rows and paused budgets. All three functions are ACTIVE/version15. The rolled-back scheduler trials did not leave a live job or privileged HTTP wrapper.
- Next scheduling design must avoid long-lived credentials in the readable queue. A catalog-only, short-lived signature with atomic replay protection is a candidate, not an implemented/approved result. Its residual race/delivery-denial tradeoff must be explicitly evaluated; it cannot authorize any paid/audio/owner-data route. Meanwhile do not hold the independently verified display/client release for this scheduling issue. Owner-authenticated receipt of52 lessons, actual recurring delivery, broader grading, language isolation and Japanese learning remain open.

### Library delivery correction — September14

- Confirmed the owner's report: the hosted 52-entry snapshot passes the actual catalog and material schemas, while the account's synchronized profile has `onboarded=false` and no catalog-only materials have been synchronized. The app incorrectly required onboarding before even requesting the shared directory. The sidebar's Synced status did not mean content had loaded.
- Corrected the frontend gate: an authenticated, owner-bound account now loads page-only courses before initial learning setup. Personalized legacy selection still waits for onboarding. Setup changes invalidate the refresh identity correctly; no profile completion, evidence, recordings or account permissions are fabricated or overwritten.
- Library now distinguishes directory loading, success, unavailable/error, offline and account-needed states. Retry is disabled with an explanation while shared background work is pending. Independent review found this retry-state issue and approved its correction; the reviewer independently passed five focused store cases.
- Verification: the original pre-setup regression failed before the fix (zero catalog requests). The final store suite passes20 tests; the unchanged related client suite passes41. Scoped lint, Vue typecheck, configured production build and230-file security scan pass. The new real SDK/store/IndexedDB/UI journey passed desktop/mobile Chromium and desktop/mobile WebKit, covering visible503 recovery,52 saved entries, search, reload and unchanged onboarding/evidence. Cloud/auth responses in that journey are fixtures, not an owner login or physical device acceptance.
- Initial WebKit fixture attempts exposed intercepted requests missing under an active service worker. This one mocked-network test now blocks service workers, as Playwright documents; existing real offline/update tests remain unchanged. The regular WebKit context forwards that option. A subsequent mobile-only failure was the screenshot engine's32767px limit for the entire52-course list; viewport capture and the repeated post-reload search now pass. No production auth/PWA workaround or broad timeout increase was added.
- **LIVE and owner-confirmed:** application commit `5d68e70c129ec7cfafa5209ab7a494c959f59b6c`, [verify/deploy34828524069](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/34828524069), completed successfully on September14 at09:50:49UTC. September20 local readback confirms HTTP200 and `index-BtIcsUoP.js`. Production-origin external-study checks pass **10 cases /2 explicit Windows WebKit capture skips**; their cloud/auth interception remains fixture evidence. Separately, the owner explicitly confirmed that `Everyday English · Lesson 2` is now visible in their actual account. The missing-course delivery defect is closed; broader content, language isolation and Japanese learning are not complete.

### Directory scheduling alternative — backend live, timer enabled; recurring delivery still pending

- Replaced the stalled queue-token approach with a GitHub Actions/main/opt-in-gated metadata job and a fresh, directory-only capability. New authority rejects browser/user/legacy-job combinations and any action except the exact bounded catalog refresh. No elevated owner lookup, model, audio, history or usage route is reachable from it. Existing six-hour leases/backoff remain; no database or queue privileges were changed.
- Added authenticated on-use refresh for missing/older-than-six-hour directories, because GitHub schedules can be delayed or disabled after public-repository inactivity. Failed publisher replacement retains the previous readable catalog; cancellation remains fenced. This does not fabricate new publisher releases or expand the52-lesson beginner coverage.
- Local verification: **65 targeted tests passed /1 explicit live-network opt-in skip**, scoped lint/typecheck passed, server bundle built at **1,810,095 bytes**. Independent review approved and independently passed24 focused tests. Script response bounds, exact endpoint/action, sanitized failure output, no automatic retries, credential separation and prior-snapshot recovery are covered.
- September20 local verification of candidate `8ce7d9a`: complete unit suite **1722 passes /59 explicit skips**. All three dedicated hosted functions are ACTIVE/version16; downloaded runtime bytes and entries match the reviewed local bundle exactly: **1,810,095 bytes / SHA256 `dbcf8d4cae9a90db926c397cccf6524969a1c8b9dac04cc9352dd2a6b2601e58`**. No new schema or other project was changed.
- **Actual on-use refresh succeeded**, not merely a mocked request: hosted snapshot success at September19 **17:22:01UTC**,52 lessons, zero failures, next attempt23:22:01UTC. All49 catalog-only lesson identities are now also present in the owner's synchronized materials; the three existing lesson identities are preserved. Paid budgets remain paused and usage rows remain11. This is on-use delivery, **not a scheduled-run claim**.
- The directory-only scheduler credential is distinct from the older paused content job. Its operator helper received independent review; explicit GitHub host pinning and honest unconfirmed-write reporting were corrected before execution. Installation into the dedicated Edge secret store and this repository's Actions secret store both completed successfully, without plaintext files, command arguments or logs. Main now contains the reviewed workflow at `97759a8cb825c379ce055bcf3b4a5937c90704b7`, and `JOVE_CATALOG_SCHEDULE_ENABLED=true` is read back.
- **Actual GitHub-to-Edge capability check passed:** operator dispatch [35458662319](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/35458662319) completed at September19 **17:37:36UTC**. The refresh step ran (not skipped) and honestly returned `not-due-or-running`, consistent with the17:22 snapshot and six-hour backoff. This proves credential transport and job reachability, **not a timer-triggered refresh or newly delivered lesson**. The next scheduled execution and eligible-course delivery remain unproven. No backoff, evidence or paid budget was changed to manufacture that proof.
- Application verify/deploy [35458626864](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/35458626864) ended **failure** for main commit `97759a8`: **170 browser passes /3 skips /2 flaky WebKit external-draft reload cases**, rejected by the unchanged fail-on-flaky gate. Deployment was skipped; the existing Library fix remains live. All prior lint/typecheck/unit/real SQL/Auth/Storage/Deno/multi-profile steps passed. No unchanged full rerun was started.

### Graded English reserve — September20 backend and client deployed

- Implemented separate30-lesson VOA intermediate directory support alongside the52-lesson beginner sequence. Metadata-only exact-source parsers, source-scoped persistence/leases/backoff, optional source request fields and separate material IDs preserve old Level1 callers and history. Browser requests have independent deadlines and imports; partial failure is visible without rolling back another course. Scheduler attempts both fixed sources once and reports any failure honestly. Existing ability gates and already-bound plans remain; participation does not change skill evidence or certify progression.
- Official source and first/second/final lesson pages were inspected; the course mixes human conversations with an animated teaching character. Prompts explicitly use the main human conversation and exclude Professor Bot as a speaking model. No all-segment human certification or currently publishing feed is claimed. See `CONTENT_PIPELINE.md` for provenance and limitations.
- **126 targeted unit/integration passes /2 opt-in network skips**, scoped lint/typecheck/configured build and235-file source/build secret scan pass. Separately both real pinned publisher directories were fetched and parsed successfully (52/30); persistence in those two network tests is simulated. The isolated local backend now has16 migrations and all five real rollback SQL suites pass, including source/lease mismatch and default compatibility assertions.
- The WebKit flake was reproduced through delayed real Dexie/fake-IndexedDB commits: the old disabled-button assertion passed while stage2 was still saving. The browser test now waits for `aria-busy=false` and the exact persisted stage/answer/expression/example before reload. Existing recovery assertions and strict-flaky gate are retained. The independent reviewer added a delayed-save compiled-page regression; main inspected it and reran it successfully. No product persistence rewrite, added sleep or widened timeout was used.
- Fresh built-preview external-study checks pass **10 cases /2 explicit Windows WebKit capture skips** across desktop/mobile Chromium/WebKit, including82 fixture-backed saved course entries, partial availability/retry, both-course search, reload and unchanged evidence/profile. Desktop/mobile screenshots inspected. Windows now reserves port4173; the preview used available loopback4242 without changing system port exclusions or project deployment configuration. These are not physical-device, actual owner Level2 delivery or paid-provider proofs.
- Final scoped independent review approved the source/lease/client/learning changes and independently passed13 focused cases. Complete candidate unit suite passes **1738 cases /60 explicit opt-in or environment skips**. Formal backend/client release, real timer-triggered eligible Today delivery, broken-link alternatives, broader sources, language isolation and Japanese-specific learning remain unfinished.
- Dedicated production migration readback now matches all16 versions. All three hosted functions are ACTIVE/version18; downloaded runtime and entry bytes exactly match **1,812,789 bytes / SHA256 `852ec856bf8309b6058393450c41f22004dd91fb59d2db1d322b80ce5e03d25a`**. Actual SQL readback confirms catalog RLS, anon/authenticated RPC denial, security-invoker execution, retained52-lesson beginner snapshot, zero intermediate rows and unchanged11 usage rows. No Level2 warmup was dispatched: scheduled initial delivery remains available to verify independently. The live application still uses the previous Library-fix bundle until client CI/deploy succeeds.
- Candidate **`831985455ecd9a4176c842f0481bba15dd63c3d9`** is now main and exact-commit verify/deploy [35461234791](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/35461234791) is running. The commit-bound readiness checks passed before promotion; no duplicate feature-branch full workflow was dispatched. Source/job code is published, but this is not a successful Pages deployment claim.
- Fresh security advisors list the same four known warnings: authenticated execution of `append_sync_operations`, `content_lesson_available`, `reconcile_recording_retention`, and disabled leaked-password protection. The graded migration changes none of those functions or Auth settings; its own RPC remains security-invoker and inaccessible to anon/authenticated roles. No grant was weakened or paid upgrade purchased to silence warnings. Fresh readback confirms paid budgets remain paused and service usage remains11.
- Release update: workflow35461234791 completed verify and deploy successfully. Actual public HTML returns200 and `index-BpzDkHke.js`, matching the configured candidate. Public-origin external-study checks pass10 cases with2 explicit Windows WebKit capture skips; authenticated catalog transport is fixture-backed, not proof of owner Level2 delivery. Earlier pending-client statements above describe the pre-release checkpoint. The last timer readback still has only the successful manual not-due dispatch; scheduled delivery is not yet accepted.

### External-link recovery — LIVE, September20

- Added an explicit inaccessible-lesson action: save the original draft/recording, select an approachable human publisher link (prefer another publisher), replace only that listen slot, retain other assignments and daily minutes, and keep the original unfinished attempt available. A24-hour learner-specific access report is not objective playback, a publisher-wide outage, completion, skill evidence or an FSRS score. No suitable alternative leaves saved work intact and explains the limitation.
- Transactional checks cover account changes, cancellation, failed saves and stale-device plan merges with original recording metadata.138 focused content/sync cases and274 affected planner/page cases passed; scoped lint, typecheck, configured build and secret scan passed. Independent review found and corrected selection/binding and navigation-cancellation races; the final minimal reproducer confirms cancellation causes zero replacement navigations, resolving the remaining finding. The new built-browser recovery journey passes on all4 desktop/mobile Chromium/WebKit profiles; configured bundle `index-C-lFuLQr.js`. Publication remains pending. No paid request or backend change was made.
- Owner reminder, September20: prioritize completing usable functions; avoid repeated unchanged whole-project checks. Finish this bounded recovery slice with only relevant critical checks, then proceed to language isolation and Japanese-specific implementation. Already completed checks are reused; staged release status stays separate from whole-goal completion.

### Language-partition foundation — September20 local implementation, not enabled

- Preserved the English database name/record IDs, version2 backup and existing sync/recording contracts. Japanese uses the same database/merge code in a separately bound IndexedDB; a sticky open fence rejects relabelling and never adopts old unlabelled English data. Same IDs can coexist without mixing projections. Both spaces use the same owner account.
- Implemented Japanese metadata remote/RPC and additive owner-RLS table, explicit version3 Japanese backups with pre-mutation cross-language rejection, separate private recording manifest/canonical object paths, and a Japanese-frontier/policy-fenced retention RPC. Existing English data/table/RPC identities are untouched. Shared-bucket originals retain hash checks, no-overwrite and draft protection. The new migration is tested in local rollback only, NOT applied to the hosted backend.
- Scoped checks:12 new partition/SDK/audio cases passed,72 existing database cases and115 affected sync/store cases passed; typecheck and scoped lint passed. Real dedicated-local SQL migration/RLS/old-client/clock/recording-path/retention assertions passed and rolled back. Independent read-only review approved the base partition; its encoded-traversal finding in audio was fixed and the focused no-network regression passed. No unchanged full suite was repeated for this stage.
- Japanese UI remains disabled. Shared total daily-time allocation, safe language switching/initialization, Japanese-specific content/diagnostic/processing/AI practice, automatic supply, physical-device/actual owner sync proof and staged production acceptance remain required. This foundation is not a completed Japanese learning space or final goal delivery.
- English recovery commit `959cf306cfec9d3500b5b6989e9bc43582a7d443` is live: exact-commit verify/deploy35462605487 succeeded, actual public HTML returns200/`index-C-lFuLQr.js`, and all4 focused desktop/mobile Chromium/WebKit production-origin recovery journeys pass. Earlier publication-pending wording records the pre-release checkpoint. No duplicate full workflow or paid request was made. Language-partition commit `ddccc28` remains a separate local candidate, not the deployed application/backend.

### Shared Japanese account lifecycle — September20 local development candidate

- Reused the existing Supabase auth client and per-language remote rather than a second login. A background Japanese handle survives page teardown; English-only startup does not create a Japanese workspace. Shared logout pauses Japanese even before its initialization has started. Epoch checks and the accepted-English-owner guard block delayed startup and a newly selected member B from binding/uploading data while English still belongs to A.
- Added per-language sidebar/account status, pending recording/error notices, safe retry after failed admission, and Japanese navigation that cannot accidentally open English Review under a Japanese label. Remote downloads refresh unattended Today recommendations, never overwrite an active response. Full cloud and AI readiness remains separate.
- First browser integration exposed a real Dexie absence-probe/creation race. Installed dependency source showed `Dexie.exists` dynamically opening then deleting a missing database; concurrent first creation could fail with OpenFailedError. Replaced both language-presence checks with documented name enumeration. No owner data was involved; failure and recovery used fresh isolated browser stores.
- Verification:8 coordinator cases (including actual SDK with mocked auth/REST, separate RPC streams, member-switch denial and pending-init/logout cancellation),7 affected shared-day cases, typecheck/scoped lint, and desktop/mobile Chromium review journeys after the first-open fix. Independent review explicitly closed the pre-upload owner and pending-initialization/sign-out findings, and confirmed non-opening presence checks; this is not real-account/device acceptance. No unchanged full test matrix, new paid request or hosted deployment. The language migration remains un-applied to hosted production at this checkpoint.

### Japanese delayed-review page — September20 local development candidate

- Added `/ja/review` behind the same development-only route gate. Today automatically reserves a short due-review block inside the shared allowance, followed by a new lesson. New expressions wait until a later local day; future-created items are excluded. A started lesson does not acquire newly inserted reviews. Each block selects at most one modality per expression, avoiding answer priming from adjacent sibling cards; other due cards remain scheduled rather than forming a compulsory backlog.
- Chinese prompts separate recognition, listening, recall/sentence construction, speaking and new-context use. First responses/recordings are durably saved and frozen before reference reveal. Hearing a publisher recording remains self-report; its transcript is not fabricated from app-authored examples. Using help schedules an early retry even after an easy rating. Self-ratings adjust only the selected FSRS modality and do not raise observed speaking/acoustic/mastery scores.
- Added revision/repetition guards and an actionable conflict-recovery path. When another device has advanced a card, retain the unscheduled answer, preserve any frozen original, and skip that changed card without a second FSRS application. Fully skipped blocks retain optional/not-done plan history and supply neither `TASK_COMPLETED` nor completed-minute credit; the next lesson remains reachable.
- Evidence: six new review unit cases plus six affected workspace cases; desktop/mobile Chromium persisted-review journeys; a focused browser conflict/recovery journey; scoped typecheck/lint. The conflict case explicitly proves Japanese completed minutes remain0 and account remainder45 after a wholly skipped block. Independent review caught sibling-answer priming, missing reps-conflict recovery and optional-completion budget leakage; all received targeted corrections. Final scoped re-review independently passed the budget/recurrence regression and reported no new blocking finding. No unchanged whole-project test matrix or paid provider call. Review is not yet production-enabled or actual two-device cloud proof.
- Next implementation: shared Japanese account/cloud lifecycle and truthful per-language sync/navigation; then language-specific AI and broader graded curriculum/reading, hosted migration and staged production/device acceptance. The English public site remains959cf30.

### Japanese guided page — September20 local development candidate

- Implemented a development-only `/ja` page in the existing shell: resumable six-item script/meaning diagnosis, automatic daily course, publisher link → closed-text meaning recall → contextual expression/own sentence → recorded new situation → replay/compare/full recorded retry. Chinese guidance and kana/mora/reference support remain separate from unmeasured listening/speaking. Native Chinese design/full-implementation autonomy is now explicit in `FINAL_UPGRADE.md`.
- Drafts use revision checks; stage prerequisites, same-workspace saved recording checks and atomic/idempotent completion preserve both originals and create only authored reference review cards, not the learner's unverified examples. Neither a link click nor participation earns a mastery/acoustic score. Overnight completion consumes the original assignment once on the real completion day. Recorder binds to an immutable database, durably attaches before releasing a recoverable draft, and never routes Japanese to the English transcription provider.
- Focused evidence: six new workspace cases; desktop/mobile Chromium diagnosis persistence and full two-recording journeys; delayed-save/navigation regression; existing English recording/API-failure recovery case; scoped lint and typecheck. Browser capture uses the configured fake microphone in a real browser, not owner/physical-device evidence. Initial browser checks found a missing in-page draft-list refresh, fixed and rerun only for the affected journey. Independent review identified save/navigation timing races; fixes serialize actions and freeze editing while changing practice. Narrow re-review reproduced the delayed refresh and approved both fixes, including release after failure, with no new blocking finding.
- Not a production delivery: Japanese navigation is `import.meta.env.DEV` only. Japanese review UI, shared account/cloud orchestration, language-specific AI, broader graded input/reading and hosted migration/real owner/device acceptance remain required. No paid call or backend/Pages release was made. English official release remains959cf30. Next: connect Japanese modality-specific recall/delayed reviews and shared sync, then complete language-specific AI and release acceptance; do not repeat unchanged English audits.

### Shared daily time and Japanese course core — September20 local candidate

- Added one English-profile daily time allowance shared across the two bound account workspaces. Completed task minutes are deduplicated within each language (not between equal IDs), optional completed work still consumes the allowance, and begun work is reserved before bounded overdue-review weighting. This is workload accounting, not measured learning time or a mastery score. Already completed excess stays as history; no new compulsory quota appears on switching.
- English Today and task start now honor the shared allowance. Offline midnight/focus refresh discards yesterday's cap, stale starts cannot emit new start evidence, and external replacement checks fresh remaining time and maintains valid plan totals after shortening. No Japanese database is created by merely opening English; foreign-owner data is never combined. Built desktop Chromium/mobile WebKit cases confirm Japanese40 minutes leaves English5 minutes out of a45-minute total.
- Added18 original Chinese-guided Starter missions linked to Japan Foundation Irodori's actual publisher playback pages, with narrator-credit provenance, contextual grammar/register, reading and mora-awareness tips, and new-situation prompts. Text-only diagnostic varies kana/furigana support and a conversation probe without declaring listening/speaking or CEFR attainment. Japanese uses its own script segmentation, kana timing units and progression/expiry logic. These are finite Starter materials, not complete Japanese graded/long-term supply.
- Bound the existing event/chunk/FSRS/repair repository to immutable database instances. Legacy named exports remain English-bound. Japanese bootstrap does not auto-complete setup; material language is checked at initialization, backup and both journal directions. Equal-ID card tests preserve the other language's schedules/skills; script recognition does not supply speech/acoustic evidence.
- Relevant checks passed:7 shared-day cases,30 store cases,15 Japanese cases (the final wrong-language download fixture was corrected and rerun alone),72 existing database cases; scoped lint/typecheck, configured build `index-DTSqle44.js`,245-file source/build secret scan, and2 focused built-browser cases. Independent scoped review found and resolved rollover/replacement-cap issues and approved the language-bound repository; no unchanged whole-suite repeat or paid provider request.
- Not deployed/enabled: Japanese UI/safe switch, diagnostic persistence and full practice route, AI language context, broader graded reading/curriculum supply, shared-settings/cloud orchestration, actual hosted language migration and owner/physical-device acceptance. Routes live in `src/main.ts`; the next implementation step is to connect the verified course/repository/budget core to the Japanese learning pages, not restart old English checks. Official site remains the recovered English release959cf30.

### Earlier English catalog checkpoint — local acceptance

- Implemented a separate metadata-only worker for the exact official VOA Level 1 directory: all 52 distinct lesson positions/URLs are required, existing lesson identities retained, incomplete replacements rejected, prior snapshots preserved. It cannot call an AI provider or download audio/transcripts. A real DNS-pinned publisher fetch parsed all 52 links; persistence in that network test was simulated, not hosted delivery.
- New additive RLS-protected snapshot/lease RPC and separately installed `catalog-refresh` cron route. Actual dedicated-database DDL and permission/lease/failure assertions passed inside **BEGIN/ROLLBACK**, leaving no lasting schema or data change. Scheduler installation is not implicit in migration; the old audio job and paused paid budgets remain untouched.
- Authenticated client receives the whole reserve independently of legacy audio delivery, fences account changes and preserves learner fields. Source freshness alone advances; catalog-only links expire for new assignments after 90 days, while static starter links, saved work and already-bound tasks remain. The planner's fallback also observes expiry. Missing/failed refresh cannot delete the reserve.
- Independent review found and confirmed fixes for cancellation-before-commit and stale-catalog reassignment. **146 targeted tests passed /1 explicit live-network opt-in skip**, typecheck passed; the initial 52-link live check also passed. Five transactional SQL suites are now wired into isolated release CI. Exact-commit CI, hosted migration/runtime/scheduler, authenticated delivery to Today and public desktop/mobile acceptance remain pending.
- This is a finite beginner sequence, not an ongoing new-publication feed or complete advanced curriculum. Broader graded sources, language isolation and Japanese-specific learning remain required. No paid provider request was made for this checkpoint.

## Final upgrade — active, NOT accepted

### Partial release — LIVE, 2026-09-13 local / September12 22:34UTC

- **Official site:** https://mnijc19-netizen.github.io/jove-english-os/ . Deployed application commit **`32eed7ef28035438d7815e9ea25e9d10262fad79`**, [successful verify/deploy workflow34722286803](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/34722286803), GitHub Pages deployment6415138313. Actual production entry is `index-C_rvDL4S.js`, matching the locally tested cloud-configured build. Subsequent changes to this acceptance record are documentation-only, not a different application release.
- Final CI: **1644 unit passes/59 opt-in or environment skips**; actual Auth/Storage9 passes, isolated content persistence314 passes/15 skips, Deno4 passes, real Chromium/WebKit multi-profile migration/offline/review recovery2 passes, **168 browser passes/3 skips** in9.0min. Lint, typecheck, build, security, dependency audit and both commit-bound release gates passed. This supersedes the failed f41 browser run below without removing its assertions.
- Actual public-origin checks after deployment: external-study flow **6 passes/2 explicit Windows WebKit capture skips** across desktop/mobile Chromium and WebKit. Linux CI exercises capture; these are not physical iPhone/Android tests. The two separate old-production desktop/mobile profiles deliberately updated from `index-CviBBF9R.js` to the new bundle: saved original listening text survived update and reload, external lessons and the cloud sign-in form appeared, and no uncaught page error occurred. Earlier fixed-window observers expired before deployment; only the completed final observation is update acceptance.
- Shipped: four publisher-link starter lessons, automatic daily selection, guided recall/expression/recorded retell with durable work, configured account/sync entry, and normal practice without automatic acoustic scoring. Existing routes, design system, offline-first storage, FSRS and original recordings are retained. Publisher audio/video is not copied or automatically downloaded by the external lesson flow; publisher playback still requires the network and remains self-reported.
- **Still not final-upgrade completion:** recurring curated content discovery beyond the four links, remaining live AI work, and physical-device/owner production sync and recording-retention acceptance. A fresh dedicated-project readback after publication confirms AI budgets remain paused,11 service-usage rows, private Vault access and no scheduling extensions. This release made no further paid provider request. Account entry being available is not a claim that paused AI services or every three-device journey have passed.

### Current owner scope amendment — 2026-09-13

External publisher playback/links now replace mandatory bulk hosted audio processing. Automatic acoustic pronunciation/prosody scoring is explicitly removed from required delivery. Keep automatic courses, the learning loop, reference listening/recorded retries and three-device progress. The `FINAL_UPGRADE.md` amendment supersedes conflicting older gates below. The owner subsequently authorized publishing the completed subset now; remaining final-upgrade work must not be represented as delivered or silently block this partial release.

### External-study partial release — locally verified, publication pending

- Four editorially selected publisher links are available to the automatic daily planner. The existing Listen page guides meaning recall, a noticed expression, a new example and a saved spoken retell. Drafts survive reload; external playback is explicitly self-reported and cannot create objective listening/comprehension or acoustic scores. No publisher audio/video, transcript or answers are copied into the app. This small starter selection is not a completed long-term automatic discovery service.
- Automatic pronunciation/prosody assessment is disabled on normal Listen/Speak pages; original recordings, shadowing and legacy assessment data remain intact. No Azure configuration or further paid content-audio trial is required for this release.
- Independent review identified and verified fixes for an immutable-event retry conflict and a route-change/delayed-recording-lookup race. Final local units: **1644 passed,58 explicit integration/opt-in skips**; lint/typecheck/build pass. New built-browser checks: **6 passed**, covering desktop/mobile Chromium and WebKit draft recovery; two Windows WebKit capture checks explicitly skip because that port lacks audio APIs. Linux CI will exercise those capture cases. Physical iPhone/Android acceptance remains separate.
- Dedicated-project public frontend connection variables are installed; the validated public anon credential is not a server secret. Exact-commit main CI, Pages deployment and fresh production browser checks remain pending at this checkpoint. The historical backend/provider diagnostics below are not reasons to delay this authorized subset.
- Release candidate `02024c4` was pushed to main. Its first workflow `34720138737` was deliberately cancelled after identifying old BYOK browser tests that assumed a non-cloud build. The correction opens the existing optional-key panel after waiting for the Settings-specific heading; no production behavior or prior assertion changes. A real mobile failure reproduced the initialization race before the final correction. Independent review approves; all six affected desktop/mobile cases now pass against the actual cloud-configured production build (`index-Cca6t7QE.js`), which also passes typecheck/build/security. The successor needs one complete main deployment run. Two isolated profiles hold saved old-production drafts for the actual update/migration check; no owner profile is used.
- Candidate `258967b` workflow `34720521127` was stopped before deployment when the final mobile screenshot exposed overlapping CloudAccount heading/description text. The account section lacked the existing design system's icon grid cell; adding the same small-icon/shield used by other settings sections fixes it without CSS/layout redesign. The regression now asserts vertical text separation and same-column alignment; an isolated real mobile DOM probe passes the fix and fails after removing only that icon, reproducing the old layout. Independent review approves. Final configured build/typecheck/security pass (`index-C_rvDL4S.js`); external flow6 passes/2 Windows capture skips and the strengthened layout/draft checks pass all four browser profiles. No paid request or backend mutation was involved. Publish this bounded correction; final-upgrade scope remains incomplete.
- `f41fd22` CI `34720804232` passed units, SQL/RLS, real Auth/Storage, Deno, multi-profile offline/migration/recovery, configured build and security, but browser deployment gate failed: **158 passed,3 skipped,10 failed** in19.5min. The ten failures are five scenarios across two viewports, all caused by two older fixtures inserting a placeholder key without explicit `provider-mode: byok`; the configured app correctly kept signed-out account AI unavailable. A local fail-fast run reproduced the first disabled-button failure. Both test fixtures now seed the explicit optional mode alongside the fake key, with unchanged assertions/provider interception and zero product-source changes. All ten affected configured-browser cases pass in24.4s; lint/diff and independent review pass. CI now uses `--max-failures=1`, retaining the entire successful suite, retries and flaky-test rejection while returning the first failed result promptly. No production permission was relaxed. A successful successor deployment is still required. An earlier fixed-window production-update observer expired while CI was pending; it is not a product data-loss failure or a successful update check.

The final v14 rotation slot3 finished September12 20:56:16UTC: the legacy VOA item left the selectable queue, and a fresh STT request returned HTTP400/INVALID_ARGUMENT. Original financial history was preserved; one new service row and an uncertain0.50 hold were added, budgets restored to paused, no scheduler or eligible clip. Production totals are11 service rows,2.5027424 held and0.00164783 reported; holds are not confirmed invoices. All three rotation receipts are consumed.

One separate user-authorized backup-key request using synthetic silence and the production review schema also returned400/INVALID_ARGUMENT at20:57:38UTC. Its ignored exclusive receipt retains an independent0.10 unknown-cost reservation; no learning evidence, production secret or DB was changed. Neither the simple-schema nor text-control variant ran. Additional paid diagnostics stopped on the owner's amendment; exact rejected parameter remains unknown.

2026-09-08: full owner instruction read and mapped in `FINAL_UPGRADE.md`. Clean starting HEAD `3912be4`; previous V1 evidence below remains historical only. Docker engine and GitHub authenticated CLI are available. No cloud/Speech production availability is assumed.

Remaining final-upgrade work under the amended scope: recurring curated external-content supply beyond the four starter links; production owner and physical-device sync/recovery journeys, including recording retention; remaining live AI features and usage readback. Automatic acoustic scoring and mandatory bulk hosted source-audio processing are no longer required. Partial-release gates are exact-commit main CI, Pages publication and production browser checks; they are distinct from final-upgrade completion. No item is complete merely because its adapter or test fixture exists.

### Reconciliation queue fairness — deployed, 2026-09-13

- The single instrumented v13 invocation at September12 19:23:59–19:26:09UTC reached the reconciliation fence before any model dispatch: two sources claimed, zero feeds/items/segments processed, zero service rows or holds added. Historical service/content rows remain unchanged; budgets were restored to paused, no scheduler is installed. The exclusive `.work/content-structured-diagnostic-once-20260913.json` receipt is consumed. Ten service rows still hold2.0027424 and report0.00164783; holds are not invoices. No backup-key use or new HTTP400 cause is established.
- Fresh read-only SQL found exactly one matching current-version VOA item in `awaiting-analysis`. The worker's broad `required` classification made it occupy the episode/audio-work slot repeatedly. The candidate quarantines the exact reconciliation error and normalizes only matching legacy current-version awaiting rows before the existing bounded episode slice. It uses normal lease/revision-fenced checkpoints, preserves protected work and adds operational history; no migration, ledger edit, media replay, framework change or increased network limit.
- Failed-first tests reproduced both the queue problem and a cancellation-before-normalization race found by independent review. Ten focused tests now pass, including fresh material progress, preserved audio/intents/history, new revision/version and incomplete-metadata guards, stale lease refusal and cancellation without dispatch. Final full units pass1631/58 explicit opt-in skips in43.68s; scoped ESLint, Vue typecheck,1,796,669-byte function build,224-file security scan and whitespace checks pass. Rollback-only real SQL projection/fence assertions are included for isolated CI because local Docker remains unavailable. Hosted functions remain v13 until the reviewed candidate passes release gates; no new paid trial has run.
- Independent review approved worker/SQL code in `13ef381`; CI `34715029293` then caught a test-only5000ms timeout: recursive deep equality over the full70s PCM fixture took2.55s even locally. Native `Buffer.equals` preserves exact byte/length equality and missing-blob failure while reducing the isolated test to52ms (independent55ms). This assertion-only successor passes the full worker file298/31 explicit skips in7.33s and scoped lint; production source, SQL and runtime hashes are unchanged. No global timeout increase or weakened preservation check. Successor CI remains required before deployment.
- Successor `be4c0c5c0a8c49be1c1c6cd383a10d19e7122780` CI `34715248911` succeeded at September12 20:03:59UTC, including actual SQL, Auth/Storage, Deno, offline/multi-profile recovery,160 browser passes in7.4min and0 dependency vulnerabilities. Dedicated14-migration readback passed. All three functions were deployed to **ACTIVE/version14**; downloaded entries and1,796,669-byte runtimes match SHA256 `9921395B241E698A46C323F9AA9C5953CECAB6D371E7B8D750E288E425454DC5`. All24 metadata snapshots match before/after, aggregate `9ad370a4917a329c87555f3206b5f67ab8f1233907a87ed12a111adbe2677e97`. Fresh anonymous production-origin probes remain401/SIGN_IN with correct CORS. No Pages publication or readiness variable was issued.
- After independently reviewed live preflight, one v14 worker invocation at20:12:20–20:14:32UTC again dispatched **zero** model requests: no new service rows/holds or eligible lessons, original service/content/intent histories preserved, budgets restored to paused and no scheduler. Receipt `.work/content-queue-diagnostic-once-20260913.json` is consumed and cannot be replayed. Actual read-only item metadata identifies a newly quarantined current-version Open Yap STT item (one existing intent/usage row) plus an HPR item quarantined without usage. The prior current-version VOA awaiting-reconciliation item is unchanged: this run rotated to other sources, not VOA. Queue advancement is real but fresh course generation and the HTTP400 cause remain unverified. The supplied backup key remains unused.
- Read-only order inspection confirms older JB The Launch/JB Linux audio work precedes VOA; the current-version Open Yap quarantine is excluded. A separately reviewed operator now has exactly three fixed exclusive rotation receipts, no replacement slot or automatic loop. Later slots require all earlier results to be observed/paused with zero added financial rows/holds/reported cost, no usable lesson or provider diagnostic, empty/reconciliation-only error codes, no timeout/cancellation/unknown failure, unchanged live10/4/3 financial histories and same-ID/revision/source legacy items demonstrably moved out of the selectable queue. An increased unrelated quarantine count is insufficient. Fifteen offline tests and independent missing/non-array diagnostic probes pass; the unchanged v14 worker may still perform STT plus two analyses within one invocation, so stopping means **no subsequent slot** after provider work or uncertainty.
- Rotation slot1 at20:37:38–20:40:05UTC advanced the old JB The Launch item to current-version reconciliation quarantine without changing its revision, added no service rows/holds and restored pause. Its exclusive receipt `.work/content-queue-rotation-20260913-1.json` is consumed. Slot2 is in progress under the same live gates; there is no permission to replay slot1 or invent a fourth slot. No new product source, SQL migration or CI run was introduced for this bounded operational continuation.
- The supplied backup key was checked only through the existing read-only metadata adapter, using verified non-echoing stdin and process memory. Its response reports HTTP200/accepted, its own3USD limit,3USD remaining and0USD usage; generation remains explicitly unverified. No credential was written to a file, environment setting, command argument or production secret, and no new-key generation was issued. This metadata is not an account-balance proof or reconciliation of historical requests.

### Bounded provider diagnostics — deployed, 2026-09-13 local / September12 19:17UTC

- Fixed the missing structured HTTP diagnostic path without changing provider requests, model fingerprints, automatic retries, billing settlement or lesson quality gates. Only allowlisted error types, canonical upstream codes and up to four public parameter roots can persist; raw/provider messages, headers and private field suffixes cannot. Parsing is bounded by16KiB/500ms, cancellation and an empty-chunk limit. This does **not** recover the discarded historical400 bodies or establish their cause.
- Independent review found a worker-cancellation race in the initial implementation; two failed-first worker tests reproduced it for both routes. The corrected adapter throws the observed HTTP failure immediately, and the worker failure checkpoint awaits only the bounded optional enrichment. All four independent cancellation/stalled-body probes pass, retaining saved audio and uncertain usage. Final scoped results:103 audio-contract tests and289 worker-file tests pass;31 opt-in tests are explicitly skipped. Scoped lint/typecheck, function build and224-file source/build security scan pass. Full units before the final race correction passed1620/58 skips; that earlier count is not a final-source full regression claim.
- Exact commit `c973b2aa9a2c90c9e550d51acb454e8dfbece2fd`, CI `34712674176`, completed successfully at19:12:53UTC:160 browser passes in8.4min,0 dependency vulnerabilities, plus real isolated Auth/Storage, JSONB/RLS/content, Deno and Chromium/WebKit multi-profile gates. This replaces the pending real-DB check: local Docker could not start and was not repeatedly restarted, but the revised persistence test passed in isolated CI. Production owner/device acceptance remains separate.
- After source and readback-helper review, the existing dedicated-target deployment command verified all14 migrations and deployed all three functions to **ACTIVE/version13**. Downloaded runtimes and entry files match1,795,855 bytes/SHA256 `2F3E0C88BB042FCE860DD6D28DF888765C3E175696A025AC58F7F1CDF76E590C`. All24 public/Storage metadata snapshots match before/after (aggregate `d66722d6b5ac7aacb5f7774500e5721fc9e5c7ed009e02b9dbf311629b0e31b4`);10 usage rows,0 active leases and paused budgets remain. The3.8 override is preserved. Three anonymous production-origin probes return401/SIGN_IN with correct CORS. No generation, scheduler, Pages release or backup-key configuration occurred; Azure Speech key/resource configuration is still absent.
- Scoped native Windows accessibility interaction successfully opened the owner's existing OpenRouter Logs view without browser-profile/token extraction. The latest3.8 upstream detail identifies `gen-1789238305-NvOJoLNzgpogZQSyq3Wo`, resolved model `google/gemini-3.8-flash-20260902`, Google AI Studio,400 and2.8s. The expanded visible dialog offers summary fields, not a concrete Error/Response reason; the Generations tab has no visible3.8 row. The browser was returned to Upstream Requests. This narrows the missing evidence but does not establish the rejection cause. A supplied alternative key remains unused, with no assumption about its spending limit.

### Active-model trial — 2026-09-13 local / September 12 18:40 UTC

- One independently reviewed, bounded worker invocation using the explicit `google/gemini-3.8-flash` setting completed with **OpenRouter / generate / HTTP400 / content-analysis**. Two sources were claimed, one quarantined segment was saved, and **zero eligible lessons** were produced. Updating the model did not resolve the observed provider rejection; the discarded response body means the rejected parameter or upstream reason is still unknown.
- The fresh operator passed8 standalone checks and actual read-only preflight against unchanged f10/20E/v12 source. Its exclusive receipt `.work/content-active-model-once-20260913.json` is consumed. The inclusive2.05 cap was restored to paused after the single invocation; historical service/content/intent rows remained unchanged. One new uncertain service row reserves0.50, **not a confirmed charge**:10 service rows,2.0027424 total held and0.00164783 reported. No schedule, queue reset, historical settlement or repeat invocation was performed.
- Workflow adjustment following the owner's efficiency instruction: batch documentation updates, use scoped tests first, reuse unchanged source evidence and run full regression for substantive release candidates. This documentation/configuration checkpoint does not launch another full CI or publish Pages. Further paid retries are stopped pending actionable provider error details; acoustic configuration, authentic core coverage and final owner/device acceptance remain open.

### Production content model configuration — 2026-09-13 local / September 12 18:24 UTC

- At the owner's explicit request, set only `JOVE_CONTENT_AUDIO_MODEL=google/gemini-3.8-flash` in the dedicated Jove backend. The setting was previously absent. Current OpenRouter catalog and endpoint metadata support audio input and structured outputs; a synthetic offline test of the existing adapter verified the override, exact audio bytes, privacy/schema constraints and model-bound version. No paid generation or resolution of the earlier HTTP400 is claimed. The source fallback remains `gemini-2.5-flash`; production now uses the explicit override.
- Independent source/configuration review found no blocking issue. Native CLI metadata uses `value` for the SHA256 digest, not a `digest` field; the initial preflight stopped before any write and the reader was corrected after a read-only shape check. Saved model digest matches `422e5ba87859dd55a2d59ee40eda13777c0c0d7976e023e4e5c8982a0ba38584`. All other secret names/digests/update times and all24 public/Storage metadata snapshots are unchanged. Budgets remain paused,9 service rows, no schedule or additional provider request.
- Saving the configuration automatically advanced all three ACTIVE functions from version11 to12, contrary to the initial readback's unchanged-version assumption. No explicit deployment was run. Fresh downloads of all three runtimes and entry files confirm unchanged code:1,790,976 bytes/SHA256 `20E77B2ADA5F6407521AECF36DBB22D80A60BC488D1E4C64831646FF6B1C3D5F`. Source remains `f10d580` with successful CI `34705961359`; this configuration-only change does not need another full CI or Pages publication. No historical usage hold, intent, queue or retry receipt was reset.

### Hosted HTTP 400 and fact-contract correction — 2026-09-13 local / September 12 UTC

- **Later deployment checkpoint,16:32UTC, CI refreshed16:39UTC:** source commit `e38174f80fff01cc84c754736849764df7397aa1` CI `34704234553` is terminal success, including160 browser passes in7.6min and0 dependency vulnerabilities. Commit `3029de00f2bb8005dde426de0e5e82e3ac2183fa` differs only in `docs/STATUS.md`; its separate CI `34704677550` was running at deployment and subsequently completed successfully at16:38UTC, with160 browser passes in8.3min and0 dependency vulnerabilities. Independent review verified unchanged backend/build inputs and approved this **functions-only, byte-identical** deployment on e381's code evidence; no latest-HEAD-required operator was bypassed, and Pages/final acceptance/paid tests remain separate. Fresh target,14 migrations, source/test/runtime hashes, paused9-row/no-lease state, empty release variables and terminal main workflows were verified. The guarded deployment completed normally; all three functions are now **ACTIVE/version11**. Actual downloaded runtimes are1,790,976 bytes/SHA256 `20E77B2ADA5F6407521AECF36DBB22D80A60BC488D1E4C64831646FF6B1C3D5F`, with matching entry files. Three anonymous production-origin requests returned401/`SIGN_IN` with correct CORS.
- The independently inspected read-only helper `.work/fact-domain-deploy-readback.mjs` (SHA256 `7649F323E815E38003416ABCE10017E5677C29C4E046C24ED926200E9B6CCAAC`) compared all22 public-table row snapshots plus Storage bucket/object metadata before/after:24 snapshots match, aggregate SHA256 `dc875febfab731217b2257993620bcae45d1d969435a0d71016d5bf2da2cbc62`. This preserves row metadata, not a new byte-by-byte audit of every stored recording. Budgets remain paused,9 service rows/0 active leases, no paid dispatch or scheduler; the earlier HTTP400 remains unresolved. [Google's current lifecycle table](https://ai.google.dev/gemini-api/docs/deprecations) lists no announced shutdown for the exact stable `gemini-2.5-flash`, and [OpenRouter still lists it](https://openrouter.ai/google/gemini-2.5-flash). This does not establish authenticated route availability, but it does not justify declaring the model retired or blindly switching providers. The bounded OpenLearn source check is recorded in `CONTENT_REVIEW_PACKETS.md`; nothing was admitted or approved.
- Exact `2048fb023e5176de7feffa0521e78a3b7a5af145` CI `34702405472` completed successfully:160 browser passes in8.4min and0 dependency vulnerabilities. No Pages publication occurred. The new, separately reviewed diagnostic operator passed8 standalone and28 independently mocked lifecycle checks, then actual read-only preflight confirmed the deployed v10 bytes,14 migrations and preserved baseline of8 service rows,2 content-usage rows and1 modern intent. The older content row predates intents and remains protected by its existing same-owner item checkpoint and the014 fence.
- Exactly one new worker invocation completed at15:53:06UTC under an inclusive1.75 allowance, within the owner's external3.00 cap. Receipt: ignored `.work/content-http-diagnostic-once-20260912.json`. It processed one source and saved one quarantined candidate, but approved **zero** lessons. The real diagnostic is **OpenRouter / generate / HTTP400 / content-analysis**. This is not the earlier STT failure's missing HTTP evidence, and it does not identify the rejected parameter. The involved `jb-the-launch` MP3 prefix has SHA256 `67ae08dc7282038da21eea9426ba1e6edce62b48042d7e3f2a477d1f19be9ab8`; the requested interval207.419–265.679 seconds is within its349.492-second recorded coverage. Coverage alone does not certify decoding, human speech or quality.
- Original service/content-usage/intent row images remained identical. All new service usage mapped to a new matching intent and same-owner content checkpoint. Actual post-run state: **paused**,9 service rows,3 audio objects,0 active leases,0 eligible lessons, no scheduler,1.5027424 held/0.00164783 reported. The additional0.50 is an unknown-charge reservation, **not a confirmed invoice**. The exclusive receipt is consumed, including for zero-progress/uncertain outcomes; no replay, rotation, queue reset, historical settlement or additional paid request is permitted by it. Operator/test SHA256: `CE256D2CC9A91B370E18E7A0AADB42F23F2F4CF6C43C34FE104936F3AD0D0A73` / `441E5BEF1DE6322FB7C525C6BDAEC661DBF5736B1BBAFC2E153CFA88A0B2EF8A`.
- Unpaid adapter checks independently found a separate P2: the shared fact union accepted inappropriate per-field types/domains, although downstream quality checks still rejected those observations. Main reproduced21 failed-first cases. The minimal correction uses nullable field-specific schemas matching the existing downstream booleans, accent enum,0–1 fractions and integer1–20 speaker count. Full local validation and cost receipts remain intact; null stays unknown, and false/other-accent observations do not become eligible automatically. No request fingerprint, version, historical hold or retry fence changed. **This is not established as the cause or fix of HTTP400.**
- The48 new malformed/null/boundary cases and2 existing audio-byte contracts pass; independent review approves the exact two-file delta after63 focused passes. Full units pass **1605/58 explicit opt-in skips** in42.60s; scoped ESLint/typecheck and whitespace pass. Source/test SHA256: `DF630A7780D5409D05F4142636C864E193252104F8C62DF8A6DC0DB7C597CAEC` / `DFD0F82B6167EA03D3E820F9E4CEEA721143F55A0B071F3E593455AE58F3FC08`. The new runtime is not yet deployed; fresh build/security/CI gates remain separate from2048's earlier green result. The owner was asked asynchronously for the failed request's redacted OpenRouter Logs details, without a key or another model call.
- Subsequent build/security gates pass: runtime1,790,976 bytes, SHA256 `20E77B2ADA5F6407521AECF36DBB22D80A60BC488D1E4C64831646FF6B1C3D5F`; frontend remains `index-DfcXU1Zn.js`;224 source/build text files passed secret-pattern checks. Actual local PostgreSQL/worker/pipeline passes420 cases/18 opt-in skips in52.74s, followed separately by95 passing network-transport cases. The combined scoped total is515 passes/18 skips, not a hosted or paid-provider result. New exact-commit CI and hosted deployment remain pending.
- Fix commit `e38174f80fff01cc84c754736849764df7397aa1` is pushed; CI `34704234553` is still running at16:14UTC, currently in its final page suite. An additional **zero-provider-call** audit (`.work/inspect-hosted-analysis.mjs`) re-fetched the exact public source through pinned allowlisted transport, revalidated source rights, matched the hosted prefix SHA/coverage and rebuilt the actual requested interval. Historical2048 and corrected adapters submit identical audio and messages:1,453,661 bytes/SHA256 `3b627de812155d5d6f77145f1622f403bb9a22fd47c57009a2a108cfa5222a2d`, frame window205.400816–265.952653. The original/corrected request sizes are1,947,622/1,946,685 bytes and schema sizes5,773/4,836 bytes. Disposable Windows Chromium, with no owner profile, decoded that exact clip to60.551833 seconds of finite non-silent stereo samples; this is neither provider acceptance nor WebKit/play/seek/human/acoustic-quality certification. Paused9-row metadata matched before/after. Its initial helper-only build failed because esbuild rejects a Unicode-flagged plugin filter; a standalone unpaid reproduction confirmed this, and the helper's filter was repaired before the successful audit. No production request format was changed by that helper repair. The original HTTP400 cause still requires trustworthy provider error details; no further paid request was made.

### Read-only provider access — deployed and checked, 2026-09-12

- Exact commit `a44bd09f1ae6ba5be1cc1e8222464e2ab67a646f` CI `34700624485` is terminal **success**, including160 browser passes and0 dependency vulnerabilities. Earlier HTTP-diagnostic commit `5674829` CI `34699925183` also completed successfully with160 browser passes. Neither feature-branch run published Pages.
- With budgets paused,8 unchanged service rows,0 active leases, no scheduler, no Pages readiness variable and no active main deployment, the guarded Jove-only function deployment completed at15:17UTC. All three functions are now **ACTIVE/version10**. Actual downloaded runtime for `ai`, `content` and `speech-assess` is1,790,482 bytes/SHA256 `965E35F1CF75C6708F656E071387D21F9A523494B09CFE6043EB3263ADECFEA8`; all entry files also match the reviewed source. Anonymous production-origin requests still return401/`SIGN_IN` with correct CORS. Public Pages remains the earlier main build.
- One initial protected migration-list read failed without a captured cause. A separate read-only split-phase diagnostic confirmed the healthy target and a subsequent successful CLI table response; its JSON-only parser therefore could not verify that table. The **unaltered original guard**, which already supports both output formats, then passed the exact14-version local/remote comparison. No reset, migration edit, connection override or weakened assertion was used; the initial cause is not established.
- The independently approved, exact-candidate-pinned one-shot status operator completed at15:19:51UTC. It verified actual deployed bytes and all preconditions before its exclusive fsynced receipt and in-memory job-token access, then sent exactly one fixed `provider-status` POST. The server's OpenRouter key-metadata GET returned200/`accepted`, confirmed the intended3.00 Key cap and approximately2.997 remaining. This is **Key metadata, not account-balance or generation proof**. Seven relevant metadata snapshots (preferences, service usage, source/item queues, content usage, intents and content-audio objects) matched before/after. The result explicitly retains `generationVerified:false`; no model generation, refresh, budget mutation, settlement or schedule was requested.
- Operator SHA256 `E6DD6563FB7804A8996B4A90B6C2424083E04E640B449AF9E744FD7CFC731B40` and offline tests `FBA04DC6DF57E38626CCF1FC6C91AB2EABE66DA825CF29CF0E6C2E134232C95C` were independently approved after5 standalone and11 fully mocked operator scenarios. Receipt: ignored `.work/provider-access-readonly-once-20260912.json`; it must not be replaced or reused to authorize generation. Fresh readback remains8 service rows,2 saved audio objects,0 eligible lessons,1.0027424 uncertain holds/0.00164783 service-reported cost, paused budgets and no schedule. Aggregate Key usage is not allocated to those historical holds. All paid content tests remain stopped; a possible future separately bounded test is not yet implemented or authorized by this read-only receipt.
- A subsequent bounded **offline** exact-adapter request audit reuses the original demo WAV and the already acquired, hash-pinned public Linux prefix. Submitted bytes match the current window/remux implementation, base64 round-trips exactly, WAV/MP3 format and media limits hold, and model/strict-output/privacy-routing fields remain unchanged. Serialized sizes are1,302,274 and1,922,014 bytes; no source audio or prompt was sent to any provider. A deliberate in-memory400 verifies the new HTTP diagnostic path, not the historical failure. The original JSON schema contains constraints outside Gemini's provider-enforced subset, but [Google's API documentation](https://googleapis.github.io/java-genai/javadoc/com/google/genai/types/GenerateContentConfig.Builder.html#responseJsonSchema(java.lang.Object)) explicitly allows sending the full JSON schema while supporting only some features. This is not proof of rejection and did not justify a speculative schema rewrite; local Zod validation remains intact. Existing playback evidence is reused, not relabeled as new acoustic/lesson approval. Reproducer: ignored `.work/inspect-content-request-contract.mjs`.

- Added explicit `provider-status` to the authenticated content handler. The separate job-token union permits only the existing empty-object refresh or this exact read-only action; browser-origin jobs, all extra fields, explicit job refresh/other owner actions and the legacy probe remain rejected. Owner membership validation is retained. The status branch returns before profile reads, audio-service construction, refresh execution, storage or budget operations.
- Only the active OpenRouter route performs one hardcoded server-side `GET /api/v1/key`, with no redirects/cookies/cache, an8-second total deadline and16KiB response ceiling. Output projects only closed status/provider fields, observed HTTP status and finite key cap/remaining/aggregate usage/free-tier values. Unknown/null is not converted to zero; key labels, identities, credentials, response messages and arbitrary metadata are excluded. Every outcome includes `generationVerified:false`. Direct Gemini configuration is explicitly unverified/unsupported by this diagnostic, not tested using an inactive router key.
- Nine new failed-first cases now pass.29 focused checks cover authentication, payload separation, unchanged empty-job entry/probe refusal, unknown/zero/exhausted caps, malformed/oversized/nonfinite responses, stalled fetch/body deadlines and no provider requests for an inactive key. Full units pass **1557/58 explicit opt-in skips** in49.09s; actual local PostgreSQL/pipeline/worker/network regression passes **467/18 opt-in skips** in52.73s. Scoped ESLint, typecheck, function build,224-file source/build scan and whitespace checks pass. Runtime:1,790,482 bytes, SHA256 `965E35F1CF75C6708F656E071387D21F9A523494B09CFE6043EB3263ADECFEA8`; frontend source/assets are unchanged.
- Independent review approves the three-file source/test delta with no P0/P1/core-P2 findings and independently passes29 focused plus13 synthetic checks. Approved hashes: audio `91926106051D70845FC5B9EA03965E93A4E88333E8F9315033712D733A627FFB`, handler `75B244308EB8AFB10077835D89731A546A4FCF0880AC16E6432C210298AA188D`, tests `8CDFB4586DE81429B746D0853C54339699531C84D8FDE01CA65DC5E7574DFB3F`. No actual hosted status probe or new deployment has run. The ignored one-shot operator, separate exact-commit CI and downloaded-code gates remain required before that check.
- An accepted key response would not prove model availability, privacy-compatible routing, generation permission, account balance, Jove-only spending or any historical charge. No uncertain hold has been cleared; all paid content tests remain stopped. The earlier HTTP-diagnostic commit `5674829` has CI `34699925183` still in progress at this checkpoint; it cannot cover this subsequent access-status change.

### Hosted prefix activation and failed transcription — 2026-09-12

- Exact commit `073859f60d0ac67876c0f7db3b5c59d71d1d2c62` CI `34696620713` is terminal **success**, including160 browser cases in8.4m, real local Auth/Storage/content/Deno/multi-profile gates and0 dependency vulnerabilities. Feature-branch Pages publication steps remained skipped.
- After independent approval and target-pinned checks, the dedicated Jove hosted project advanced from7 to14 migrations and all three functions (`ai`, `content`, `speech-assess`) to **ACTIVE/version9**. The protected dry-run matched exactly008–014. Metadata hashes/counts across23 legacy tables matched before/after the migration and function deployment, excluding only the four new columns checked independently. Five installed function definitions/grants and40 constraints/column facts matched reviewed local14; all10 pre-worker invariant checks passed. An initial readback helper returned unverified without a captured cause; the same functional checks passed after adding bounded diagnostic field names, without changing SQL or weakening assertions.
- Actual downloaded source for each hosted function matches the reviewed entry and shared1,786,094-byte runtime, SHA256 `422321C6497FE152641F84FC91510F6E9D5D38D5B15914AB681E6121E7447ABC`. Anonymous production-origin requests return401/`SIGN_IN` with the expected production CORS. This is deployed backend/source evidence, not owner-authenticated end-to-end learning acceptance or a new Pages release.
- The reviewed one-shot worker attempt at14:03–14:05UTC stopped at the historical request-reconciliation fence:0 new service rows/holds and0 eligible lessons. A separately reviewed, fixed-slot zero-dispatch-only rotation extension then permitted one further attempt at14:16–14:18UTC. It ended with `CONTENT_AUDIO_PROVIDER_FAILURE`, **one additional uncertain0.50 hold**, no reported new cost and0 eligible lessons. Both attempts restored paused budgets and preserved every historical usage row. No next rotation was invoked; the new eighth row invalidates the reviewed seven-row activation baseline.
- Fresh read-only SQL confirms budgets paused,8 service rows,0 active leases,2 content service rows,2 saved audio objects,0 eligible lessons, **1.0027424 held** and **0.00164783 provider-reported**. Held amounts are not confirmed spending and must not be cleared or replayed without evidence. The14:15–14:20UTC failure window contains exactly1 uncertain STT usage row with its matching intent,0 analysis rows and0 captured HTTP diagnostics. No scheduler is installed. Post-worker queue/object changes are legitimate new state; the old23-table pre-worker snapshot must not be used to reset them.
- The previous adapter cancelled non-2xx responses but discarded HTTP status. The original failure therefore cannot be assigned to credentials, routing, credits, payload validation or provider availability from its stored code. The public model listing still advertises structured-output endpoints, but that is not authenticated route or billing evidence. [OpenRouter's error documentation](https://openrouter.ai/docs/api_reference/errors-and-debugging) distinguishes request status from later generation failures; neither an HTTP failure nor a missing lesson alone proves zero cost.
- A new **local, not yet deployed** diagnostic patch retains only a closed provider/stage enum and observed numeric HTTP status in owner-isolated `content_usage.usage`. Existing public error codes/messages, privacy routing, reservation/settlement/replay fences and lesson quality gates are unchanged. Raw response bodies, headers, URLs, credentials and provider-defined error text are excluded; rejected or stalled stream cleanup cannot mask the checkpoint.21 failed-first cases now pass, plus lookalike rejection and an actual PostgreSQL JSONB/owner-vs-outsider RLS test. Full units pass **1529/58 explicit opt-in skips** in45.42s; the actual local SQL/pipeline/worker/network regression passes **439/18 opt-in skips** in51.42s. Scoped ESLint, Vue typecheck, both builds,224-file source/build secret-pattern scan and whitespace checks pass. Client remains `index-DfcXU1Zn.js`; the new local server runtime is1,787,411 bytes.
- Independent review approves the exact three-file diagnostic diff with no P0/P1/core-P2 findings: audio adapter SHA256 `C3AD2FBD62EAEB39B70BDD8591A27AC163AF4FE601EFCBB77E2DB20550B892DB`, worker `F3979D15550C11D399FAAD4964DB905945BF3C8255173658E91BA4733EC4DA60`, tests `A3F196C1ED901DFBD9B6725FAD08E0273D482F6CE8CB5ADCDFB14D035E9EE4BA`. The reviewer independently ran40 focused offline tests and19 additional synthetic checks against identical source hashes; it inspected but did not rerun the new SQL fixture while main owned the database. This is code approval only, not historical cost reconciliation or paid-retry authorization. Exact new CI and hosted diagnostic deployment remain separate gates.
- Public Pages remains the prior main `3912be4` / `index-CviBBF9R.js`; no main merge, production-readiness flag, scheduled spending or further provider request was made. Screened core-life content supply, live acoustic-provider/reference evidence, remaining owner/device recovery journeys and final exact-SHA publication remain open. No Japanese expansion or memory update occurred.

### Cloud provisioning checkpoint — 2026-09-09

- Following the owner's explicit authorization and owner-entered database password, submitted creation of `jove-english-os` in the dedicated personal **FREE** organization `Jove English OS`, Singapore (`ap-southeast-1`). Chrome's authenticated dashboard now shows the exact project `lnkxdwzdcrtlucaezhkd` as **Healthy**, with no GitHub repository connected and no migrations. No password/token was read, generated or copied. No paid plan, existing Cognition backend, repository remote or public deployment was changed. The current default CLI independently returns two projects and does not include this new reference; the guarded inspect therefore refuses to proceed, and no local project link exists. Browser sign-in has not authorized the local deployment tool. Continue only after owner-approved CLI account authorization and exact-project readback, then follow `OPERATIONS.md`. Successful free-project creation is not a platform-policy ruling on account-quota aggregation, functioning sync or final acceptance.
- A separate-profile operator-wrapper prototype passed 11 fully mocked cases under both Windows PowerShell5 and PowerShell7, environment-restoration variants, and the existing20 backend-fence tests; its actual CLI startup with a separate `SUPABASE_HOME` failed. A custom public profile without that home override reached an access-token error, while the home override alone reproduced the startup failure. Therefore the prototype and its instructions were withdrawn, not handed to the owner as working authorization. Independent review also caught the inconsistent bare-command documentation before withdrawal. No real login/link/migration/deploy was attempted through the prototype, no credential storage was read, and the existing backend-operation fence remains unchanged. The owner has now explicitly approved switching this computer's default Supabase CLI login to the new account, understanding that future deployment of old projects requires switching back. This permits the local identity switch, not any mutation of the old cloud backends. Actual interactive owner authorization and exact-Jove CLI readback are still required; passwords, verification codes and login-link output remain owner-only.

#### Current provisioning readback — 2026-09-08T18:37:58Z

This supersedes the pending-CLI/link statements in the two historical provisioning bullets above; it does not supersede the outstanding final-release gates.

- Owner completed the explicitly authorized default CLI account switch. Exact-project inspection passed for `lnkxdwzdcrtlucaezhkd`; this workspace's ignored local link was created and read back as that exact target without observing a password, login code or credential store. Initial inventory confirmed zero public tables and zero buckets. All five source migrations `202609080001`–`202609080005` are now applied and independently queried from the remote migration catalog. All21 public business tables have RLS enabled and no anonymous read/write grants;12 policies match the source owner/membership boundaries. All7 public security-definer functions have an empty search path and deny anonymous execution. Both `jove-recordings` (25MiB per file) and `jove-content-audio` (64MiB per file) are private. These are file limits, not total storage allocations. Hosted Auth users0 and app members0: no owner has been bootstrapped.
- First guarded migrate applied the complete schema but correctly stopped at an unsupported CLI readback format: the installed native CLI emits Markdown backtick-wrapped version cells even with `--output json`. A failed-first regression reproduced the incompatibility. The focused parser fix preserves JSON/bare tables and exact local/remote set equality; three further failed-first regressions now reject malformed extra version rows instead of silently ignoring them. All29 scoped tests, focused ESLint,213-file source/build secret scan and live guarded five-version readback pass. The target/link/environment fences and migrations are unchanged. Independent bounded re-review approved the final parser/tests with no remaining actionable finding, separately rerunning29 tests/lint and five-version failed-first probes, including CRLF/reordering. Reviewer did not reverify the cloud account or credentials; this does not claim a refreshed whole-application CI run.
- Guarded deployment built the unchanged server source associated with `f85da5a7f212566d8f973c9129f8427b64e63807` and deployed exactly `ai`, `content`, `speech-assess`, each read back ACTIVE/version1. Shared bundle SHA256: `5CD3506C5DFD64680067D8A835A9094484E9B024FCBBCAA44A0FE9AE3CF9620D`. The intentional `verify_jwt=false` gateway setting is paired with handler authentication; actual anonymous POST requests from the production origin returned401 on all three routes. No legitimate owner/provider success is inferred from those denials. No provider credentials, paid requests, scheduler or GitHub release variables were activated.
- Chrome opened this project's Authentication/Users view, but the subsequent URL-settings navigation could not be verified and Windows refused a foreground request. No hosted Auth setting, owner identity, SMTP/template or redirect was changed. Resume with the owner bringing Chrome to the foreground, self-confirming the learner email and privately entering any required credentials. Existing Cognition backends, the GitHub account/remote, public V1 and its deployment remain unchanged. Three-device production sync, real OTP delivery, owner isolation journeys, paid-provider quality, authentic content supply and final acceptance remain unproven.

#### Hosted Auth configuration checkpoint — 2026-09-09

- Following the owner's renewed instruction to operate Chrome, guarded inspection again verified the dedicated Jove target. Hosted Site URL was changed from the default localhost address to `https://mnijc19-netizen.github.io/jove-english-os/`; that same exact URL was added to the redirect allowlist, without wildcards. Public signup was disabled; anonymous sign-in/manual linking remained off, and email confirmation/email provider/secure email change remained on. Email OTP expiry changed from3600 to600 seconds and length from8 to6. After an actual browser reload, signup-off/anonymous-off/confirmation-on and the600/6 values were read back; revisiting URL Configuration confirmed the persisted production Site URL and redirect. No local `config push`, cloud-schema/policy change, old-project mutation or public release occurred.
- The current hosted email-template page explicitly keeps the default subject/body until custom SMTP is configured. No template was edited and no SMTP credential was entered or read. Auth rate limits show2 emails/hour in a disabled editing control (the rate limit itself is not disabled); the custom-SMTP toggle remains off and its Save button unchanged/disabled. The60-second resend interval still needs actual project readback during SMTP setup, not an assumption from the documented default. `OPERATIONS.md` now records this dependency. The browser is left at the new project's SMTP settings; the owner was asked only for their mailbox-provider type to identify a legitimate no-new-cost option. No paid service was activated, and no deliverability or free-provider compatibility is claimed.
- Fresh target-pinned SQL confirms hosted Auth users0/app members0. Owner email confirmation/bootstrap, SMTP and token template, actual delivered OTP and three-device owner journeys remain open. Independent bounded operator review approved the reported settings against the runbook, without independent cloud/credential access; it also flagged the release-hold timing prerequisite. Subsequent explicit GitHub readback found zero repository variables (therefore no readiness hold) and zero workflows in each in-progress/queued/waiting/pending/requested state. This is a post-change check, not retroactively claimed pre-change evidence; check the hold and active deployments before the next hosted mutation. No current production acceptance or replacement of the public V1 is implied. Documentation diff/secret-pattern checks passed; no application source changed in this Auth turn.

#### Gmail SMTP preparation — 2026-09-08T18:59:54Z

- Owner self-confirmed a Gmail identity and requested direct completion. Before this attempt, guarded dedicated-target inspection passed, the explicit repository had zero variables/readiness hold, and all16 listed workflow runs were completed. Only an unsaved hosted SMTP form was prepared: the confirmed identity in sender/username, sender name `Jove English`, `smtp.gmail.com`, TLS port587 and60-second minimum per-user interval. All six non-secret draft fields matched on readback. The SMTP password was neither supplied nor inspected; its input was focused for owner entry. No Save action, Auth-user creation, email dispatch, new purchase, server-key operation or release was submitted.
- Current Google primary documentation confirms SMTP/TLS and the app-password prerequisite of two-step verification. The attempt to open Google's app-password setup in a new Chrome tab was rejected by the tool policy; no alternative automation path was attempted around that refusal. Owner must open the official Google page and complete required sign-in/security verification, then enter a dedicated app password directly into this Jove SMTP form. The browser draft is not hosted activation. Do not report the60-second draft value as saved or the Gmail transport as working until Save, settings readback and actual mail delivery succeed. Template, single-owner bootstrap and production sync gates remain open; no email address or credential is retained in this checkpoint.

#### Saved SMTP and owner bootstrap — 2026-09-08T19:47:47Z

This supersedes the unsaved SMTP and zero-owner statements above. Actual email receipt, signed-in browser persistence and hosted sync remain pending.

- Before hosted mutations, dedicated-target inspection passed; the exact GitHub repository had no readiness variable and zero in-progress, queued, waiting, pending or requested workflow runs. After the owner entered the app password, custom Gmail SMTP was saved. Read-only CLI config comparison confirmed the intended non-secret sender/username, `smtp.gmail.com`, port587 and sender name; browser reload confirmed the60-second interval. No password value or raw remote config was read or logged. A masked password field or still-enabled Save button was not used as a persistence verdict.
- Saved subject `Your Jove English sign-in code` and the exact source-controlled `supabase/templates/otp.html` body. A partially replaced unsaved editor draft was detected and corrected before Save. Actual reload confirmed the complete body and subject; safe CLI comparison also confirmed the subject matches local configuration.
- Trusted hosted Auth now contains one confirmed intended owner. The attempted automated bootstrap-password assignment stopped at its form precondition before generating or assigning a password; account creation was observed afterward, not attributed to that failed attempt. No SQL insert into Auth users occurred. A guarded transaction inserted only the exact confirmed owner's membership, refusing unexpected users or other members. Fresh post-commit counts: users1, confirmed intended owner1, members1, intended-owner memberships1, other members0. No RLS or other project's backend changed.
- A localhost-only candidate at `http://127.0.0.1:4199/jove-english-os/#/settings` uses the public browser key in its process environment, with no key file or secret-key reveal. In a new isolated headed Chromium context, the actual app's single OTP request returned HTTP200 and exposed the code-entry field. This proves request acceptance, not inbox delivery, successful sign-in, session persistence or data synchronization. The owner must personally enter the delivered code and continue; browser/server are left open. No OTP, session token, storage state or credential was captured.
- Independent bounded review approved the described SMTP/template/member operation against the local runbook; it did not independently access the cloud. The main operator obtained the remote readbacks above. Application source, public V1, repository identity and other backends are unchanged; no paid-provider call, new subscription, scheduler activation, release variable, push or Pages publication occurred. Final upgrade remains NOT accepted.

#### Actual owner sign-in and initial hosted sync — 2026-09-08T19:56Z

- Owner reported successful sign-in after personally entering the emailed OTP. Read-only inspection of the exact localhost candidate's isolated Chromium window independently confirmed the intended signed-in identity, `Synced`, a last-sync time and absence of the code form. Dedicated-target inspection passed again. Count-only hosted readback confirms users1, confirmed signed-in users1, members1 and signed-in members1. No email, OTP, session token or Auth identifier is recorded here.
- Hosted sync currently contains exactly6 operations from1 device, all for built-in materials; recording manifests0 and recording objects0. The actual app's `Sync now` button was invoked successfully; subsequent UI inspection again showed `Synced`, no outstanding-transfer/conflict warning and an advanced sync time. These are real owner-session and initial transport checks, not evidence of migrated personal learning, original-audio transfer, conflict recovery or Android/iPhone acceptance.
- Automatic reload was not achieved: the browser reload control did not support the attempted automation patterns and Windows refused foreground/focus. No keyboard input was sent to an unverified window. The owner was asked to press F5 in this test window; cold-reload persistence remains an explicit open gate until observed. No session was copied to another profile and no additional OTP was requested.
- No application source, hosted configuration, membership, RLS, other backend or public deployment changed during this check. Public V1 remains unchanged. The existing SMTP/bootstrap review does not independently certify these new browser observations; final upgrade remains NOT accepted.

#### Owner-assisted refresh and second-device setup — 2026-09-08T20:01:56Z

- Owner reported remaining signed in after the requested F5. Independent UI inspection initially confirmed the intended identity and advancing last-sync time; a later view showed `Synced` after the owner navigated to Learn. This is an owner-assisted normal-refresh observation, not an instrumented cold-browser restart. The first window and ongoing lesson were left untouched.
- Fresh count-only hosted query now finds31 sync operations from1 device:6 materials,10 events,12 sessions,2 plans and1 conversation. Users/confirmed signed-in users/members/signed-in members remain1 each; recording manifests and private recording objects remain0. No response text, identity value or recording was retrieved, and no synthetic learning evidence was inserted. The earlier6-material-only inventory is historical.
- Opened a separate fresh Chromium context at the same localhost candidate without copying sessions, browser profiles or storage state. Exactly one further OTP request for this second context returned HTTP200; its code field is visible and it is not yet signed in. Owner must personally enter the latest code. Cross-context download, conflict/reconnect, original audio and physical mobile-device behavior remain unverified; the new context is retained for controlled follow-up. No additional account, paid service, hosted setting or public release was created.

#### Hosted two-context metadata and reconnect verification — 2026-09-08T20:08:59Z

This supersedes the second-context login/download pending state above. The frontend is still the unchanged localhost development candidate, not a public release or physical mobile-device acceptance.

- Submitted the supplied one-time code only in the intended fresh second context. Actual Auth verification returned HTTP200; the app displayed the signed-in account and `Synced`. No session/profile was copied from the first browser. The second IndexedDB contained6 materials,10 events,11 sessions,1 plan and1 conversation, with0 audio records. Material/session/plan operation counts include revisions and must not be confused with projected record counts.
- Through the normal authenticated browser client, fetched the server operation prefix through cursor37 and compared every envelope, receipt and full payload with the second context's stored operations, entirely inside the browser. Result:37 local/37 remote,0 mismatches,0 pending,0 deferred and0 conflicts. Only counts/booleans were returned, not learning text, identity values or authentication credentials. An actual automated page reload changed `performance.timeOrigin`; the account remained signed in and all37 operations still matched afterward. This is a document reload, not a full browser restart.
- Temporarily disconnected only the second browser context and changed Appearance from `system` to `dark` through the actual Settings control. Native IndexedDB retained the changed setting while offline; the first window still showed Follow device. After reconnect, cursor38 matched the cloud with no pending/deferred/conflict records. The first window then independently showed Dark, proving second-to-first delivery. No first-window manual-sync completion is claimed: that attempted button action was refused while unavailable; its normal automatic sync delivered the setting.
- Restored the original `system` preference through the second window and verified the persisted local and hosted setting, then independently read Follow device in the first window. Final second-context comparison through cursor39:39 local/39 remote,0 mismatches/pending/deferred/conflicts. Separate count-only hosted readback confirms39 operations from2 devices, one confirmed signed-in owner/member and0 recording manifests/objects. Projected learning counts remained10 events,11 sessions,1 plan and1 conversation throughout this preference test; no fabricated learning result, response or score was inserted. Original-audio transfer, deliberate conflicts/recovery and physical Android/iPhone remain separate open gates.
- All application source, Auth/RLS configuration, other projects and public Pages remain unchanged. No new account, subscription or paid-provider call was made. Documentation-only diff and213-file source/build secret-pattern checks passed; existing independent source review is not claimed as independent observation of this live browser exercise. Final upgrade remains NOT accepted.

#### Actual hosted content intake and private-Storage boundary — 2026-09-08T20:26:06Z

- Fresh explicit-target inspection passed. Before configuring routing, the exact repository had no readiness variable and zero in-progress/queued/waiting/pending/requested runs. Name-only secret inventory confirmed OpenRouter, Gemini and Azure Speech credentials absent. Set only the exact public backend URL and an origin allowlist containing the production origin plus the temporary localhost4199 candidate. This preview exception must be removed after candidate service validation and before release approval; no wildcard, provider credential or server key was exposed.
- Real owner calls reached the deployed content handler: initial status had0 sources; one actual refresh claimed/fetched5 sources, discovered106 items, processed2 and saved4 quarantined segments with0 eligible. Fresh independent SQL confirms sources5/items106/transcripts2/segments4/quarantined4/eligible0, whole and clipped audio assets0, provider content/service usage rows0, private Storage objects0 and recording manifests0. Persisted source inventories are VOA31, HPR10, JB Launch24, Open Yap16 and JB Linux25; all five source-policy checks are verified with0 source failures. Publisher-policy verification is not interval/audio clearance, and archived material is not new continuing publication.
- An authenticated lesson-selection call returned0 eligible lessons, preserving the review barrier. Its diagnostic recommendation profile was subsequently replaced through `refreshContentLessons` with the actual learner store and deterministic `planLongitudinal` parameters; no learner preference or ability evidence changed. Actual audio alignment, human/naturalness screening, usage rights, reviewed life-task coverage and continuing nontechnical supply remain explicit next gates. No scheduler extensions or job were activated.
- Account controls initially showed default daily1/monthly30 USD. With paid execution unapproved, both limits were explicitly saved as0 through the actual Settings controls and independently read back as0/0 with unchanged `minimal` retention and0 requests/holds/charges. The first immediate monthly read raced its asynchronous save and still showed30; a fresh completed read and independent SQL confirmed0. These are app-level provider guards, not a hosting invoice guarantee or a functioning paid service.
- Tested only a uniquely named synthetic private-Storage object, never adding it to learning events, `db.audio`, assessments or formal references. Through the legitimate JWT-bound owner client, a32,044-byte one-second PCM WAV uploaded with overwrite disabled, downloaded with matching SHA256 and advanced native muted playback. Its unauthenticated public-object request returned400 rather than audio. The exact test object was removed successfully; subsequent SQL confirms0 Jove Storage objects and learning-event/audio counts were unchanged. This proves owner Storage transport/privacy/decoder behavior, not physical microphone capture, original-recording synchronization or acoustic quality.
- Independent read-only content review ran63 distinct narrow synthetic tests with0 failures. It confirmed missing-provider dispatch/approval fences and noted that ordinary refresh can perform retention on existing stale assets; absent credentials alone do not prove zero Storage mutations. The new backend's empty/final object inventories support this particular run's0 objects. Successful deployed external retrieval now has direct main evidence, but the Deno preflight-only DNS transport still lacks independently proven connection pinning/controlled public-only egress; a bounded follow-up investigates that gap. Source is unchanged and final upgrade remains NOT accepted.

#### Connection-pinning follow-up — 2026-09-09

- Pushed hosted-verification checkpoint `1e8f7833cd48b24b49787c5b91473922a07b2031` passed the complete feature-branch CI [34274877977](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/34274877977); deployment was skipped. It does not cover the newer, uncommitted transport implementation described here.
- Two failed-first tests demonstrated that the existing Deno path did not pin its connection and silently used global fetch when native socket support was absent. The candidate now opens a literal validated public address, checks the connected peer, performs trusted TLS verification for the original hostname and speaks one bounded HTTP/1.1 exchange. Every redirect still repeats URL/DNS checks; no unpinned fallback, custom CA or certificate bypass is introduced. Historical preflight-only evidence remains distinguishable.
- Current candidate passes62 focused transport tests, including fragmented/binary/chunked bodies, size/framing limits, certificate failure, late socket cleanup, cancellation and redirect rebinding denial. Full local unit/integration run passed1357 with46 opt-in skips; focused lint, typecheck and diff checks pass. The server bundle builds successfully. These are deterministic synthetic tests, not proof of native Supabase socket support or hosted operation. Actual user-worker capability and independent source review remain required before deployment; no provider credentials, paid calls, scheduled content, public release or final acceptance are implied.
- Subsequent real local USER-worker probing confirmed a Deno2.1.4 compatibility defect: `startTls` omits the peer's `transport` field, although its IP/port are preserved. The upstream version's `ext/net/02_tls.js` confirms this distinction from `connectTls`. A failed-first regression reproduced the rejection; the fix accepts only missing TLS transport after strict TCP validation and still checks both IP/port pairs, trusted hostname handshake and protocol. Focused tests now63 pass; independent read-only source review included this change and found no actionable P0/P1/core-P2 in scope.
- Actual `supabase-edge-runtime-1.74.3` USER worker verified the TCP literal peer, matching TLS IP/port, the missing TLS metadata field, successful trusted handshake for the original public hostname and a200/198-byte HTTP response through the new main module. A second real hostname also handshook successfully, so it was not accepted as a certificate-mismatch control. A fixed `.invalid` hostname later failed without timeout, but its failure was not attributable to certificate-name validation. A controlled certificate-mismatch rejection, native cancellation cleanup and hosted publisher readback remain unproven. These partial results do not authorize calling connection security fully accepted or deploying this candidate.
- New temporary route registration returned502. Testing through the existing local-only content entrypoint reached the USER worker; an attempted query-suffixed local module import returned500 and the temporary runtime subsequently stopped. Restored the exact content entrypoint/config, deleted probe files/helpers, and restarted only the dedicated local Jove functions server using its existing public-only environment file. All five migration checks, four rolled-back SQL suites, three authenticated handler readiness checks and4 actual local Edge/private-Storage tests passed after recovery. An empty probe directory remains because its removal was refused; no executable probe route remains. Hosted services, owner browsers, other projects and public Pages were not changed.
- Final candidate rerun passed1358 unit/integration tests with46 explicit opt-in skips,63 transport tests included; typecheck, focused lint and215-file source/build secret-pattern check passed. Rebuilt server is1,760,992 bytes, SHA256 `40DAABDBC02F192F2F537AE6C164248DA8E77FFB3FA0549D41EC1036AFAB53A7`; the subsequent actual local Edge4-test rerun passed against that build. Independent source review reaffirmed no actionable finding but did not claim independent native hostname rejection. This candidate remains held from hosted deployment and Pages publication pending the specific live gates above.

#### Real native certificate rejection and cancellation — 2026-09-08T21:19Z

- The actual local Supabase USER worker now passed five fixed-public-destination cases through the unchanged `050baef` `pinnedDenoContentHop`: trusted `badssl.com` returned200 with a nonempty body; `wrong.host.badssl.com` failed specifically in native handshake with a certificate-name error; `self-signed.badssl.com` failed with a certificate-chain error. Both negative controls attempted0 HTTP bytes and closed the TLS socket. The controls use the public testing service's defined bad certificates, not an unrelated CDN hostname or disabled verification.
- Two bounded cancellation cases returned `network-timeout-or-cancelled`: abort immediately before native read closed the real TLS socket, and delayed delivery of an already-established TCP socket after abort closed that late socket without any HTTP write. All five cases had0 tracked live sockets before the fixture's fallback cleanup; subsequent actual writes to each closed TLS socket rejected. This verifies socket ownership/disposal, not interruption of an outstanding operating-system connect call.
- The interrupted turn left the local entrypoint temporarily holding the test bundle. It is now restored exactly to HEAD, as is the unchanged local config; all five migrations/four rolled-back SQL suites and4 actual Edge/private-Storage tests pass again. Independent review inspected the native wrappers and error/cleanup accounting, independently passed63 transport tests, and verified both restored files byte-for-byte. The live outcomes were main-observed, not independently repeated. No probe was deployed, and no owner learning evidence, cloud state or other project changed during these tests.

#### Hosted pinned-transport deployment — 2026-09-08T21:28Z

- Rechecked the dedicated healthy target/link, no readiness variable and0 active main/deployment attempts; the sole active run was feature-branch CI34279446875. Its unit/build/security, real Auth/Storage, content persistence and Deno stages were already successful; final browser stages were still running, not reported as a complete CI pass. Source and function entrypoints matched committed `050baefcb6c9b0f19bb05019d796a2f0bdd821bd`; only this status document was dirty. Guarded deployment rebuilt the same1,760,992-byte server/SHA `40DAABDBC02F192F2F537AE6C164248DA8E77FFB3FA0549D41EC1036AFAB53A7` and updated only this project's three functions. Fresh readback reports all ACTIVE/version3. Each real anonymous production-origin request returned401 `SIGN_IN` with the exact CORS origin. No temporary test endpoint was deployed.
- A legitimate owner refresh on the new hosted runtime claimed5 sources, processed2 more items and saved4 quarantined segments. Feeds were not due, so feedsFetched0/feedsUnchanged0 is not a new RSS-poll success claim. Actual publisher-transcript processing completed without a network constraint/error; missing transcripts and audio-analyzer credentials remain explicit gates. Independent count-only SQL now confirms5 sources/106 items/4 transcripts/8 quarantined segments/0 eligible,0 original/clipped audio assets,0 Storage objects,0 content/service usage,1 member, no cron/net extensions and budgets0/0 with `minimal` retention. Exact normal-owner sync-prefix comparison still matches39/39 operations with0 mismatches/pending/deferred/conflicts.
- Name-only inventory freshly confirms no OpenRouter/Gemini/Azure credentials or scheduled job token. No paid service request, acoustic score, authentic-course approval or scheduler was activated. The temporary localhost4199 CORS entry remains solely for candidate acceptance and must be removed before release approval. The new Supabase connector returned no permission for this project; the already owner-authorized explicit-target CLI was used, not another project's connector/backend. Public Pages and the complete final-release gate remain unchanged.
- Fresh hosted security advisors returned4 WARNs, not a clean/no-warning report: three intentionally authenticated SECURITY DEFINER RPCs and disabled leaked-password protection. Read-only normalized-body fingerprints for `append_sync_operations`, `content_lesson_available` and `reconcile_recording_retention` exactly match the reviewed migrations. All three have empty search paths, deny anonymous EXECUTE and allow authenticated execution intentionally; sync/retention derive the caller from `auth.uid()` and check membership, while lesson availability returns only a boolean for that caller's active, currently eligible recommendation. No grants, functions or RLS were changed merely to suppress the advisor. Official password-security documentation places breached-password detection on Pro and above; email OTP remains the normal login path, and no paid upgrade or password change was made. The warning remains disclosed, not fixed by an OTP claim.
- Bounded independent source research (6queries/10target pages) found no newly cleared continuing everyday-life source: Judge John Hodgman fits daily situations but episode adaptation/transcription rights were not established; ELLLO's current FAQ permits specified classroom/LMS audio uses without establishing this automated pipeline's permission; Lingthusiasm has an episode-specific CC BY-NC-SA lead and fresh releases but a conflicting all-rights-reserved RSS channel label and advanced linguistics content. These remain research leads, not enabled sources, approved human lessons or a solution to17 core-scenario coverage. Main independently re-read ELLLO's current FAQ; the Lingthusiasm page was unavailable to main, so its lead is explicitly reviewer-reported pending direct policy verification.
- Exact-source CI [34279446875](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/34279446875) is now terminal **success** for `050baefcb6c9b0f19bb05019d796a2f0bdd821bd`:1358 unit/integration passes/47 Linux opt-in skips,9 real local Auth/Storage passes,96 content passes/7 skips,4 real Deno/private-reference passes,2 Chromium/WebKit multi-profile journeys,156 page E2E passes and0 dependency vulnerabilities. Main-only readiness/publication and deploy were correctly skipped. This does not turn8 quarantined segments into approved lessons, establish a paid provider, or certify the unchanged public V1 as final delivery.

#### Direct/BYOK privacy follow-up — 2026-09-10

- Main full local suite on this patch passes1,360 tests with46 explicit opt-in skips (38 files:34 passed/4 skipped). The actual-store60-day learning-continuity case passed in32.721s; this is deterministic persistence evidence, not measured human learning efficacy. No skip or timeout was weakened.

- Independent baseline review identified a direct/BYOK-only privacy gap: the hosted `quoteTextDispatch` already applied `data_collection: deny`. The shared provider now applies the same filter before its trusted dispatch hook for every chat-completions attempt, retaining strict parameters and server price ceilings through ordinary/streaming chat, schema downgrade, model fallback, retries and discovery. Dedicated STT/TTS request formats are unchanged. Unavailable configured routes cannot cause privacy relaxation. This is a routing constraint, not proof of zero retention or a search/audio-provider privacy audit.
- Eight regression checks failed before the fix. The final focused provider/server/cloud-provider run passes173 cases, with4 opt-in cases explicitly skipped; main scoped ESLint/typecheck, function build (1,761,165 bytes), complete frontend/PWA build and215-file source/build secret-pattern scan pass. An independent reviewer approved the current source/test/operator patch with no actionable P0/P1/core-P2 finding, independently rerunning the same173 passes/4 skips and checking unchanged file hashes and the actual server pricing hook. Full CI [34374498175](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/34374498175) subsequently passed at exact `810e7b7b3b51655dbfce3211ab77c7309341f217`, including actual local Auth/Storage/content/Deno, Chromium/WebKit multi-profile journeys and156 page tests (8.0min). Main-only release/readiness/artifact/deployment steps were skipped; no production completion is inferred.
- Baseline CI [34315981541](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/34315981541) is terminal success at exact `37edb77724a209e83e1836acf77f633172d8f1d6`, including156 page tests. Fresh public HTML still returns200 with `index-CviBBF9R.js`; `main` remains `3912be41f43e4fc5c1473edb590604715a865316`. No public upgrade or hosted-function redeployment of this privacy patch occurred.
- The 2026-09-09 target-verified, name-only inventory still found no OpenRouter/Gemini/Speech credentials or content-job token; it is not a 2026-09-10 credential readback. Official free-tier, privacy and eligibility constraints are recorded in `OPERATIONS.md`. Provider/account/budget activation, reviewed human references/core coverage, actual scheduled supply, remaining owner/device journeys and final exact-SHA public acceptance remain required. No subscription, paid call, scheduler or other project's backend was activated.

#### Hosted code alignment — 2026-09-10

- Before mutation, exact-target inspection passed; repository release variables were empty and the latest five main-branch workflows were terminal. Only the current feature-branch CI was running, with its backend and Chromium/WebKit multi-profile gates already passed. The guarded wrapper deployed exactly `ai`, `content` and `speech-assess` from committed `810e7b7b3b51655dbfce3211ab77c7309341f217`; all three read back ACTIVE/version4. Bundle SHA256 is `39A297C66DB14926CD413F6EE5B3A3A535A92EDFA1DE4C2A212070728491BF29`. Actual anonymous production-origin POSTs returned401/SIGN_IN and the exact production CORS origin on all three routes. Intentional gateway `verify_jwt=false` is unchanged; handlers still require authenticated membership. These denials prove deployment/access protection, not real provider success.
- Fresh 2026-09-10 target-verified name-only inspection again found OpenRouter/Gemini/Azure Speech credentials and the content-job token absent. No owner session was recreated, OTP reused, credentials read, budget changed or paid service/scheduler activated. Public V1, Auth/RLS, all five migrations and other backends were left unchanged. Temporary localhost4199 CORS remains for future authorized candidate verification and must be removed before release approval. The full-goal blocker is legitimate provider/account/budget activation plus the subsequent real content/acoustic/owner journeys, not a lack of green local tests.

#### Test-tool security maintenance — 2026-09-10

- A fresh audit after CI34374498175 reported two moderate package entries for the same [Vitest redirect-mock path-traversal advisory](https://github.com/advisories/GHSA-82fw-gwwq-j7x9). That CI's high-severity threshold passed; it did not mean zero vulnerabilities. This is a development-server/mocker exposure, not evidence that the public website leaked files. Official remediation starts at4.1.11; the3.x line has no planned backport. Vitest is now pinned to4.1.11 with its lockfile, retaining Vite7.3.6. Main and independent checks found all52 production package records unchanged, with no new production records.
- The migration narrows callable test mocks to explicit signatures and restores URL spies after component unmount. Independent review first reproduced the leaked-spy P2 with2 passes/1 failure; the unchanged exact-count assertion then passed with3 passes after teardown repair. The final independent provider/server/cloud-provider run passed173 tests/4 explicit opt-in skips and approved the patch with no remaining actionable finding. No runtime/UI source, timeout, assertion or skip gate was weakened.
- Main typecheck, scoped ESLint and diff checks pass. The complete local Vitest4.1.11 suite passes1,360 tests/46 explicit opt-in skips (34 passed/4 skipped files,46.39s); a fresh audit reports zero known vulnerabilities. Frontend/PWA and server builds pass, as does the215-file source/build secret-pattern scan. The1,761,165-byte server bundle retains exact SHA256 `39A297C66DB14926CD413F6EE5B3A3A535A92EDFA1DE4C2A212070728491BF29`, already deployed above; the new local frontend index is `5A1CAAA7FF4766C8877A9AC53C6677DA3343108EF8989AE95A7338693DCB014C`. The exact-commit replacement CI remains a separate gate; earlier CI results do not certify this toolchain migration. No hosted redeployment, public release or paid activation was made for this maintenance.
- The replacement [CI34407098582](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/34407098582) subsequently completed successfully at exact `5439fadd57c79c1407bf3a8d09e718c40180b1b8`:1,360 unit/integration passes/47 Linux opt-in skips,9 real local Auth/Storage passes,96 content passes/7 skips,4 actual Deno/private-reference passes,2 Chromium/WebKit multi-profile passes, and156 page passes/3 inapplicable skips (8.2min). The source/build scan and zero-vulnerability audit passed; the two existing multi-component test-host lint warnings remain disclosed. Main-only readiness/publication/deploy were skipped. This closes the test-tool migration gate, not the final production goal.
- After rereading the full47-section owner instruction, the final requirement audit remains incomplete: no actual provider-supported acoustic result, approved continuing/core human input, live scheduled supply or exact-upgrade public owner journey is certified. Fresh target-guarded name-only inspection around05:30 Shanghai again found OpenRouter/Gemini/Azure Speech credentials and the content-job token absent. The public site still returns200 with `index-CviBBF9R.js`, public main is still `3912be41f43e4fc5c1473edb590604715a865316`, and release variables remain empty. The indispensable next external step is legitimate provider account/credential authorization and an explicit no-charge/approved-budget boundary; no new account, paid commitment, token reuse or privacy relaxation is inferred from the autonomous technical mandate.

#### Owner-saved OpenRouter credential — 2026-09-10

- After the owner reported Save in the dedicated Edge Function Secrets page, fresh target-guarded name-only inventory confirms `OPENROUTER_API_KEY` now exists. Its value was not read or printed; this supersedes the earlier missing-OpenRouter snapshots, but does not prove validity, credit, model/audio entitlement or actual inference success. Azure Speech credentials and the content-job token remain absent. No paid call, budget, Auth/RLS, schedule or public deployment was changed.
- The existing authenticated account-status path checks OpenRouter's `GET /api/v1/key`, not a model generation. Candidate CORS returned204 for the already-authorized localhost4199 origin. A local upgrade settings server now uses only the project publishable client configuration in process memory; actual HTTP200 and a fresh isolated browser prove the learning-account form and `Check account connection` button render, require login and send zero OpenRouter requests on load. The owner must use their real learning-account session for the connection check; no OTP/session was copied or fabricated. A saved secret and a visible button are not a verified connection or final production acceptance.

#### Actual owner OpenRouter smoke — 2026-09-10

- This supersedes the missing live OpenRouter evidence above for **one fixed synthetic chat/TTS/STT smoke only**. The owner reported a successful account connection check and authorized the first round up to USD0.10; their reported provider-side USD3 key limit was additional protection, not an instruction to spend USD3. Current public catalogs confirmed the configured Gemini3.8 Flash, MAI-Voice-2/Harper and Nova3 models and price units before dispatch.
- A temporary same-origin local diagnostic used the existing owner's real authenticated browser and IndexedDB owner binding, the actual `CloudProvider`, and hosted owner/RLS/budget handlers. No session, OTP or provider key was copied into another browser or exported. Independent review required revision-specific command routing and reconciliation of logical calls plus attempt receipts; old routes returned403. Five isolated browser safety cases passed (success, failure cleanup, nonzero baseline refusal, reload refusal, competing-call stop), plus a real fresh-browser no-login refusal and collector403/400/413 checks. These mocks are harness safety evidence only, not the real provider result below.
- Fresh revision-r4 owner preflight read back0/0 service caps and zero requests, reported charges, holds and unknowns for all periods. The single marked run temporarily used fixed USD0.04 daily/monthly caps, sequential fixed inputs, no helper retries, no learner-event writes and no budget replenishment. Existing hosted adapter retries remain independently reserved; a helper call is not a promise of one upstream attempt. The returned receipts in this run show three actual attempts, with no fallback required.
- Actual hosted results: chat completed in7.535s with a brief context-appropriate grocery-direction reply (Gemini3.8 Flash,489 tokens, reported USD0.00136575); TTS completed and decoded in6.071s (MAI-Voice-2/Harper,78,720-byte MP3,3.936s, SHA256 `1f5d150432239e5da8ea988dca2aa5ab65dd2e4e94e624a40bb63ca84e0ceb97`); Nova3 transcription completed in4.658s and matched the fixed original words after punctuation/case normalization, reported USD0.00028208. This is a generated-audio transport/recognition roundtrip, not an owner microphone test, human-listening input, perceptual voice review, pronunciation/prosody assessment or learning-gain measurement. Strong-model structured evaluation was not exercised.
- Fresh hosted summary reconciles six operational rows (three logical identities plus three attempts), **USD0.00164783 reported + USD0.00274240 retained TTS hold**, one unknown invoice. Their USD0.00439023 sum is a conservative accounted amount, **not a final provider bill**. Successful binary TTS did not supply an actual charge; it was not recorded as free. Cleanup freshly read back both service caps0/0, minimal recording retention and enabled prosody unchanged. The local command gate was disabled after the result. No subscription, top-up, other backend, Auth/RLS, release variable, main merge or Pages publication was changed. Operational receipts exist; synthetic tests did not create learning evidence.
- The temporary public-directory diagnostic entry was backed up under ignored `.work` and removed before any build/publication; the sanitized preflight/result artifacts remain ignored locally. The existing learning preview remains available. Live acoustic assessment, voice/content quality review, continuing eligible human content, deeper feedback/recovery and final public/device journeys remain required. Final upgrade is **NOT accepted**.

#### Content budget before-media guard — candidate, 2026-09-10

- Real provider activation exposed a cost-ordering defect: content could fetch/store an entire episode before discovering a known-denied AI allowance. The candidate adds an owner-pinned, cancellable read-only preflight before episode download/private cache reads. Missing preferences do not create paid allowance; reported and unknown/held costs use the existing UTC arithmetic. The original atomic reservation still guards dispatch, including a competing call after a successful preflight. Metadata/transcripts and retained original bytes are preserved; no framework/UI, provider cap, hosted schema/function, scheduler or public deployment changed.
- New migration `20260910115020_content_budget_preflight.sql` is additive and must precede its function bundle. SQL fixtures cover exact day/month boundaries, zero budgets, missing preferences, invalid estimates, other-owner/prior-month isolation and actual service-role/anonymous/authenticated permissions. They were **not yet executed** at this checkpoint: dedicated local startup ended with SIGTERM/exit1 at the verifier's startup timeout. No migration/reset was performed as a workaround and no other project's Docker/backend was altered. Existing branch CI will verify the migration in its isolated database; publication remains held for actual SQL results and independent review.
- Before the additional cache regression, full local units passed **1369 /46 opt-in skips**, the four focused suites passed **246 /22 opt-in skips**, and typecheck/scoped lint/frontend+function builds/216-file security scan passed. The unchanged frontend artifact is `index-q0unWLog.js`, SHA256 `5A1CAAA7FF4766C8877A9AC53C6677DA3343108EF8989AE95A7338693DCB014C`. An added retained-cache regression now confirms zero private reads/downloads/reservations and byte-hash preservation under a paused budget; the selected budget suite passes **20 /93 name-filtered or opt-in skips**. These are synthetic regression evidence, not paid content qualification. Current complete candidate/CI and independent review results remain separate gates.
- The candidate is committed/pushed as `356b1c793280dadf3096894ff123d094cb0bf055`; [CI34474714432](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/34474714432) is running, not yet passed. After Docker freshly returned a valid server version, one guarded retry successfully applied/read back all **6 local migrations** and passed **all four real rollback SQL suites**, including preflight numeric/permission assertions. No Docker reset or other backend was used. The final local candidate also passed **1370 unit/integration tests /46 opt-in skips** in38.95s, **247 focused tests /22 opt-in skips**, typecheck/scoped lint and the216-file scan. This supersedes the missing-local-SQL gate above, not the earlier genuine startup failure. The migration/function bundle is still **not hosted**, and independent review/full CI remain open.
- Independent read-only review of the exact six source/migration/test hashes found no actionable P0/P1/core-P2 defect. Its **16 independent network-blocked probes** covered fresh/cached denial, separate STT/screening ceilings, cancellation/late success, RPC errors, reserve denial/replay, invalid estimates and owner mismatch. The reviewer did not independently execute SQL and approved only this fix. Main's actual local content-persistence suite subsequently passed **106 /7 opt-in skips** in25.25s. Full CI/hosted activation remain separate gates; no test approval is a qualified human lesson or pronunciation result.
- On the September11 continuation, exact-commit CI34474714432 is terminal **success**: **1370 units /47 Linux opt-in skips**,9 real local Auth/Storage,106 content /7 skips,4 Deno/private-reference,2 Chromium/WebKit account journeys and **156 browser passes /3 inapplicable skips**, with the strict-flaky gate and zero-vulnerability audit. Main-only deployment was skipped. The dedicated hosted target was revalidated; additive migration readback now matches all **6 versions**. Actual hosted service-role execution denies this owner's media preflight while paused; direct ACL/catalog checks confirm anonymous/browser denial, server execution, security-invoker mode and preserved RLS. Usage remains6 rows and both budgets remain0/0. The reviewed1,762,333-byte function bundle (SHA256 `70E3AC9DD15F95148B91C8CCA7CCB7CD119526F40E944515D730F1B089C96453`) was then deployed through the guarded dedicated-target entry point. Function-version/HTTP readback follows separately; no paid dispatch, scheduler activation, Auth change, main merge or Pages publication is implied.
- Hosted readback confirms `ai`, `content` and `speech-assess` are **ACTIVE version6**. All three actual anonymous POST probes return handler `401/SIGN_IN` with the correct production-origin CORS header. `verify_jwt:false` remains intentional because these handlers validate the owner themselves; the anonymous probes do not substitute for owner journeys. Current task-credential inventory shows no `JOVE_CONTENT_JOB_TOKEN`, no named Jove Vault job secret and no scheduling extensions. A bounded, ignored operator helper is under independent review to bootstrap only that internal job credential; it has not run against production. Eight local safety tests and an actual installed-CLI read-only SQL query through an ephemeral Windows memory pipe passed without a plaintext payload file. This is credential-transport preparation, not automatic content supply. Azure Speech remains unconfigured and the full final upgrade remains **NOT accepted**.

#### Content job activation and long-lived queue recovery — 2026-09-11

- Exact-commit documentation CI34506699650 (`d1162c6f0608d12bb817383c57bc36bc865c500d`) is terminal success. The earlier observation timeout did not restart or invalidate that job. The final upgrade is still unpublished and NOT accepted.
- The internal content-job credential was generated in this dedicated project's encrypted Vault and transferred to its Edge secret through an ephemeral Windows pipe. Independent review first identified overly broad pipe access, incomplete configuration parsing and unrestricted diagnostic codes; these were corrected with protected owner-only/native-client-PID pipe checks, an exact inspected configuration hash and explicit output allowlists. All15 local safety tests and independent review passed before activation. Actual job authentication succeeded; budgets remained0/0, usage stayed6 rows, no plaintext credential file was created, and no scheduler/provider call was activated by bootstrap.
- A single actual zero-budget content refresh completed on September11 at05:35:43Z:5 sources claimed,3 feeds fetched,4 new items discovered,2 segment records saved and **0 eligible clips**. Paid AI usage and private content-audio object counts remained unchanged (0 audio objects); before-media budget denial worked in the hosted handler. This proves independent background discovery and the paused-media guard, not qualified listening supply or scheduled operation.
- A separate real local SQL regression reproduced a lifetime queue failure: after1000 checkpoints, an awaiting-analysis item violated its counter constraint even when processing could resume. Forward migration `20260911102838_content_lifetime_attempts.sql` removes only that arbitrary lifetime ceiling, preserving the nonnegative integer counter, ordinary six-attempt retry policy, RLS and existing records. All **7 local migrations/four rollback SQL suites** pass, including1000→1001→1002 recovery, negative rejection and exhausted ordinary retries. Independent review approved the exact migration/test hashes; the217-file source/build secret-pattern scan passed. Hosted activation/readback follows separately; this does not qualify an actual human lesson.
- That reviewed migration is now hosted: all7 versions match; actual catalog readback confirms a validated nonnegative constraint, preserved content RLS and server-only worker execution. No rows were reset, no function redeployment was needed, and budgets remain0/0. Commit `7b5185951623b397928d3337002e4d7f1f7659f0` CI34589810836 subsequently failed a test-only null-injection target collision: its first generic `jsonb_array_length(response)` replacement now hit the new recovery assertion instead of the intended retention assertion. Distinct recovery variables and two additional null-output cases retain both contracts. The corrected actual local content suite passes **108 /7 opt-in skips** in35.69s, plus all four rollback SQL suites/scoped lint/secret-pattern scan. The failed CI is not relabeled as passing; new exact-commit CI remains required.
- The first paid-content operator invocation stopped before creating its one-shot receipt, changing allowance or dispatching the content handler: a blanket unresolved-usage precondition rejected the earlier r4 TTS row. Fresh hosted readback confirms6 original service rows, no content service row and paused budgets. That original TTS invoice remains explicitly uncertain with `$0.00274240` held; it was not cleared or labeled free. A narrowed first-run guard now recognizes only this exact prior non-content hold and still accounts for it under the temporary `$0.75` daily/monthly cap. Eight local safety tests pass; independent re-review and the first actual paid content dispatch follow separately.
- On the September12 continuation, independent review approved that exact baseline delta and the isolated SQL assertions (8 pure operator tests/10 independent simulations). Commit `20d70376fef456d4a69120a65e4f7bd108a612bb` was pushed; CI34619705384 was subsequently read back as terminal success. One actual hosted refresh ran from September11 16:03:36Z to16:05:23Z with the temporary0.75 allowance and restored0/0 afterward. It claimed5 sources, saved2 segment records, produced0 eligible clips and added **0 service rows /$0 reported /$0 held**. No content-provider call was recorded; the prior TTS hold remains unchanged. No scheduler or new Pages release was enabled.
- Actual persisted failure reasons expose real activation gaps: VOA's Plan B candidate returned `invalid-audio-response`, HPR `url-not-allowlisted`, both continuing Jupiter shows `response-too-large`, and Open Yap `audio-save-failed`. A59,291,565-byte Open Yap asset reservation remains preparing with0 Storage objects; the bucket's64MiB setting does not override the [hosted Free global50MB limit](https://supabase.com/docs/guides/storage/uploads/file-limits). Do not raise limits, upgrade billing, delete evidence or relabel these failures as qualified lessons. Bounded large-media intake/storage and the changed redirect require implementation/verification.
- The exact public VOA Plan B GET freshly returned200, `audio/mp3`,1,750,039 bytes and an ID3 prefix. The worker wrongly rejected this publisher alias although its existing audit/parser supported it. A failed-first regression reproduced the rejection. The candidate now validates every MP3 frame through the existing strict parser, canonicalizes only this alias to `audio/mpeg`, and preserves original bytes/SHA before Storage/STT; HTML and unsupported media still fail before asset reservation/provider dispatch. Four focused tests pass; an opt-in real exact-URL pinned-fetch/frame/clip probe passes in2.90s; the full actual local content suite passes **112 /8 opt-in skips** in31.52s. Typecheck/scoped lint/function build/secret-pattern checks passed. A first test timeout came from deep comparison of1.6MB typed arrays; native byte-for-byte comparison preserves the assertion under the original timeout. Independent review and hosted activation of this MIME fix remain separate gates. This transport verification is not acoustic approval.

### English continuation — 2026-09-12

- Independent read-only review of the exact MIME fix found no actionable P0/P1/core-P2 and independently passed all four focused offline cases. Main verified matching source SHA256 `6123EC6F51EE9F004C1921C0EC7CDDBD9D95972F1F6C1FB50946C05AA3E8755D` and test SHA256 `04CB6031D95162337A2931952A9B86A4B702D9A561A5314BFE5DF7B79DEF14CE`; the217-file source/build scan and diff whitespace checks pass. A fresh full local-content rerun could not execute its SQL cases because Docker Desktop's Linux engine is stopped (98 passing non-SQL cases,10 environment failures,12 skips); this does not supersede the earlier successful112/8 run or constitute current full acceptance. Restoring the dedicated local runtime and exact-commit CI/deployment remain separate checks.
- Read-only exact-project inspection and guarded database metadata query confirmed paused budgets and private Vault. Public HPR hub GETs for stored episodes actually rotate among the already registered DigitalOcean/mirror hosts and `archive.org`; no unregistered redirect was followed. Investigate deterministic publisher-CDN resolution without broadening the allowlist or changing episode identity. No paid request, cloud data write, schedule or public Pages release occurred in these diagnostics.
- Docker Desktop was started and its dedicated Jove runtime restored without a database reset. All7 migrations/four rollback SQL suites now pass again. The HPR worker now resolves only a validated, same-episode hub enclosure to the already registered DigitalOcean CDN, retaining the original feed enclosure in persisted provenance. Five failed-first resolver cases and a worker integration case pass, including source/page mismatch, extra query parameters, removed-CDN policy and unrelated-publisher preservation. The exact live HPR4725 feed/CDN/Ogg timing probe passed in6.46s; this is media transport/container evidence, not approved lesson quality.
- Current combined pipeline/worker verification passes **232 tests /12 opt-in skips** in31.75s, including actual local SQL. Typecheck, scoped lint, function build (1,763,348 bytes),217-file source/build scan and whitespace checks pass. Independent read-only review found no actionable P0/P1/core-P2, reran6 targeted cases,29 network-policy cases,5 Deno transport cases and the actual HPR probe, and matched all four source/test hashes. MIME commit `b8cd7c3a471f2db20b5c8f8e09a6e98113b86abb` CI34648577522 is still in progress at this checkpoint; exact combined-commit CI and hosted deployment remain separate gates. Large-file intake, qualified continuous human lessons and actual acoustic acceptance remain unfinished; no paid call or public release is claimed.
- The MIME commit's CI34648577522 is now terminal success. HPR commit `aa6832d7c7871f3f59c3de7042e8d54824df8ba1` is pushed with separate CI34649141464. Main's whole unit suite passed **1380 /50 skips**, and four actual local Edge Auth/private-audio cases passed after explicitly starting the dedicated functions server; the temporary server process was stopped afterward. The earlier Edge-readiness failure was a stopped local functions server, not a successful acceptance.
- Further failed-first tests reproduced locally unsupported containers/intervals reaching the provider reservation wrapper and becoming uncertain despite no service dispatch. STT now validates the container and its trusted provider-specific pure preparation before reserving; analysis validates and reuses its actual clip window before reserving. The shared routed-STT preflight rejects oversized inline audio while direct Gemini upload retains its existing allowance. No historical usage is rewritten, saved source/segment work remains, and actual post-dispatch timeout/failure still stays uncertain. All **236 combined pipeline/worker tests /12 skips** pass with real SQL, plus12 narrow cases, typecheck, scoped lint,217-file secret-pattern scan, function build (1,764,007 bytes) and whitespace checks. Independent review approved all five exact file hashes with no actionable P0/P1/core-P2 and independently reran12 focused cases. This does not add a decoder or complete large-media intake; hosted activation remains pending.
- The provider-media preflight is committed as `39f7b67`. The next HPR source change selects the publisher-listed `hpr_rss.php` MP3 feed, retaining the original source identity/rights, older Ogg rules and enclosure provenance. Actual official feed/CDN/MP3 parsing and30–90s frame clipping passed in5.85s (episode4725 is18,628,209 bytes). Six focused HPR tests and **237 combined pipeline/worker cases /12 skips** passed with real SQL; typecheck/scoped lint/build/scan also passed. This lets the existing clipping path handle suitable continuing publisher MP3 episodes, but does not solve oversized Jupiter/Open Yap intake or approve human speech quality. Final independent source/operator review was interrupted by tool quota, then resumed after the reported reset; deployment and the new paid test remain on hold until approval.
- On the later September12 continuation, old process handles were missing rather than treated as still running. The whole unit suite was rerun and passed **1385 /50 skips** in53.90s. Exact HPR-route CI34649141464 is terminal success. Fresh guarded hosted metadata remains paused,6 usage rows,0 content service rows,0 eligible clips,0 content Storage objects and no active source lease. All functions report ACTIVE/version7, but an isolated download of the actual hosted content runtime still matches the earlier pre-fix SHA256 `70E3AC9DD15F95148B91C8CCA7CCB7CD119526F40E944515D730F1B089C96453` (1,762,333 bytes); the version counter is not evidence of new code deployment. No new paid dispatch, source reset, cloud schema write, scheduler or public Pages release occurred. A separate non-overwriting post-fix test receipt and unchanged0.75 cap/baseline/finally-pause guards are prepared locally and awaiting independent approval.

#### Verified audio deployment and one-shot content result — 2026-09-12

- Independent review approved exact `246c1bc8f6d32e94d8695c9616c7cfc4d88615ee` and the separate one-shot operator hashes. CI34682467105 is terminal success, including real Auth/Storage/content/Deno, Chromium/WebKit account journeys, full browser regression and dependency audit. Pages/readiness steps correctly stayed skipped on the feature branch. Fresh local build and217-file source/build scan also pass.
- The guarded dedicated-Jove deployment completed. All three hosted functions are ACTIVE/version8; isolated source downloads verify each entry and shared runtime byte-for-byte against the reviewed local build:1,764,386 bytes, SHA256 `645529D3940DA77F457A08A805593ED064FDDC72030999686BE2B1CF8CC28432`. No schema change, scheduler, other backend or public Pages release was made.
- The approved single post-fix refresh ran08:25:40–08:27:26Z. It claimed5 sources, fetched2 feeds/1 unchanged, discovered10 items and saved2 segment records, but produced **0 eligible clips**. VOA Plan B's1,750,039-byte original asset is now ready in private Storage. One content-STT usage row became uncertain with **$0.50 held and no reported cost**; this proves entry into the reservation/provider-adapter path, not confirmed acceptance by the provider HTTP endpoint. Persisted reasons only say `content-operation-failed`, so the historical upstream failure remains unknown. The script restored budgets0/0, with no automatic retry. Independent readback confirms7 usage rows,1 content row,1 content Storage object,0 active leases and0 eligible clips. Total reported cost remains$0.00164783; total held is$0.50274240 including the prior TTS uncertainty. No hold was cleared or reclassified as free.
- Failed-first local tests reproduced two diagnosability/accounting defects: typed audio-service failures lost their safe code, and valid provider cost/ID metadata was discarded when response schema/timing/coverage validation failed. The new candidate retains only enumerated service codes and validated private receipt metadata, keeps rejected lessons ineligible and operations uncertain, distinguishes explicit zero from absent cost, and retains a validated charge after downstream persistence failure. Unknown/forged receipts and plain errors still retain the hold without raw-message leakage. It does not recover the missing historical invoice or diagnose that historical provider error. All **1401 unit tests /50 skips** and **253 combined content tests /12 skips** with real SQL pass;22 focused service/privacy cases and typecheck pass. Independent re-review is currently unavailable due to tool quota, so this new candidate is **not deployed or accepted**.
- Large-file prefix acquisition remains an implementation task, not a delivered capability. Independent read-only design review identifies explicit coverage/hash/provenance and an acquisition-version-aware forward migration as necessary; merely accepting HTTP206 or storing a truncated file as a complete episode is invalid. Actual range support, decoder timing/playback, core life-task coverage, recurring eligible supply, real acoustic assessment and public owner journeys remain open.
- The first prefix-transport increment is now implemented locally: audio-only byte-zero requests capped at8MiB, explicit complete/prefix byte coverage, exact length/range checks, no conditional/multipart/encoded response acceptance and no full-download fallback. Range survives redirects only through existing source/DNS/socket/TLS checks. Duplicate Content-Range and unsafe native Range/trailer fields are rejected. Failed-first tests reproduced missing coverage/request validation; the new suite plus existing transport/worker/real SQL tests passes **347 /15 opt-in skips**. Three separate actual public Node probes for The Launch, LINUX Unplugged and Open Yap passed in23.67s, each receiving only4096 audio bytes after current rights/feed checks. This proves those current enclosures support bounded retrieval, not native Deno runtime, valid audio timing, long-file storage, screened lessons or future source availability. Typecheck/scoped lint, function build (1,770,042 bytes) and217-file source/build scan pass. This increment is not yet used by the worker, independently re-reviewed or deployed; partial-artifact/frame/migration/playback integration remains next.

#### Interrupted legacy billing checkpoint — current candidate, 2026-09-12

- Independent review blocked the preceding candidate with a reproduced P1: an old `service_usage` hold can survive without its lease-fenced `content_usage` checkpoint, bypassing migration010's per-item-only lookup. In a rollback fixture, the old0.50 hold plus a changed fingerprint authorized a second reservation, totaling1.00; no provider call or real charge occurred. The ordinary current-schema SQL assertions failed first when the new orphan assertion was introduced.
- Forward migration014 adds a service-only, invoker-mode orphan preflight and applies it before intent insertion. Relevant active-owner content service rows require a matching request/fingerprint intent or a same-owner non-null-item content checkpoint; otherwise the owner content boundary fails closed. Ledger status cannot hide an unknown mapping. The existing per-item legacy fence, atomic reservation and all historical holds remain intact. The current lease must also have a non-null unexpired deadline. No existing migration or usage row is rewritten.
- Actual14-migration local SQL checks and3 focused worker cases pass; the real PostgreSQL content/network regression passes **416/18 explicit opt-in skips** in44.17s. Full units pass **1507/57 explicit opt-in skips** in41.12s. Scoped ESLint, Vue typecheck, client/function builds,224-file source/build scan and whitespace checks pass. Client remains`index-DfcXU1Zn.js`; runtime remains1,786,094 bytes/SHA256`422321C6497FE152641F84FC91510F6E9D5D38D5B15914AB681E6121E7447ABC`.
- A disposable-database rehearsal now applies unchanged first7 migrations plus synthetic legacy rows, then008–013; the new SQL suite fails with the exact orphan-replay assertion. Adding014 passes that regression and all4 rollback SQL suites while11 legacy table snapshots, audio defaults and the old test hold remain unchanged. The exact temporary database is removed; shared local membership is0. One concurrent rehearsal attempt correctly refused its initial zero-membership guard while an unrelated local integration fixture was active, before creating any database; the completed rerun followed that fixture's terminal cleanup.
- Independent corrective review reproduced the fixed SQL/worker boundary and verified actual installed function bodies/grants. P1 is closed for migration014 SHA`F5E16A0A083B0BEE6DA173E4EAFB3E69778C96D5F958DFC2060D4D60EB7D5B67`, SQL tests SHA`B4819D2728E3D93E3E196477F401304230E10D9F577BA01FBF297BC9BC37A228` and worker tests SHA`F62656B8A897352D15AA51CF7B58578A9F2D6D4312126056AFCAE523A7BB4D30`. No additional P0/P1/core-P2 findings were identified in the reviewed delta; operational activation and production acceptance are separate.
- Hosted **read-only** metadata confirms the prior7-row baseline,0.5027424 held/0.00164783 reported, paused budgets, no intent schema yet and0 content service rows lacking a same-owner item checkpoint. This does not settle the old charge or authorize its replay. An ignored new one-shot helper is prepared with an inclusive1.25 ceiling (under the owner's3.00 external limit), frozen old-row hashes, its own non-overwriting receipt, exact candidate/runtime/migration checks and finally-pause behavior. Its13 isolated operator tests pass; only its read-only inspect mode ran. Paid execution still needs separate review and correct candidate pinning. No new paid call, hosted migration/runtime change, scheduler or Pages release occurred. Prior docs-only`a3ca458` CI34695777978 is terminal success but does not cover014; the corrective exact-commit CI remains required.

#### Encoded publisher filenames — prior candidate, 2026-09-12

- Reconstructing the earlier cleared local source's first pending candidate from the current25-item LINUX feed reproduced `url-not-allowlisted` at the already registered ART19 CDN. The publisher supplied a signed MP3 basename containing `%20`; no new host was involved. The probe records redacted redirect paths, not reusable signing values. This is a current reconstruction, not an original historical trace.
- Two failed-first tests reproduce rejection in both URL policy and pinned-Deno HTTP transport. The existing exact ART19 rule now permits only encoded spaces within the bounded MP3 basename, leaving the URL encoded. Fixed origin/signature/episode/hash/suffix shape, no-query rule, total URL bound, byte-range limit and per-hop TLS/DNS checks are unchanged. Tests retain rejection of raw spaces, controls, other escapes, encoded slash/backslash/dot, double encoding, traversal, overlong names and foreign hosts. No wildcard rule or unpinned fallback was introduced.
- The same reconstructed episode then completed its actual native pinned redirects and returned206/16,384 bounded bytes. This is transport evidence, not reviewed speech or a lesson. The3 focused URL/transport cases pass; combined pipeline/worker/transport acceptance passes415/18 explicit opt-in skips with real local PostgreSQL in44.33s. Dedicated local readback still has13 migrations/four rollback SQL suites. Full unit regression passes1506/57 explicit opt-in skips in40.72s with two workers, including the unchanged60-day limit.
- Scoped ESLint, Vue typecheck, client/function builds,223-file source/build scan and whitespace checks pass. Client stays`index-DfcXU1Zn.js`; server runtime is1,786,094 bytes, SHA256`422321C6497FE152641F84FC91510F6E9D5D38D5B15914AB681E6121E7447ABC`. Prior focus commit27b1970 has terminal successful CI34693900450 with160 browser passes and0 dependency vulnerabilities. That CI does not cover this filename fix. Exact new CI/independent review/hosted activation remain separate gates; no paid call, hosted mutation, scheduler or Pages release occurred.
- Additional actual PostgreSQL upgrade rehearsal at13:09UTC: a uniquely named disposable database used empty installed Auth/Storage schemas and their actual app-facing/default grants, then the **unaltered first7 migrations**, synthetic legacy records, and all6 unaltered forward migrations. All11 populated legacy table snapshots match before/after (only the4 explicitly new fields excluded and independently checked). Legacy audio/clip defaults, a still-unknown0.50 test hold and denial of an unmapped legacy paid request all pass. The upgraded database also passes the4 rollback SQL suites with its legacy records intact. Initial harness setup failed because an application policy preceded its table and platform grants were missing; the corrected harness recreates that policy via original001 and reads the real local grant metadata rather than weakening assertions. Each disposable database was removed only after exact name/ownership checks; shared local membership remained0. This is synthetic old-schema migration evidence, not hosted migration or actual audio-byte approval. Reproducer: ignored `.work/verify-legacy-prefix-upgrade.mjs` and `.work/legacy-prefix-fixture.sql`.
- Fresh hosted aggregate readback still reports budgets paused,7 usage rows,0 active leases,1 content usage row,0 eligible clips,1 audio object,0.00164783 reported and0.5027424 held; all functions ACTIVE/version8. No hosted state changed. Exact candidate`d65da3919e2a28eb03467445a69a237a76b812fb` CI34694917675 is terminal **success**, including real local Auth/Storage/content/Deno/multi-profile gates and160 browser passes in8.3m; dependency audit reports0 vulnerabilities. Pages readiness/upload/deployment stayed skipped on the feature branch. Independent review was requested after the stated reviewer quota reset at13:09UTC and is still pending; successful CI is not final release approval.

#### Route input focus — prior candidate, 2026-09-12

- Recovered the prior terminal local diagnostic:12 focused Linux WebKit runs passed, but its initial temporary copied-test module configuration failed6 cases with an ESM/CJS loader error; neither result establishes that the intermittent CI issue is fixed. The ordinary14-case Linux WebKit longitudinal suite also passes before the focus change. Exact observer commit`e18e0558f4a559d1d5e1001b760f8192ee7cbf9f` CI34693327163 remains in progress at this checkpoint.
- Found an actual competing focus path in`src/main.ts`: every route scheduled an unconditional title focus80ms later, even after a learner focused an editor. A new actual-browser regression freezes/advances only time and proves the editor loses focus on both Chromium and WebKit before the fix. It does not manually dispatch input events or write editor values; normal browser keyboard input/readback follows the focus assertion. This explains a concrete possible interruption mechanism; it does not establish that every historical WebKit failure has the same cause.
- Replaced the elapsed-time callback with Vue's actual render completion, and refuse focus transfer after another navigation or interaction, in an active editor, or after failed navigation. Cold-start/skip-link focus remains covered. The fixed built client`index-DfcXU1Zn.js` passes all8 focused desktop/mobile Chromium and Windows WebKit focus/keyboard cases in43.0s, with actual typed input and saved-draft readback. Scoped ESLint, Vue typecheck/build,223-file source/build scan and whitespace checks pass. Native Linux full-course regression, exact candidate CI and independent review remain separate gates; no hosted/Pages change or paid call was made.
- Subsequent exact-source full longitudinal regression passes16/16 isolated Linux WebKit desktop/mobile cases in2.5m and16/16 Windows Chromium desktop/mobile cases in31.3s. These include actual DOM/IndexedDB continuity, optional drafts, aborted-save recovery, five-part assessment and begun-assignment preservation. The native container exited without changing local database/storage. Existing strict timeouts/retries remain; full CI, independent review and final production/device acceptance are still required.
- Focus fix is committed/pushed as`27b1970e263503a0901be72f36aa5c7d5a41e4a0`, with exact CI34693900450 in progress. Prior observer-only CI34693327163 is now terminal success with156 browser passes and0 dependency vulnerabilities, but it does not cover the focus fix or erase the intermittent failure in34692005740.
- A separate full native public-content probe reached the real pinned LINUX feed (9,213,876 bytes), publisher VTT (92,683 bytes),8MiB audio prefix, real local private Storage/checkpoint/atomic budget and actual analysis-request serialization (910,471 bytes) under256MB/2s CPU supervision in25.80s wall time. Its in-memory provider deliberately returned400 before any real AI call; no synthetic transcript/approval was applied to public speech. Readback preserved one publisher transcript/one source asset/one uncertain **local fixture** reservation, with0 eligible clips. The harness incorrectly expected a completed item despite that deliberate provider failure, so its original exit1 is retained; offline report inspection verifies the intended stop and the corrected0-item expectation, without another request. An earlier public-item probe stopped at`url-not-allowlisted` before any provider/storage work; the specific blocked hop remains untraced. Both exact local fixtures/containers were cleaned up; no hosted budget, held charge, source, scheduler or public deployment changed. Combined live-provider capacity and sustained source reliability remain unverified.

#### Native media limits and resumable batch rotation — prior candidate, 2026-09-12

- Exact candidate `b5cb68f11c7d2c13cf4871c611e88112c06d8466` CI34692005740 is terminal **failure**: local Auth/Storage/content/Deno and multi-profile sync passed, but full browser regression ended153 passed/3 skipped/2 flaky/1 failed. WebKit's assigned-course journey remained stuck on a disabled chunk-save button even on retry; an optional-reading save also flaked. Earlier green commits do not supersede this failure. Input-event and persisted-state diagnosis is in progress; no timeout, retry or acceptance gate was relaxed.
- Added bounded, text-free input-event/state metadata only to isolated WebKit longitudinal test contexts, plus failure-only GitHub trace/report retention for3 days. No owner browser, Auth integration state, `.work` or environment files are uploaded. Workflow YAML, scoped ESLint and Vue typecheck pass; the two affected journeys pass4/4 desktop Chromium/Windows WebKit in35.4s with the observer. Native Linux repetition is diagnostic work in progress. This is observability, **not a fix or a replacement for the failed exact-head CI**; production code and UI are unchanged.
- The full native worker now has separate local integration evidence under the same256MB/2s CPU supervision: real13-migration PostgreSQL, private Storage and atomic service budgets, with synthetic media and an in-memory provider. Four fresh isolates passed: STT-only830.31ms, two resume batches750.69/489.03ms, and fresh STT+two analyses1176.47ms wall time. Resume made no second STT request. Readback found two transcripts/five ready clips/seven completed zero-cost fixture usage rows/no uncertain rows; the drained first source asset was removed while its three clips still downloaded with exact SHA256. The initial three-phase harness failed because it expected only two segments from a three-segment fixture; the corrected sequence drains existing work before the fresh episode. Both runs and exact fixture cleanup are preserved, not relabeled as provider/human-content acceptance.
- Three independent read-only native Deno source probes passed current rights checking, pinned TLS feed intake, actual8MiB byte-zero acquisition, MPEG preparation and playable clip construction: The Launch7.98s, Open Yap5.31s, LINUX Unplugged22.35s wall time. The latter includes a9,213,876-byte feed. All returned `pinned-deno-tls`; clip hashes match the earlier browser-decoded artifacts. This closes those specific native transport samples, not continuing reliability or combined real-provider capacity. No paid service, hosted data, eligibility record or production deployment changed; the temporary native probe containers and scoped synthetic local fixtures were cleaned up.
- Prior candidate8753cf85cc74d9cca91de11bc65286e2afc7172d has terminal successful CI34691031365: verification passed, including actual local content/Edge/multi-profile sync and156 browser cases, with0 reported dependency vulnerabilities; Pages deployment remained skipped. It does not cover the changes in this checkpoint, and the prior Linux WebKit intermittent failure remains historical evidence.
- Actual isolated Edge Runtime1.74.3 with256MB/2s hard CPU supervision exposed a real prefix endpoint failure before resource measurement. Three8MiB failed-first regressions reproduce sample-count versus summed-frame timing drift. Prefix windows now normalize only the already validated endpoint, retain all original audio frames, use canonical artifact duration and reject requests beyond declared coverage. Legacy complete-acquisition behavior is unchanged.
- With that fix, the synthetic high-bitrate single-item media hot path (STT preparation/serialization plus two120s analysis preparations/serializations) passed in1000.71ms wall time. Five high-bitrate items triggered `WorkerRequestCancelled` at the CPU limit; five low-bitrate items passed in1645.21ms. The failed five-item observation is retained. These are actual supervised runtime observations with a fake provider, not real provider calls, full-worker capacity proof, acoustic approval, CPU-time measurements or physical-device acceptance. No production env/credentials were present; only public runtime Node type metadata was downloaded during bootstrap. The probe container was stopped after its terminal output, with no database/storage cleanup.
- Candidate prefix jobs now default to one expensive audio item across all sources. Forward migration013 records least-recent audio work under actual source lease/revision checks; inventory ordering breaks ties. Repeated same-clock runs advance across sources, and failed sources yield to others. Unstarted work is neither failed nor charged. Twelve targeted acquisition/batch cases pass; the dedicated local backend reads back13 migrations and all four rollback SQL suites pass. Five real PostgreSQL worker cases pass in37.52s. No hosted migration or runtime changed.
- Final full unit regression passes **1504 tests/57 explicit opt-in skips** in52.72s; scoped lint and Vue typecheck pass. Client build remains`index-CSXWepD6.js`. Function build1,785,931 bytes, SHA256`873AB1BB2BA84C7B5BBEFAEAE9D392E48CFF9BA3820A7CBD998137C170F78B43`;223-file source/build scan passes. Independent review is still unavailable until quota recovery, and exact-commit CI/actual full-worker prefix verification remain pending. The final English OS goal remains active; no Japanese expansion, memory write, paid AI call, hosted/Pages deployment or product-completion claim was made.
- The final rebuilt bundle also passed all4 actual dedicated local Edge authorization/private-reference tests in4.41s after13-migration readback and all four rollback SQL suites. This validates the current bundled handler/transport gates, not a real content provider call. Temporary function/probe services were stopped while preserving local database/storage state.

#### Versioned acquisition and safe recovery — prior candidate, 2026-09-12

- Owner's English-only continuation stays in scope: no Japanese expansion, memory writes, framework/UI replacement, paid calls, hosted migration/function deployment or Pages replacement in this increment. Previous candidate `a686857ddcf024525210757b945f8a8fb3bf00bd` has terminal successful CI34689182018; its deployment job was skipped. That green run does not cover the new changes below.
- Candidate worker now requests a bounded byte-zero representation, saves measured MPEG frames and explicit coverage in a separate `prefixes/` namespace before requesting AI, slices only wholly covered transcript cues, and restores the same STT bytes/range without redefining the original episode duration. Small complete non-MPEG responses and existing complete acquisitions retain their original path/hash. Clips identify their source acquisition and retain the previously tested Xing timing version.
- Forward migrations009–012 are applied to the **dedicated local backend only**, with all12 migration versions read back and four rollback SQL suites passing. Existing complete assets survive under the expanded versioned key. Failed-first SQL cases exposed same-digest/different-coverage selection, followed by SQL NULL fallback ordering;011/012 correct both without editing applied history. STT checkpoints cannot rebind their digest or coverage. Actual retention assertions verify exact prefix deletion retry, preservation of the complete asset, and survival of replay/billing evidence.
- Private service-only request intents now bind each item revision and STT/segment operation before the existing atomic spending reservation. Changed adapter/acquisition fingerprints and unmapped historical usage require reconciliation; they do not clear prior holds or silently become new paid requests. An intent alone is not a charge and can survive a budget denial. A failed-first test found a stalled intent lookup outliving cancellation; the fixed bounded await and proper PromiseLike adoption pass the focused10 prefix cases and Vue typecheck. Existing historical uncertain content charges remain unresolved and untouched.
- Five real PostgreSQL worker tests passed, including the new prefix STT/JSONB restore and exact versioned clip parent binding. Their media/provider/Storage adapter are synthetic and do **not** approve human audio. Full final unit regression passed **1499 tests/57 explicit opt-in skips** in47.36s; unchanged60-day/test timeout gates remain. Four actual local Deno/Auth/private-reference cases also passed. The temporary functions service was stopped with exact project/workspace identity verification; database and Storage data were preserved.
- Final pipeline/worker run with real local SQL passed314/18 opt-in skips; the separate transport/cloud-contract run passed125/125. The unchanged built client's completed45-minute/optional-draft/reload/completion journey passed4/4 in desktop/mobile Chromium and Windows WebKit,37.6s. These browser layouts are not physical iPhone/Android acceptance, nor proof of the hosted prefix pipeline. The earlier Linux WebKit intermittent blank-page observation remains recorded rather than erased by this green run.
- Full lint passes with0 errors and the same2 pre-existing reading-test warnings; final scoped lint is clean. Client build remains `index-CSXWepD6.js`. Function build is1,783,982 bytes, SHA256`C3A7F584F4D2F468DF05BF8661E6A6BD27F88CD39222A86C90D7799844A34FD7`;222-file source/build secret-pattern scan passes. This candidate is **not deployed**. Independent review remains unavailable until the tool quota recovers; native runtime prefix limits, actual hosted Storage/provider behavior, reliable continuing core-life lessons, acoustic-provider evidence and physical-device/production acceptance remain required. The old V1/public deployment is not final-upgrade acceptance.

#### Covered-cue selection and CI test cost — prior candidate, 2026-09-12

- The deterministic slicer now accepts trusted measured prefix coverage and filters whole cues before sentence grouping/segment limits, with no time interpolation, transcript mutation or promotion of audio/rights evidence. Twelve failed-first cases now pass, including40/45-second boundaries, unheard punctuation, preserved original cue indices/lesson identity and invalid ranges. The complete pipeline suite passed132/3 opt-in skips; four network/pipeline/worker/client suites with actual local SQL passed428/18 skips. Typecheck/scoped lint, the unchanged client bundle`index-CSXWepD6.js`,1,775,155-byte function build and218-file source/build scan pass. The content worker still does not acquire or restore versioned prefix assets or pass this coverage option in production.
- Candidate9f29ec7 CI34688913379 **failed**,1476 passes/1 failure/57 platform or opt-in skips: the new MPEG2.5 (version0) clip test exceeded its unchanged5s limit. Later SQL/Edge/browser steps therefore did not execute. A focused local three-version run took6.43s in test bodies; changing the remaining large-array generic equality assertion to exact native byte comparison reduced the same bodies to46ms. Byte-for-byte immutability, all timing/TOC checks and the5s timeout remain intact; no retry or assertion was removed. The prior96c6396 green CI is not evidence for this later candidate. Fresh exact-commit CI and independent review remain required.
- The final isolated two-worker regression then passed **1489 tests/56 explicit opt-in skips** in50.13s. Final source runtime SHA256`D652375CFEA279B281C5F719B729C4FBE713ACC9B7A57C35B451A5716E6D0FAB` is **not deployed**. No active diagnostic browser/function container remains; local Auth/database/Storage data was preserved. This is progress on the full upgrade, not completion of the content pipeline or final product acceptance.

#### Versioned playable prefix container — prior candidate, 2026-09-12

- Baseline96c63962 CI34687422821 is terminal **success**:1465 unit tests/56 opt-in skips,9 real local Auth/Storage,160 content/15 opt-in skips,4 actual Deno/reference tests,2 multi-profile Chromium/WebKit journeys and156 browser passes/3 inapplicable skips. The strict-flaky gate stayed enabled and audit found0 vulnerabilities. Pages deployment and production readiness steps were skipped on the feature branch. This supersedes the earlier pending-CI note, not the distinct prior intermittent Linux failure or final production gates.
- The candidate source now builds a new clip-specific Xing frame/seek table and retains encoded speech frames byte-identically. The explicit timing version is shared across server, app schema and additive migration008; the new version requires MPEG MIME, with unchanged old rows/hashes, grants and RLS. The dedicated local backend applied8 migrations and passed all four rollback SQL suites. No hosted migration/function/Pages deployment occurred.
- Actual Linux Chromium/WebKit decoding, duration and play/seek/end checks pass for all three sampled source clips. The Launch and Open Yap were acquired on Linux; LINUX Unplugged's Linux audio fetch timed out, and its decoder result uses a SHA-verified Windows-pinned public artifact mounted read-only. Exact hashes and the failed raw-window observations are recorded in`CONTENT_PIPELINE.md`. None of these checks certifies accent quality, phonemes, iPhone Safari or long-term supply.
- Failed-first tests cover malformed restored coverage, unavailable initial reservoir, client/SQL timing-MIME mismatch and raw-vs-versioned STT submission. Both provider adapters accept an explicit bounded input and reject invalid coverage before dispatch; legacy clip bytes remain unchanged. The production worker does **not** yet acquire/persist/recover prefix artifacts. Acquisition versioning, transcript coverage filtering, replay/accounting and full real pipeline acceptance remain required. No paid provider call, budget change, scheduler, approved human lesson, memory update or Japanese feature was added. Independent re-review is still required; the previously observed reviewer quota resumes at21:09 Shanghai.
- Final local regression passed **1477 tests/56 explicit opt-in skips** in51.44s with two workers, retaining the60-day test's original limit. The earlier targeted SQL-enabled network/pipeline/worker/client run passed412/18 skips before the final reservoir/MIME/VBR additions; the new failed-first reservoir/MIME and both analysis-container cases then passed4/4. Typecheck, full lint (0 errors,2 pre-existing reading-test warnings), client build`index-CSXWepD6.js`,1,774,678-byte function build and218-file source/build scan passed. These checks are not independent review or production acceptance.
- After the first local Edge readiness check found no running function service, a dedicated local service was started and all4 actual Deno/Auth/private-reference tests passed. It was then stopped without removing database/storage data. Runtime SHA256 is`C7B1A4F7A0BF03861D3E8621E725F6FF620CA8B7870A9FAC3E55BFE3C3C9BFC2`. The built client's exact completed45-minute/optional-draft/reload/completion case passed4/4 desktop/mobile Chromium and Windows WebKit in34.0s. An initial incorrect grep selected no tests and is not counted as a test run. No test assertion/timeout/retry was weakened.

#### Prefix frames and current-plan write safety — prior candidate, 2026-09-12

- Recovered clean feature HEAD `34586cb7799153c66655d65ba9c04c37605beab7`, already pushed. CI34685342834 at`d3f8e29` and34685795928 at`34586cb` both reached the full browser suite but failed its strict flaky-test gate: the first lost the completed-plan display on WebKit mobile; the second left the reading Save button disabled on WebKit desktop. Each recorded155 passes/3 skips plus one flaky case, not a successful release. No main merge or Pages deployment occurred.
- Added bounded MPEG prefix frame preparation without enabling partial acquisition in the worker. It retains only consecutive complete frames up to600s, validates explicit byte coverage and bounded ID3 metadata, records source byte/sample coverage and discarded metadata/tail, rejects unavailable initial reservoir/interior corruption, and never calls a frame count an acoustic measurement. Twenty-five parser cases pass after16 failed-first cases; existing complete-container/window assertions remain intact. Combined network/pipeline/worker acceptance passed372 cases/18 opt-in skips with real local SQL before the final two safe prefix-error-code cases. Actual2MiB probes and the WebKit playback limitation/remux experiment are documented in`CONTENT_PIPELINE.md`. No prefix Storage object, paid analysis or approved human lesson was created.
- Three failed-first real Pinia/IndexedDB tests prove that late `beginTask` and `completeTask` writes could overwrite a more recent completed/optional plan or revert another task's completion. Both paths now read/update the current persisted plan under a transaction; starting also computes from one fresh profile/skills/cards/events/material snapshot. Completion updates the addressed current assignment, preserves evidence prerequisites and records the committed task's original minutes. Additional overlapping-completion and failed-commit tests pass:13 app-coordination tests total, with no weakened timeout/assertion. Internal evidence append and store refresh are separated to avoid refreshing the entire history twice per assignment while still refreshing in failure cleanup. No sync protocol, schema, unrelated UI or learning score changed.
- The intermediate start-only build passed28 longitudinal journeys across desktop/mobile Chromium and Windows WebKit; the subsequent start+completion transaction build also passed all28 in3.8m. Windows WebKit is not an iPhone/media acceptance substitute. A separate Linux WebKit repeated run of that build passed11/12, with one blank-document/JavaScript-response failure after clicking Continue optional practice and a teardown timeout; trace and screenshot remain under`.work/plan-native-webkit-results`. This was not the same observed signature as the earlier disabled-Save CI failure. The repeat gate remains open, no timeout was relaxed, and no claim is made that every WebKit flake is repaired.
- The earlier full local unit run passed1459/56 skips before the final completion/cleanup increments. A later run overlapping the Linux browser job passed1462 but hit the existing60s limit in the real60-day learning-loop test; it is retained as a failure, not relabeled. After removing duplicate store refreshes, the final isolated run with two workers passed **1465 tests/56 explicit opt-in skips** in53.41s, including that unchanged60-day/60s regression. Typecheck/scoped lint, client build (`index-Dnupy8Io.js`), function build (1,770,509 bytes) and217-file source/build scan pass. Fresh feature CI and independent re-review remain required; reviewer-tool quota is still unavailable. Hosted functions remain the previously verified version8, budgets were not enabled, no additional paid request was made, and the full final-upgrade goal remains **not accepted**. No memory or Japanese feature was added.
- Final built-client focused completion/reload/optional-retell journey passes4/4 desktop/mobile Chromium and Windows WebKit in35.5s;31 focused frame-parser/safe-service-code tests also pass. These do not close the separate intermittent Linux gate or certify actual iPhone/media behavior. A fresh read-only hosted metadata check confirms budgets paused, the same7 usage rows/1 uncertain content row,0 active leases,1 content object,0 eligible clips, reported$0.00164783 and held$0.50274240, with all three functions still ACTIVE/version8. No test container remains running; failed diagnostic artifacts are preserved locally.

### Latest authoritative checkpoint — 2026-09-09, continued final-upgrade goal

- Integrated source is committed and pushed as `106bca418f5ca58db838feb2b782ec18e972c5d9`. Its clean full Chromium rerun58320 passed **115/115 in4.5min**, without retries, on the exact root build below and the final reviewed browser tests. CI [34250548274](https://github.com/mnijc19-netizen/jove-english-os/actions/runs/34250548274) is terminal **success** for this exact commit, completed 2026-09-08T16:37:42Z: **1286 unit/integration passes /47 opt-in skips**,9 actual local Auth/Storage passes,96 content passes /7 opt-in skips,4 actual Deno/private-reference passes,2 Chromium/WebKit account journeys and **156 browser passes /3 inapplicable skips** (8.3min), with the strict-flaky gate active and zero dependency vulnerabilities. The Linux unit suite registers one additional opt-in browser compared with Windows; its47 skips must not be reported as the local46. Earlier failed fixture runs remain historical evidence, not relabeled. Production readiness/publication steps and deployment were correctly skipped on the feature branch; no main merge or Pages publication has occurred.
- The coherent sync/reading/plan source has passed independent bounded review, full lint (zero errors; two existing multi-component test-host warnings), typecheck/build, **1286 unit/integration tests /46 opt-in skips**, the212-file source/build secret-pattern scan and a fresh dependency audit with zero vulnerabilities. The original60-day actual-store case passed in28.177s during the full37.73s suite; no timeout or learning/evidence assertion was weakened. The current root build index SHA256 is `D4C52FB249C796618D601D5972D9CE86EF36FB8C431D98BFF1253F3162BC6DE9`; earlier snapshot artifacts below have distinct build hashes and are not this exact artifact.
- New actual local two-profile account journeys pass in **Chromium (77127,100.97s test)** and **native Linux WebKit (87235,2.0min total)**. They retain the original reading response/retell/completion events byte-for-value across offline conflict, preserve the other device's response-only draft, recover it explicitly without invented task/skill/assessment credit, and retain the original9-minute assignment within the45-minute required budget. Existing OTP/migration/review/audio/reset/restore/account-isolation assertions also pass. Main independently read back members0 and disposable browser test-users0 afterward. Journey test SHA256 is `3B57FEC2...`; these are local real-service tests, not production or physical-iPhone acceptance.
- The earlier reading-navigation failures were identified as a dev-test preparation issue: one reloaded browser had never loaded the Settings module before its network partition. Safe DOM/request diagnostics showed the intact reading page and exactly that failed module request. Both devices now visit the actual Settings route while online before the offline journey; offline navigation/save guards and every evidence assertion remain intact. Built-PWA offline precaching is tested separately, not inferred from this warm dev-module test.
- On exact root build `D4C52FB2...`, the full native WebKit desktop/mobile matrix95761 passed **41 /3 inapplicable skips**, without retries; representative Today/Speak/Learn screenshots were inspected. Chromium matrix34590 ended **113 passed /2 failed**: both failures occur before entering a newly raw-IDB-injected reading fixture because same-document routing leaves Pinia's material list stale. One independently reviewed test-only startup reload now hydrates that fixture before any learner edit or recovery; the corrected case passed all four Chromium/WebKit desktop/mobile variants (Chromium5.4s, native runner79342 22.6s total). The separate optional9-minute test also passes all four variants after real Start/persistence/navigation barriers and exact full success-message matching. No assertion, timeout or retry was relaxed. Final browser-test SHA256 is `2376695A984CCA6E72E33B196D1520D7F3B1C032D3F735DB51F76BEDB007BEC0`; the subsequent complete Chromium rerun and exact-commit CI are now clean as recorded above, while the earlier failed full run is not relabeled a pass.
- The current server runtime rebuilt to1,752,157bytes, followed by all five migrations/four real rollback SQL suites and **4 actual Deno/private-Storage/reference-catalog tests passed** (45690). No paid provider call or human-audio approval is implied. Authenticated CLI metadata was freshly inspected at00:12: two unrelated projects and **no dedicated Jove production project**; none was modified. Scheduled screened supply, complete authentic life-task coverage, legitimate production AI/Speech, hosted owner login/sync and exact-SHA Pages release remain incomplete.
- [Content review packets](CONTENT_REVIEW_PACKETS.md) prepare six bounded, text-supported everyday-exchange candidates and explicit rights/acoustic/timing gaps. Independent read-only review approved the final document SHA256 `4FC727C04D41B56C0695784DD0EA470C21DB05655F6F48F2E8D3D4FF89924D60`; the reviewer could not freshly reload the historical Neighborhood near-match, so that one exclusion is not fresh independent web evidence. The packets add **zero eligible human clips**; the interview duplicates an existing pilot source, the bank lead has no verified human recording, and the rental/manager candidates require applicable permission. Further primary-source checks did not substitute an internal bank meeting, a bank employee's family conversation or an explanatory banking page for a customer transaction. No media download/playback, provider call, allowlist change, content approval, stocked lesson or continuing supply is implied. The local working-copy source/build secret-pattern check includes this new document and passes213 files; the committed CI scanned212, before this documentation-only addition.
- Next external activation gates are the dedicated Jove project, owner-only Auth/SMTP delivery, server-side AI/Speech credentials with approved budgets, and legitimate reviewed human material. Follow [the production operations runbook](OPERATIONS.md); never use another project's backend or put credentials in chat/Git. These are not merely settings that can be marked complete: actual provider, audio/rights/quality, scheduled supply, retention and public owner-device journeys must still pass afterward. Public main was rechecked at this checkpoint: `3912be41f43e4fc5c1473edb590604715a865316`, HTTP200, bundle `index-CviBBF9R.js`. Final upgrade remains NOT accepted.

### Earlier checkpoint trail — historical, superseded where noted above

- At23:53 main captured the third immutable candidate `.work/reading-sync-20260908-final3`: all15 source/test SHA256 values matched the independently reviewed frozen work. Its production build passed (1812 modules,102 PWA entries; index SHA256 `AA29082A2F94B109B8F1266A3E6EF216C5384DF3120247C8E5A768AFBD7E1D13`). The actual Chromium journey23743 passed the formerly failing original reading submission, its three durable events and the other device's offline unsent draft, then failed at the Settings navigation barrier. Main replaced only that helper's explicit document navigation with the actual Settings RouterLink, retaining all offline/save/recovery/evidence/budget assertions; independent static review approved the test-only change (journey SHA256 `694D61A5...`). The next real run remains an open gate, not a passing retry claim. SQL cleanup after23743 confirms members0/browser test-users0.
- Independent review closed the second frozen candidate's coherent-attempt, completed-budget, all-optional-container and metadata-only-CAS findings: protocol `AE16ACE6...`, ReadingPractice `682C8587...`, schema `A587C9F8...`. Separate component verification passed36 selected cases and4 virtual lifecycle probes; protocol/plan review includes actual reverse/paged projection and backup recovery. These approvals are bounded to those implementations and do not replace merged native-browser or production acceptance.
- The third candidate indexes started tasks after one global history validation, receiving raw events so contradictory same-ID/future copies cannot be hidden by prior filtering. Independent review approved exact engine `9E45CDBE...`, longitudinal `9A3DC464...`, and longitudinal tests `A052465A...`; two new independent regressions passed. The original60-day actual-store learning case56402 passed in43.14s (45.29s total) in an isolated test window with its original60000ms limit and all60-day assertions unchanged. Earlier timeout results remain real failures of the earlier candidates, not erased or treated as efficacy evidence. A same-process synthetic comparison measured the new planner at0.44–0.64x the slower frozen candidate's median cost; it does not establish whole-app latency or human learning gains.
- Latest pushed/committed source is now `1b2ddad16c36eb4d8ecfa4282d92fe932c6136aa`. CI `34243807683` is terminal **success**: **1243 unit/integration passes /47 opt-in skips**,9 real local Auth/Storage,96 content /7 opt-in skips,4 actual Deno/private-reference/catalog tests,2 Chromium/WebKit account journeys and **148 browser passes /3 inapplicable skips**. The strict-flaky gate was active, and the final audit found zero vulnerabilities. This proves the committed speech-discovery/large-catalog/Recorder changes, not the uncommitted sync/reading work. Main/public deployment remains unchanged.
- Main froze an immutable diagnostic source at `.work/reading-sync-20260908-2329`, based on that commit plus14 exact overlaid source/test files; pre/post source and destination SHA checks matched. Its build passed (1812 modules,102 PWA entries; index SHA256 `0976666838782E56BA5418C30E70ACEFA0B430CBA4E31B3D321632816004F283`). The new actual Chromium two-profile reading journey failed twice on this fixed app (63030/77023), before reconnect: original reading remained respond with zero response/retell/completion events, while the first local answer was diverted into a conflict copy. Native IDB diagnostics prove the conflict path, not a slow-button failure. Independent actual-SFC plus real-projector reproduction identified removal of the derived empty `syncReadingConflicts` field as the only normalization difference causing false CAS conflict. Fixtures were removed; SQL readback confirms members0 and browser test-users0. No native WebKit or successful full acceptance is claimed for this artifact.
- Five earlier component lifecycle findings were independently closed on frozen ReadingPractice `7E115365...` (33 selected tests plus independent virtual recovery/feedback probes). Later independent review found additional open gates: coherent response/retell events must belong to the same submitted attempt, lowering another device's budget cannot retroactively mark completed required work optional, and all-optional retained plans need a valid zero-required-minute container without zero-minute tasks. The normalization/CAS failure above is also open. Earlier bounded approval does not cover these newly reproduced issues.
- The six-suite working verification ended **388 passed /4 opt-in skips /1 timeout**; the unchanged60-day real-store learning case also timed out in a second run that partly overlapped other work. Neither is a passing suite or proof of the sole cause. A main same-process alternating planner probe independently measured frozen new code at2.14–2.27x the prior code's median cost across501/2001/5001 synthetic historical events. Repeated full-history validation in each started-task check is under investigation; no60-day assertion or timeout has been weakened.
- At the earlier checkpoint after rereading the full1604-line,47-section instruction, pushed source was `8c88a614877b5e7acac2d8cc5910a2912e1df685` and local HEAD was `6c97a2f`. GitHub run `34240236546` was terminal **success**: **1183 unit/integration passes /46 opt-in skips**,9 local Auth/Storage,96 content /7 opt-in skips,3 real Deno tests,2 Chromium/WebKit multi-profile journeys, and **148 browser passes /3 inapplicable skips with no flaky result**. Build/security and zero-vulnerability audit passed; main-only deployment was skipped. That CI did not include `d95d0bb`'s strict-flaky gate, `6c97a2f`'s reference-discovery fix, or later working changes. Public main and its observed HTTP200 asset remained `3912be4`/`index-CviBBF9R.js`.
- At23:10 the current server runtime built successfully (1,752,157 bytes), then all five local migrations/four real rollback SQL suites and **4 actual Deno/Auth/private-Storage tests passed**. The new catalog case uses104 disposable, explicitly artificial metadata rows to prove real PostgREST filtering/JSON ordering before the100 cap, scoped/global saved-reference recovery, owner/material isolation and revocation. It makes no provider request and reads zero service usage. Independent SQL readback confirms members0, test-user0 and both catalog/transport fixture-row counts0; these fixtures do not certify actual human speech, approved courses or production services. The new test includes an empty-member lease guard so a personal local account cannot be displaced.
- Independent review identified further open reading-conflict issues: response-only unsent branches, false conflicts during causal cross-device continuation, and a local transaction rollback advancing the component's saved-version fence too early. The optional-overcommit implementation also still needs Today wiring and final review: completed45minutes must stay complete while an extra offline-started9minute task retains its original identity/time as optional recovery. No unfinished patch or earlier scoped test result closes these findings. Implementation/review resumed after the owner's explicit continued-goal instruction.
- Historical run `34236277635` at `fde179a789554eea1e0c63e2afd3ece4912287f8` was terminal **success**, but its browser summary was **147 passed /1 flaky (passed on retry) /3 inapplicable skips**. Its capture/navigation issue was subsequently repaired and verified below; it must not be reported as a clean no-retry result.
- The exact integrated `C65C7883...` build completed **111/111 Chromium desktop/mobile/media/PCM/UX cases without retries** (runner55588, 4.4min), plus the earlier full native Linux WebKit **37 passed / 3 inapplicable skips** (runner68719, 2.9min, including all10 new longitudinal journeys). These are local regression evidence, not hosted owner/provider acceptance. Representative screenshots were inspected. A suspicious full-page mobile screenshot tail was separately checked in a fresh native WebKit regular profile: Today before/after capture and actual bottom scroll both read 2029px document height, 390px width, zero horizontal overflow and no page errors; viewport captures show a reachable footer with no blank scroll region. This does not certify every full-page screenshot or a physical iPhone.
- The CI capture/navigation race has a failed-first compiled-component reproduction: the original audio was committed and its completion badge visible while the local refresh still held the parent capture guard. Recorder now reports finishing local updates until that guard is released. All49 audio-component tests and scoped lint pass; a separate reviewer independently reran12 relevant lifecycle tests and approved the two-file fix. A frozen `fde179a` plus this exact template patch built successfully (index SHA256 `F5C4B09FF6DB72F645D6E53DF5415399513B17306CED82D74EA4E5F72D9A1672`); the unchanged real capture/save/navigation/original-byte test passed **16/16 native WebKit desktop/mobile repetitions without retries** (runner84320, 1.8min). This closes the demonstrated completion-indicator race, not a claim that every possible CI timing cause is excluded. The final merged artifact/CI still require fresh verification.
- A further independent cross-module review reproduced an open **P1** in same-day cloud plan projection: last-writer task-array replacement can drop the other device's started/completed assignment while preserving its underlying event, causing planned remaining time to be regenerated. Repair and failed-first actual-plan regressions are in progress. Do not classify sync/planning as fully accepted from the previously approved narrower scopes.
- Production metadata was read again through the authenticated bundled CLI: two existing unrelated projects and no dedicated Jove project. Neither other backend was modified; no credential or paid resource was created or inspected. Genuine screened content, reviewed General American reference audio, legitimate live provider calls and exact-SHA production owner journeys remain required.
- Recorder status repair is committed locally as `0765039`, with no deployment. The visual matrix now waits for each exact route heading (not any old visible heading after a hash change), fonts and top scroll, and keeps actual viewport captures alongside full-page images. On frozen `F5C4B09F...`, both native WebKit desktop/mobile all-route cases passed without retries (runner97476, 39.9s,36route/theme views). Fresh Today full-page capture is1170x6087 with no old black tail; representative Today/Speak/Learn captures were inspected. A separate reviewer checked every actual heading, the exact17-line diff and unchanged retry/timeout/skip/assertion boundaries, and approved this test-only strengthening (file SHA256 `54439102284E27ED9727FEFCEEF235317154450E2D58FDE6D2AA632EB8190474`). Concurrent new fixes address same-day task loss, torn saved/unsent reading snapshots with accessible recovery, and current-material speech references hidden behind a global100-row limit.
- Commits `0765039` and `8c88a61` are now pushed on the feature branch; no main merge or production release occurred. A one-line `failOnFlakyTests: !!process.env.CI` gate is independently approved in the current worktree: installed Playwright types/runner and official documentation agree, actual local/CI config reads retain0/1 retries and all7 projects while toggling this stricter failure condition. The next CI including this line must fail if any test only passes on retry. This strengthens reporting; it does not itself repair a flaky test.
- The independently identified global100-reference discovery defect is repaired in a frozen five-file speech patch. Failed-first tests also reproduced a stranded pending recording when its still-approved saved reference moved past100; validated preferred-reference lookup now restores that exact reference without exceeding the100 response cap or bypassing owner/material/revocation/voice checks. Main independently reviewed the final handler/client/session flow and reran all **237 speech/handler/component/security tests**, all passing. Installed SDK query serialization is tested, and a separate actual read-only query through the dedicated local PostgREST returned HTTP200/count0 for the material/JSON-order/limit syntax. These do not claim a populated live catalog, human-voice approval or paid acoustic accuracy; final merged server/browser/CI verification remains required. CI `34240236546` at `8c88a61` is a separately observed running job, not proof of this uncommitted speech change or the newer strict-flaky gate.

- At 22:05 the integrated root passed lint/typecheck, **1182 tests / 45 opt-in skips (1227 total)** and the 212-file source/build secret-pattern scan. Latest successful build index SHA-256 is `C65C7883CF8B6B0E865401268E6F98F9149A8800D4478F5A524475E0C2FADDF6`, including the reading retry fix. The prior 1175-test checkpoint and `5A7E15BB...` / `3D952D88...` artifacts are historical. Dependency audit at the preceding unchanged-dependency checkpoint reported zero vulnerabilities.
- The WebKit recording failure was isolated without Auth/network services using the actual native IndexedDB and SyncJournal: re-putting the same file-backed Blob could produce `NotFoundError` while preserving apparent size/type. Shared metadata updates now skip unchanged byte writes and materialize independent original bytes only for a real metadata change, atomically. This protects sync replay, Recorder's processed status and generated-audio duration. Failed/short reads roll back rather than replacing the original. Four failed-first/incremental sync regressions plus compiled component coverage passed; the independent reviewer reran all **115 sync/component tests** and approved the final helper and all three call sites.
- Actual native Linux WebKit journey **27435 passed (1.8 minutes)** against the dedicated local backend, using the frozen pre-learning-repair frontend plus the audited audio changes: real OTP, two offline profiles, evidence union, private original upload/download, reset/restore/reload and member isolation. The same run first exercised actual native journal/local metadata updates and multipart round-trips at **8 bytes (12 cycles), 64 KiB (6), 25 MiB (2)** with SHA-256 equality, changed label/processed/duration readback and IDB close/reopen. Earlier single-point fix journey 13598 also passed. Main independently confirmed zero remaining fixture memberships. These prove local browser behavior, not physical iPhone performance or production sync; 30-second per-read timeout does not cancel underlying I/O. The final integrated feature CI remains required.
- The reading/chunk automatic-plan repair preserves old tasks/drafts, gives reading and deliberate language separate slots, requires actual chunk/written submissions and connects the next assigned task. The independent review's same-page retry notification finding is closed: two actual aborted native IDB commits followed by successful same-page retry must show Continue, retain both original responses and keep a single completion. The implementer reports **211 domain/longitudinal/reading tests plus 10 actual desktop/mobile journeys passed**. A separate reviewer independently reran **211 tests / 4 browser opt-in skips**, checked the retry/identity fences and approved the exact 13-file learning/test/config patch with no remaining P0/P1/core-P2 in that scope. No ability is inferred from participation or the 60-day simulation.
- The first full `5A7E15BB...` Chromium matrix ended **103 passed / 4 failed**: two old route contracts on desktop/mobile. Independent source review approved updating those tests to exact saved-assignment routing, with stronger source-session/event, submitted-writing and no-false-completion assertions; a separate free-listening test preserves the original source-session route and forbids extra assigned work. Main reran all six relevant desktop/mobile cases on `C65C7883...`: **6/6 passed**, then obtained the full local matrix results recorded above. Newer working changes still require their own integrated artifact verification.

### Previous checkpoint — superseded where the latest checkpoint differs

- The clean-dependency snapshot `.work/clean-upgrade-20260908-1742` passed lint/typecheck, **1142 tests / 44 opt-in skips**, and frontend/server builds after the frozen content/gateway merge. The root passed lint/typecheck/build and **1143 tests / 44 opt-in skips (1187 total)** at 19:12. After the additional cold-start and six-candidate persistence guards, root lint/typecheck and **1155 tests / 45 opt-in skips (1200 total)** passed at 19:35; source/build scan passes 210 text files. Dependency audit reports zero vulnerabilities. Skipped live/provider cases are not counted as successes.
- Exact frontend index SHA-256 `3D952D88FB3E71C6B072B6FF6BE0BC4C99011769C1B371792E3FB8CD58C62066`: independently tested **105/105 Chromium**, then **27 native Linux WebKit passed / 3 inapplicable / 0 failed**, without retries or concurrent audio runners. The artifact hash was unchanged afterwards. Tests/config/assets all came from that same snapshot. Three inapplicable cases remain the two no-recorder-API fallback conditions when PCM is actually available and one desktop-only touch case. Physical iPhone/microphone and production cloud are not implied.
- The latest root build and post-content clean build contain the same **66 files with identical SHA-256 hashes** as that frozen frontend. Gateway and content-worker additions do not alter this browser artifact or its existing UI. Browser reports remain `.work/final-3d952d88-chromium-report` and `.work/webkit-linux-report/final-3d952d88`; owned temporary preview processes were stopped after verification.
- Independent AI/Speech review closed the earlier cross-owner SSE/recording, late cached-result/retry and initial-auth-hang findings. Latest Speech checkpoint passed 234 narrow tests plus independent actual SFC/native-IDB probes for timeout/retry, same-owner long idle, A→B→A isolation and original-byte preservation. Revoked reference audio now returns the required 422; prior 503 notes below are historical.
- Current actual local server bundle is **1,751,305 bytes**. At 19:09–19:10, the exact dedicated local backend passed all five migration checks/four rolled-back SQL suites, **3 actual Deno/Auth/private-Storage tests**, then **9 actual Auth/PostgREST/Storage tests**, then **94 content tests / 6 opt-in skips**, sequentially. These include nonmember denial, withdrawn reference rejection, real content persistence and rights-notice withdrawal. All test-owned synthetic fixtures were cleaned up; none certify genuine audio or paid services.
- Added CI startup/readiness/cleanup for that same real Edge test. Added a default-closed, exact-commit/project, 24-hour release hold at both artifact upload and deployment, with bounded live handler probes. Its policy/failure tests are separate from actual production acceptance. `OPERATIONS.md` specifies dedicated backend, hosted Auth/SMTP, owner bootstrap, services, content and scheduler activation and readback.
- Live metadata readback still lists no dedicated Jove production backend; only unrelated projects exist and remain untouched. Public Pages still serves V1 `3912be41f43e4fc5c1473edb590604715a865316`. No readiness approval or provider secret has been added to GitHub. Do not replace the public site with an unprovisioned upgrade.
- The first feature CI run `34217036697` at `116fcc2` failed on Node 24.20: an already-aborted buffered request was accepted and completed multipart streams could be cancelled twice. The gateway now prioritizes known cancellation/deadline state and preserves EOF cleanup; failed-first regressions and independent native-stream review cover the fix. CI also separates Auth and content fixtures into sequential steps so their independently provisioned owners cannot interfere. A new complete feature-branch CI run remains required; main/public Pages has not been replaced.
- Feature CI `34219681408` at `3470452` confirmed **1143 tests passed / 45 Linux opt-in skips**, but still failed on one deferred Undici multipart-encoder error. A Node 24.20 reproduction with no application imports proves that cancelling a partially read outgoing FormData encoder produces the same closed-stream rejection. The oversized handler fixture now supplies the encoder's actual serialized wire bytes and original boundary, retaining the 413/no-provider assertions and adding a byte-size assertion. No runtime cancellation, size limit or unhandled-error policy was weakened. Twenty native Linux oversized wire requests and a real localhost chunked HTTP upload pass: 413, incoming stream destroyed, reader unlocked, zero unhandled errors. Windows handler/gateway tests pass 37/37; the complete replacement CI is still required.
- Replacement CI `34220201239` at `ceb5641` passed install/lint/typecheck and the entire unit-test step with no unhandled stream failure. It then failed during cold local backend startup, before HTTP/SQL/Edge/browser acceptance; the prior credential-safe wrapper withheld the underlying CLI detail. CI now builds the configured functions' ignored runtime dependency before startup and reports only allowlisted failure categories/SQLSTATEs and bounded exit/signal metadata. Ten failed-first diagnostic/dependency-order checks now pass, with no raw CLI details or credential values emitted. The actual startup cause and next full-CI outcome remain unproven; health checks are not bypassed.
- CI `34220889325` at `244d647` then passed cold startup, real Auth/Storage, content persistence, actual Deno/private-audio authorization, build and security checks. Its Chromium multi-profile journey passed; WebKit failed at reconnect with metadata converged but original recording synchronization still pending. The run is terminal failure, not a full CI/browser pass. Native reproduction and safe fixture-byte/transport diagnostics are in progress; no retry count, timeout, sync assertion or production guard has been weakened.
- The frozen six-VOA candidate and 17-life-task inventory patch passed independent review, including three reproduced and repaired rights/timing/topic-selection findings. All six exact publisher pages and MP3/frame probes passed. Actual metadata persistence/readback now also passed: run `044e4ac8-ad9c-4bd0-8ce2-31104f4dc8f0`, 19:29:51–19:30:08, six real pending candidates retained. The precisely authorized existing three RSS queue entries were conservatively reset for the genuine source configuration upgrade, with all original metadata hashes unchanged; no existing segment/recommendation existed. Main independently read back members0, VOA items9/pending9, VOA segments0 and global eligible segments0. The test/doc increment passed independent review after correcting evidence attribution. No genuine clip has passed full eligibility, and key life-scenario coverage plus recurring production supply remain open. See `OPERATIONS.md` and `CONTENT_PIPELINE.md`; candidates and credentials do not establish reviewed coverage.
- An additional targeted learning-loop review found two core issues under implementation: the sole planned learn slot can absorb deliberate-language time into reading, and the automatic Listen→Learn path can omit/misidentify the assigned reading task. The old Learn page and written responses remain available; the defect is automatic budget/task wiring, not absence of all written practice. Existing independent UI evidence does not close these functional findings.

### Earlier chronological integration evidence — superseded where noted above

- Latest resumed checkpoint: stable recording playback now compares actual SHA-256 for rehydrated Blobs, preserves unchanged URLs and rejects stale comparisons after recording switches/unmount. Four failures preceded the patch; all 47 compiled audio-component tests pass. Independent audio/component rerun: 83 passed. Frozen build `3c1521f90819` subsequently passes 27 desktop/mobile Linux WebKit cases with the same three inapplicable cases; cold offline recordings retain original bytes/duration and play past one second without changing their source. The matching full Chromium rerun is separate; physical iPhone/keyboard/microphone validation is not implied.
- Mobile Speak tabs now retain their natural width inside the existing scrollable strip. A failed-first DOM text-range check reproduced the overlap; both themes pass padding bounds, actual hit-testing, scrolling and first/last tab taps. No page framework or visual redesign was introduced.
- A shared synchronous auth-principal fence subscribes before the first SDK/IndexedDB await. Sign-out or A→B→A permanently invalidates the old request; same-owner refresh is allowed. Seven policy tests pass. Account usage adds six actual-component event/lifecycle tests; services add last-owner-await and post-initialization sign-out regressions (14 passed). Content adds two failed-first ABA/transaction regressions (28 passed plus seven actual-store coordination tests). AI and Speech are integrating this same boundary and remain subject to independent recheck.
- The new `check:cloud-local` script verifies exact workspace/container/API identity, all five migration versions and four real SQL suites; each fixture transaction rolled back successfully. CLI credentials and raw subprocess errors are never printed. CI now includes these local SQL/HTTP gates, the server bundle and Chromium/WebKit installation. Pages publication requires both validated public cloud variables; an unconfigured upgrade cannot overwrite V1. This is release protection, not evidence of a configured production backend.
- Latest local full dependency audit reports zero vulnerabilities; the expanded source/build scan covers SQL, source maps, Supabase secret/JWT and literal Speech key patterns and passes 194 files at this checkpoint. Pattern scanning is not an independent security verdict. Latest main typecheck passed after correcting test subscription types; concurrent Speech/reference changes still require a final whole-tree rerun.

- Later integration checkpoint: whole unit/integration run passes **1058 tests with 40 opt-in cases skipped** (1098 total); these skipped live/browser/provider cases are not claimed passed by that run. The independent frozen browser matrix is now **105/105 Chromium** and **27 WebKit passed / 3 inapplicable**. Separate real local Chromium/Firefox/native-Linux-WebKit multi-profile journeys pass SMTP sign-in, V1 migration, offline evidence/audio, old/new Review identity, reset/merge and member isolation. WebKit uses a verified HTTP network partition because Playwright's offline flag also breaks local Blob reads; no Blob mock or physical-iPhone claim.
- Independent main-scope review prompted further repairs: main-only Pages deployment; SQL assertions that reject missing/null fields; environment/private-key-container filename rejection without reading their contents; abort cleanup even while post-auth database IO remains pending. Corrected service/content/auth tests pass 52 cases. Twelve actual-scanner fixture tests and four real rollback SQL suites pass; source/build scan now covers 200 files. The reviewer reproduced cancellation cleanup and invalid-summary rejection independently.
- Rebuilt actual Deno runtime is now 1,717,868 bytes. New local Edge integration proves all three routes reject anonymous callers and the owner's exact private reference WAV passes real Storage upload, internal signing/download and SHA validation; nonmembers are denied. The synthetic transport-only reference/user/object are removed after each run and do not count as approved voices. Revocation currently returns 503 instead of the required 422 because domain errors were normalized; a targeted error-preservation fix is in progress. No paid Azure call or production backend is claimed.

- Dedicated **local** Supabase runs on 55321/55322/55324, with no changes to unrelated cloud projects. Migrations 001–005 are now persistently applied; SQL readback confirms all five versions and RLS on service usage/results, content clip storage metadata and acoustic assessments. Earlier real Postgres transactions proved owner/RLS/private-storage policies, dispatcher idempotency, day/month budget holds, account usage aggregation and nonmember denial. New hardening HTTP/browser revalidation is in progress.
- Actual Deno 2.1.4 local Edge runtime serves AI, content and speech-assess. The rebuilt runtime is 1,700,753 bytes at this checkpoint, with no frontend store imports or credential-pattern match. The local public signed-audio origin is configured separately from internal Kong. A successful bundle is not provider or production acceptance.
- Main regression: 133 provider/domain/gateway tests passed. Nine cloud-provider tests cover owner binding, private retry IDs, account usage separation, late-account rejection and real SSE parsing. Nine AI-handler/pricing/PCM tests pass with actual provider adapter + simulated upstream: each dispatch/fallback reserves separately; uncertain charges are retained; known costs are not doubled by logical request records. These tests spend no money and do not prove live model quality.
- Account usage/preferences UI now reads UTC day/week/month server aggregates, separately labels known/held cost, and edits account budgets/recording retention online. Real account browser acceptance is pending. Hosting/storage/egress bills are explicitly unreported, not zero. Current public model catalogs and price-unit documentation were inspected; production models, speech quality, usage and actual provider cost still need live verification.
- Longitudinal worker reports 187 domain/reading tests and four desktop/mobile reading-assessment/extra-review journeys passed. Five-part assessment includes reading; saved text/time does not automatically earn skill. Main store preserves started tasks after preference changes. Whole-project tests/build are still integrating concurrent changes and must be rerun.
- Content and speech SQL each passed isolated real Postgres create/test/rollback. Their provider adapters and pages are integrating. No real audio has yet passed the full production content/reference review pipeline, no scheduled production refresh exists, and no legitimate paid LLM/STT/Speech invocation is claimed. These remain core delivery gaps, not optional follow-ups.
- Automatic client delivery now validates screened lesson/clip/sentence metadata, retains attribution, saves no signed URLs, hashes actual downloaded bytes and caches only short clips. Today coordinates login/onboarding, background refresh, limited prefetch and reconnect; approachable human content leads new listening plans without replacing assigned work. Seven actual-store coordination tests and 26 content-client tests pass. These use transport fixtures, not a claim of real human course approval.
- Independent review reproduced incorrect range completion from elapsed wall time, buffering and seeking. Three failing regressions preceded the correction; completion now requires actual media position, with waiting/seeking guards and no forced advance. Independent rerun passed 40 audio-component and 26 content-client tests. Actual quota/private-storage failures preserve verified audio for the current play and mark it not saved offline; recordings are never evicted for lesson cache.
- Independent AI review found cross-account late SSE delivery, transient result-cache loss, an overlong reading evaluation kind and cancellation overwriting known fees. Fixes and regression tests are in progress; these findings are not closed merely because the previous contracts passed. Full integration review and refreshed browser/build gates remain mandatory.
- Independent final integration review, complete Chromium/WebKit/mobile suite, dedicated production backend, credentials/owner membership, exact-SHA Pages release and fresh production learning journeys remain unproven. Do not deploy this worktree over the accepted V1 until the release gates pass.

## Historical V1 acceptance

V1 application acceptance passed on 2026-09-08. Public website: https://mnijc19-netizen.github.io/jove-english-os/. Full approved scope is preserved. Release `0d8efcebe8283dab101d686dd4df1fc23c08ffa4` passed Actions `34156243356` and exact-SHA Pages deployment `6315152368`; it serves version `1.0.0` and `index-CviBBF9R.js`. All 78 public desktop/mobile application cases and the separate real production upgrade proof passed. Later documentation-only publication must retain this runtime and pass the same automatic workflow before handoff.

Project-only Astra/Ultra, the active branched root runtime and both retained reviewer/test-agent runtime records were verified. Global Sol/max and other projects remain unchanged. The existing public source repository retains its original visibility. No owner credential, paid AI request or new purchase was used.

## Evidence obtained

- Working Vue application with all pages, database/FSRS, provider/audio, six audited original demo materials and 36 real synthesized WAV assets.
- Clean `npm ci` succeeded. Latest main run at 03:24 Asia/Shanghai: 265 unit/integration tests passed, one optional public-catalog test skipped. The live public model catalog separately passed at 02:24; no credential or paid request was used.
- The earlier 78-case built-app suite passed, including Recorder active-state guards, delayed evaluation identity, cross-modality transfer novelty, read-only transcript disclosure, keyboard skip navigation, and storage/error/reload regressions. The final expanded suite adds six real-worker update cases: 84/84 passed locally and in clean Ubuntu verification before deploying `index-CviBBF9R.js`.
- All nine pages visually inspected in desktop/mobile and light/dark. The same 36-view capture was repeated on production, with no document overflow or uncaught/console errors. Recording, saved audio, import, pending request, API error and offline states also passed on production in both viewports. Compact playback and a scrollable readable weekly table remain intact.
- Final lint/typecheck/build passed, dependency audit reports zero vulnerabilities, and source/build secret-pattern scan passed across 90 text files. Third-party Rollup comment-annotation and npm/Actions deprecation notices are nonfatal; no runtime issue or audited vulnerability is claimed from them.
- Independent read-only review: APPROVED. All nine later findings below closed; the last disclosure regression and cold reload each passed desktop/mobile independent verification.
- Additional keyboard audit caught the skip-link `#main` fragment being interpreted as a router path and returning to Today. The link now prevents route navigation and focuses a programmatically focusable main landmark. Independent desktop/mobile verification confirms route preservation and subsequent Tab navigation.
- Extra local desktop/mobile audit actually completed a queued review while offline, reloaded, and verified the persisted FSRS repetition/due date. It also verified manifest/icon HTTP responses, SW scope, no stored credentials, no uncaught/console errors, and form typing not triggering recording shortcuts.
- Fresh public production browser journeys: 10/10 passed on desktop/mobile Chromium. Coverage: onboarding/demo, listening/recall/review, real browser recording with synthetic microphone input, backup/restore and corrupt-file rejection, no-key behavior, intercepted API error with retained audio, hash routes/reload, dark theme, offline shell and actual cached audio.
- Extra production audit passed desktop/mobile: HTTP 200, manifest/start URL/icons/SW scope, actual public provider model refresh (415 text models plus placeholder), offline review completion and durable FSRS due/repetition after reload, no credentials, no uncaught/console errors or horizontal overflow.
- Local two-artifact SW upgrade passed on pages reloaded after the initial controller was installed. The instrumented public probe then captured activated controller/worker, no waiting worker, retained draft, but an unchanged old document. Reproducing without the local pre-update reload confirmed the defect: Workbox captures isUpdate=false on initial uncontrolled registration and its later controlling callback does not reload. This is a first-install-document lifecycle bug, not data loss or an AI failure.
- The correction gates a native controllerchange reload on the user's explicit Update now request, uses the same guard for the plugin callback, and handles a worker already activated by another tab. Bounded recovery restores both flags and retry after cancelled reload (2s) or stalled activation (15s). Independent review approved both the handoff and recovery fix. Local lint/typecheck/build, 265 unit/integration tests and secret scan pass; bundle is `index-CviBBF9R.js`.
- Six new real-worker browser cases pass desktop/mobile: first uncontrolled document, controlled page after reload, and real beforeunload cancellation followed by save/retry. The last case verifies the same original audio ID, byte size and SHA-256 after update. Tests use actual built SW/workbox assets served as two document revisions; no lifecycle events or reload functions are mocked. The expanded 84-test local suite passed in 2.9 minutes.
- CI run `34154677869` correctly stopped before deployment on a test synchronization race: a fixed microtask flush did not guarantee asynchronous cache hashing had reached provider dispatch. The stale-request and unmount tests now await actual dispatch, then assert cancellation; stale playback must use the exact fresh Blob. No product behavior, timeout or assertion was weakened. All 35 component tests passed five repeated runs and an independent reviewer run; the full 265 tests, lint/typecheck/build and 90-file secret scan passed. Replacement CI/deployments `34155555000` and `34156243356` succeeded.
- The first expanded public run retained 76 passes and two initial-cache timeout failures (desktop/mobile); their traces remain in the ignored test artifacts. Public audio precaching took longer than the previous 10s localhost-oriented assertion. Only this case now allows 60s to observe actual SW control and 90s overall; offline playback, routes, theme, reload and data/error assertions are unchanged. Independent review approved the change. The complete new public run passed 78/78 in 5.1m without retries, with separate `.work/production-78-results` and `.work/production-78-report` artifacts.
- Actual public upgrade from deployed `5eecfcc` to `0d8efce` passed for desktop and full Pixel 7 emulation. Observation-only UUIDs prove the first uncontrolled document survived initial worker control and the later update prompt without reloading; explicit Update now produced a new controlled document with version `1.0.0` and the exact saved listening response. Both error arrays are empty. Result: `.work/production-update-result.json`. This is a live Pages-to-Pages upgrade, not a local worker simulation.
- The latest public visual recapture passed all 36 route/theme/viewport combinations and recording/saved-audio/import/loading/API-error/offline states. Representative current screenshots were inspected; no overflow or uncaught errors. The separate public audit again confirmed actual model-list access, offline review completion and preserved FSRS state on the final application bundle.
- All 60 deployed files were fetched and compared to the local production build: 58 are byte-identical. `audio/manifest.json` differs only by the 285 Windows CRLF line endings; normalized content is identical. `sw.js` differs only in case-sensitive filesystem enumeration order: all 96 precache URL/revision entries and the remaining worker logic are identical. No application, CSS, audio, icon or HTML content mismatch remains.

## Independent review on 2026-09-08 — approved after fixes

The independent reviewer reproduced nine issues, then rechecked the fixes on the final production preview. All are now closed, with no remaining P0/P1/core-loop P2 in the reviewed scope. Original findings and resolutions:

1. Text-only onboarding now omits absent audio references.
2. Finish/mode changes cannot hide unsent conversation work.
3. Transfer selection uses normalized cross-modality card/event history and preserves the begun prompt.
4. Learn evaluations bind immutable submitted text/material/session/task snapshots.
5. Merely revealing Learn's transcript persists exposure; subsequent one-play listening is prompted, not fresh first-pass evidence.
6. Assessment re-entry transactionally resumes the persisted draft instead of using a stale store snapshot.
7. Learn navigation alone cannot complete a task; actual saved retrieval/output is required.
8. Repair workflow stage survives cold reload independently of the conversation's mode.
9. Recorder enforces persisted audio capacity before capture and before transactional save; failures preserve recoverable input.

Actual-date midnight rollover and all corrected regression assertions pass on desktop and mobile. Synthetic test microphone input proves browser recording integration, not the owner's physical microphone. Mock-provider browser responses do not prove paid-provider availability.

## Acceptance evidence map

- CONFIG/SPEC: project config, AGENTS and all permanent docs; actual active runtime plus official configuration precedence checked.
- DOMAIN/DATA: 24 domain and 69 database tests cover adaptive plans, evidence boundaries, six independent FSRS cards, idempotency, delayed repair, migrations and atomic secret-free backups.
- CONTENT/PROVIDER: 16 content and 101 provider tests; six reviewed demos/36 WAVs, import provenance, contextual lookup, dynamic catalogs, structured evaluation, streaming, retry/cancel/budget and STT/TTS contracts. Public catalog separately tested live.
- AUDIO: 20 audio and 35 component tests plus browser MediaRecorder playback/storage/error journeys.
- ONBOARDING/LISTEN/SPEAK/REVIEW/PROGRESS: all 78 desktop/mobile application E2E cases passed again on the actual public site; source review checks all four modes, 13 missions and evidence-based assessments. Tests cover the complete input/retrieval/output/repair/delayed-review/transfer/evidence/plan chain, not just page navigation.
- SECURITY/UX/PWA: secret/dependency scans, independent review, nine-route light/dark desktop/mobile visual checks, browser keyboard/record/error/offline states, scoped manifest/SW and real cached-audio replay. Six local/CI real-worker update cases plus the separate actual public update proof pass.
- LOCAL/REVIEWER: clean install and all local gates passed; independent source approval obtained.
- RELEASE: final application and versioned HTML commit passed automatic clean verification/deployment. Pages uses workflow builds and enforced HTTPS; public HTTP 200 and exact deployment SHA match the corrected application. Source visibility is unchanged. README covers startup, maintenance, backups, initial offline download and safe updates.
- PRODUCTION: 78/78 public application cases, visual/state checks, offline review, public model catalog and instrumented first-install update all passed. AI responses in automated conversation/assessment/repair tests are intercepted contract fixtures; this does not certify paid-provider availability.
- DELIVERY: the directly usable URL and first-use instructions are in README and the task handoff. No required application implementation or known core-loop defect remains within the verified scope.

## Usage limits and operating notes

1. Paid-provider smoke is conditional on a legitimate available key; none has been supplied or discovered. Chat, personal AI evaluation, STT and generated TTS require the owner's configured browser key. Contract/failure tests and public catalog access do not certify paid execution. No key is needed for built-in demo listening, local recording and basic reviews.
2. Desktop and mobile acceptance used Chromium, including Pixel 7 emulation. Synthetic test microphone input verifies browser recording/storage behavior, not the owner's physical microphone or every mobile browser. Dedicated phoneme/acoustic scoring is intentionally not fabricated.
3. Stay online on the first visit until Ready for offline practice appears. An early pre-release tab may need one manual refresh after saving work to acquire the corrected updater. Subsequent first-install update behavior passed the real public test.
4. Learning data stays in this browser without automatic cloud sync. JSON backups exclude secrets and audio blobs; download important original recordings separately. Demo audio is clearly labeled synthetic, not natural-speaker or standardized proficiency evidence.
